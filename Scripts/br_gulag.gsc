// IW8 DBG GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

initgulag()
{
    level.respawncircleinterppct = getdvarfloat( "scr_br_respawn_circleInterpPct", 0.75 );

    if ( !istrue( level.usegulag ) )
        return;

    if ( scripts\mp\gametypes\br_gametypes::isfeaturedisabled( "gulag" ) )
    {
        level.usegulag = 0;
        return;
    }

    setomnvar( "ui_gulag_state", 1 );
    setomnvar( "ui_gulag_show_closing_state", 0 );
    level.gulag = spawnstruct();
    level.gulag.arenaflag = getdvarint( "scr_br_fc_flag", 1 );
    level.gulag.maxplayers = getmaxplayers();
    level.gulag.maxuses = getdvarint( "scr_br_fc_max_uses", 1 );
    level.gulag.endonshutdown = getdvarint( "scr_br_fc_end_on_shutdown", 3 );
    level.gulag.timelimit = getdvarint( "scr_br_fc_timelimit", 15.0 );
    level.gulag.maxqueue = getdvarint( "scr_br_fc_max_queue_wait", 3 );
    level.gulag.onekillwin = getdvarint( "scr_br_fc_one_kill_win", 1 );
    level.gulag.multiarena = getdvarint( "scr_br_fc_multi_arena", 1 );
    level.gulag.planerespawn = getdvarint( "scr_br_fc_plane_respawn", 0 );
    level.gulag.introcinematic = getdvarint( "scr_br_fc_intro_cinematic", 1 );
    level.gulag.usecellspawns = getdvarint( "scr_br_fc_useCellSpawns", 1 );
    level.gulag.usefloorrocks = getdvarint( "scr_br_fc_useFloorRocks", 0 );
    level.gulag.spawnloot = getdvarint( "scr_br_fc_spawnLoot", 0 );
    level.gulag.lethaldelay = getdvarint( "scr_br_fc_lethalDelay", 4 );
    level.gulag.streamtimeout = getdvarint( "scr_br_fc_prestream_geo_timeout", 9 );
    level.gulag.numarmorhealth = getdvarint( "scr_br_fc_numArmorHealth", 0 );
    _setdomflagiconinfo( "waypoint_captureneutral", "neutral", "MP_BR_INGAME/DOM_CAPTURE", 0 );
    _setdomflagiconinfo( "waypoint_capture", "enemy", "MP_BR_INGAME/DOM_CAPTURE", 0 );
    _setdomflagiconinfo( "waypoint_defend", "friendly", "MP_INGAME_ONLY/OBJ_DEFEND_CAPS", 0 );
    _setdomflagiconinfo( "waypoint_defending", "friendly", "MP_INGAME_ONLY/OBJ_DEFENDING_CAPS", 0 );
    _setdomflagiconinfo( "waypoint_contested", "contest", "MP_INGAME_ONLY/OBJ_CONTESTED_CAPS", 1 );
    _setdomflagiconinfo( "waypoint_taking", "friendly", "MP_INGAME_ONLY/OBJ_TAKING_CAPS", 1 );
    _setdomflagiconinfo( "waypoint_losing", "enemy", "MP_INGAME_ONLY/OBJ_LOSING_CAPS", 1 );
    level.gulag.laststandplayers = [];

    if ( istrue( level.gulag.arenaflag ) )
        scripts\mp\gametypes\br_dom_quest::setupdom();

    level.gulag.arenas = gulaggetarenas();
    gulaggesturesinit();
    gulaginitloadouts();

    if ( level.gulag.introcinematic )
        scripts\mp\utility\lui_game_event_aggregator::registeronluieventcallback( ::playercinematiccompletecallback );

    if ( level.gulag.usefloorrocks )
        scripts\engine\scriptable::scriptable_addusedcallbackbypart( "brloot_rock", ::rockused );

    level.gulag.betting = getdvarint( "scr_br_fc_betting", 0 );

    if ( level.gulag.betting )
    {
        if ( !istrue( level.br_plunder_enabled ) )
            level.gulag.betting = 0;
    }

    foreach ( var_1 in level.gulag.arenas )
        level thread monitorgulag( var_1 );

    thread spawnac130();
    initvo();
    level.gamemodemolotovfunc = ::recordmolotov;
}

_setdomflagiconinfo( var_0, var_1, var_2, var_3 )
{
    level.waypointcolors[var_0] = var_1;
    level.waypointbgtype[var_0] = 1;
    level.waypointstring[var_0] = var_2;
    level.waypointshader[var_0] = "ui_mp_br_mapmenu_icon_gulag_overtime_objective";
    level.waypointpulses[var_0] = var_3;
}

initvo()
{
    game["dialog"]["gulag_spawn"] = "gulag_spawn";
    game["dialog"]["gulag_spawn_rules"] = "gulag_spawn_rules";
    game["dialog"]["gulag_objective"] = "gulag_objective";
    game["dialog"]["gulag_next"] = "gulag_next";
    game["dialog"]["gulag_win"] = "gulag_win";
    game["dialog"]["gulag_lose"] = "gulag_lose";
    game["dialog"]["gulag_teammate_gulag"] = "gulag_teammate_gulag";
    game["dialog"]["gulag_teammate_lose"] = "gulag_teammate_lose";
    game["dialog"]["gulag_teammate_win"] = "gulag_teammate_win";
    game["dialog"]["gulag_gulag_active"] = "gulag_gulag_active";
    game["dialog"]["gulag_gulag_close"] = "gulag_gulag_close";
    game["dialog"]["gulag_noenemy"] = "gulag_noenemy";
    game["dialog"]["gulag_timeout"] = "gulag_timeout";
    game["dialog"]["gulag_buyback"] = "gulag_buyback";
    game["dialog"]["gulag_taunt"] = "gulag_taunt";
    game["dialog"]["gulag_obj_wait"] = "gulag_obj_wait";
}

gulaggetarenas()
{
    var_0 = scripts\engine\utility::getstructarray( "gulag", "targetname" );
    var_0 = scripts\engine\utility::array_randomize( var_0 );

    if ( scripts\mp\gametypes\br_public::isbotpracticematch() )
        var_0 = scripts\engine\utility::getstructarray( "gulag_tutorial", "targetname" );

    for ( var_1 = 0; var_1 < var_0.size; var_1++ )
    {
        var_2 = var_0[var_1];
        var_2.gulagindex = var_1;
        setuparena( var_2 );
    }

    return var_0;
}

setuparena( var_0 )
{
    var_0.jailspawns = [];
    var_0.fightspawns = [];
    var_0.cellspawns = [];
    var_0.gates = [];
    var_0.floor = [];
    var_0.weapons = [];
    var_0.molotovs = [];
    var_0.jailedplayers = [];
    var_0.arenaplayers = [];
    var_0.matches = [];
    var_0.loadingplayers = [];
    var_0.fightover = 1;
    var_0.omnvardata = [];
    var_0.omnvardata["ui_br_gulag_players_1"] = 0;
    var_0.omnvardata["ui_br_gulag_data"] = 0;
    var_1 = [];
    var_2 = scripts\engine\utility::getstructarray( var_0.target, "targetname" );

    foreach ( var_4 in var_2 )
    {
        if ( var_4.script_noteworthy == "prison_spawn" )
        {
            var_0.jailspawns[var_0.jailspawns.size] = var_4;
            continue;
        }

        if ( var_4.script_noteworthy == "fight_spawn" )
        {
            var_0.fightspawns[var_0.fightspawns.size] = var_4;
            continue;
        }

        if ( var_4.script_noteworthy == "cell_spawn" )
        {
            var_0.cellspawns[var_0.cellspawns.size] = var_4;
            continue;
        }

        if ( var_4.script_noteworthy == "gulag_center" )
        {
            var_0.center = var_4.origin;
            continue;
        }

        if ( var_4.script_noteworthy == "spectator" )
        {
            var_0.spectatepoint = var_4;
            continue;
        }

        if ( isdefined( var_4.script_parameters ) && var_4.script_parameters == "gulag_loot" )
            var_1[var_1.size] = var_4;
    }

    if ( !isdefined( var_0.center ) )
        var_0.center = getgulagcenter( var_0 );

    if ( getdvarint( "scr_br_fc_shifted_spawns", 1 ) > 0 )
        var_0.fightspawns = scripts\engine\utility::array_sort_with_func( var_0.fightspawns, ::comparescriptindexobscuredspawns );
    else
        var_0.fightspawns = scripts\engine\utility::array_sort_with_func( var_0.fightspawns, ::comparescriptindexsmalltolarge );

    foreach ( var_7 in var_0.cellspawns )
        setupcellspawn( var_0, var_7 );

    var_0.cellspawns = scripts\engine\utility::array_sort_with_func( var_0.cellspawns, ::comparescriptindexsmalltolarge );

    if ( istrue( level.gulag.spawnloot ) )
        var_0.weapons = spawnlootweapons( var_1 );

    spawnrocks( var_0 );

    if ( istrue( level.gulag.arenaflag ) )
        setupdomendflag( var_0 );
}

gulaggesturesinit()
{
    level.gulag.gestures_enabled = getdvarint( "scr_br_fc_gestures", 0 );

    if ( !istrue( level.gulag.gestures_enabled ) )
        return;

    level.gulag.gestures = [];
    level.gulag.gestures["fc_gesture_neg"] = [ "iw8_ges_plyr_gesture_crush", "iw8_ges_plyr_gesture_rally", "iw8_ges_plyr_gesture_revive" ];
    level.gulag.gestures["fc_gesture_pos"] = [ "iw8_ges_plyr_gesture_doubletime", "iw8_ges_plyr_gesture_hold", "iw8_ges_plyr_gesture_ok", "iw8_ges_plyr_gesture_thumbs_up" ];
    level.gulag.gesturesounds["fc_gesture_neg"] = [ "tmp_gulag_gesture_neg_crush", "tmp_gulag_gesture_neg_rally", "tmp_gulag_gesture_neg_revive" ];
    level.gulag.gesturesounds["fc_gesture_pos"] = [ "tmp_gulag_gesture_pos_doubletime", "tmp_gulag_gesture_pos_hold", "tmp_gulag_gesture_pos_ok", "tmp_gulag_gesture_pos_thumbs_up" ];
}

getmaxplayers()
{
    var_0 = int( clamp( getdvarint( "scr_br_fc_max_players", 2 ), 2, 2 ) );

    if ( var_0 % 2 != 0 )
        var_0 = var_0 - 1;

    return var_0;
}

comparescriptindexsmalltolarge( var_0, var_1 )
{
    return var_0.script_index < var_1.script_index;
}

comparescriptindexobscuredspawns( var_0, var_1 )
{
    if ( var_0.script_index == 4 )
        return 1;
    else if ( var_0.script_index == 5 )
        return 1;
    else if ( var_1.script_index == 4 )
        return 0;
    else if ( var_1.script_index == 5 )
        return 0;

    return comparescriptindexsmalltolarge( var_0, var_1 );
}

copystructwithoffset( var_0, var_1, var_2 )
{
    var_3 = spawnstruct();
    var_3.origin = var_0.origin + var_1;
    var_3.angles = var_0.angles;
    var_3.script_index = var_2;
    return var_3;
}

getgulagcenter( var_0 )
{
    var_1 = ( 0, 0, 0 );

    foreach ( var_3 in var_0.fightspawns )
        var_1 = var_1 + var_3.origin;

    var_1 = var_1 / var_0.fightspawns.size;
    return var_1;
}

spawnweapon( var_0, var_1, var_2 )
{
    var_3 = scripts\mp\gametypes\br_weapons::createspawnweaponatpos( getgroundposition( var_0.origin + ( 0, 0, 12 ), 12 ), var_0.angles + ( 0, 0, 90 ), var_1, [] );
    var_3 thread outlinewatchplayerprox( var_2 );
    return var_3;
}

spawnlootweapons( var_0 )
{
    var_1 = [];
    var_2 = [];
    var_2["none"] = [ "none" ];
    var_2["tactical"] = scripts\mp\gametypes\br_pickups::getgulagpickupsforclass( "tactical" );
    var_2["lethal"] = scripts\mp\gametypes\br_pickups::getgulagpickupsforclass( "lethal" );
    var_3 = [];

    foreach ( var_5 in var_2 )
    {
        foreach ( var_7 in var_5 )
        {
            var_8 = 1;

            if ( isdefined( var_3[var_7] ) )
                continue;

            var_9 = level.br_lootiteminfo[var_7];

            if ( isdefined( var_9 ) )
            {
                var_10 = level.br_lootiteminfo[var_7].baseweapon + "_mp";
                var_8 = weaponclipsize( var_10 ) * 1;
            }

            var_3[var_7] = var_8;
        }
    }

    var_13 = [ "tactical", "tactical", "lethal", "lethal", "lethal" ];
    var_13 = scripts\engine\utility::array_randomize( var_13 );
    var_14 = [];
    var_14["floor_center_01"] = scripts\engine\utility::random( var_2[var_13[0]] );
    var_14["floor_center_02"] = scripts\engine\utility::random( var_2[var_13[1]] );
    var_14["floor_right_01"] = scripts\engine\utility::random( var_2[var_13[2]] );
    var_14["floor_left_01"] = scripts\engine\utility::random( var_2[var_13[3]] );
    var_14["floor_mid_01"] = scripts\engine\utility::random( var_2[var_13[4]] );

    if ( getdvarint( "scr_br_fc_shifted_spawns", 1 ) > 0 )
        var_14["floor_left_01"] = var_14["floor_right_01"];

    foreach ( var_16 in var_0 )
    {
        if ( !isdefined( var_16.script_noteworthy ) )
            continue;

        var_17 = var_14[var_16.script_noteworthy];

        if ( !isdefined( var_17 ) )
            continue;

        if ( var_17 == "none" )
            continue;

        var_18 = var_16.origin;
        var_19 = var_16.angles;

        if ( !isdefined( var_19 ) )
            var_19 = ( 0, 0, 0 );
        else
            var_19 = ( 0, var_19[1], 0 );

        var_7 = spawnscriptable( var_17, var_18, var_19 );

        if ( isdefined( var_7 ) )
        {
            var_7.part = var_17;
            var_7.count = var_3[var_17];
            var_7.keepinmap = 1;
            var_1[var_1.size] = var_7;
            continue;
        }
    }

    return var_1;
}

playerismatchedplayerready( var_0 )
{
    if ( playerismatchpending( var_0 ) )
    {
        var_1 = var_0.matches[0];

        foreach ( var_3 in var_1 )
        {
            if ( var_3 == self )
                continue;
            else if ( isplayerloading( var_3, var_0 ) )
                return 0;
        }

        return 1;
    }

    return 0;
}

playerismatchpending( var_0 )
{
    if ( ismatchpending( var_0 ) )
    {
        var_1 = var_0.matches[0];

        foreach ( var_3 in var_1 )
        {
            if ( var_3 == self )
                return 1;
        }
    }

    return 0;
}

ismatchpending( var_0 )
{
    if ( var_0.matches.size == 0 )
        return 0;

    var_1 = var_0.matches[0];

    if ( var_1.size < 2 )
        return 0;

    return 1;
}

isfightready( var_0 )
{
    if ( !ismatchpending( var_0 ) )
        return 0;

    var_1 = var_0.matches[0];

    foreach ( var_3 in var_1 )
    {
        if ( istrue( var_3.entergulagwait ) || !istrue( var_3.jailed ) )
            return 0;
    }

    return 1;
}

monitorgulag( var_0 )
{
    level endon( "game_ended" );

    for (;;)
    {
        if ( istrue( level.br_prematchstarted ) && var_0.fightover )
        {
            if ( istrue( level.gulag.shutdown ) )
            {
                if ( var_0.loadingplayers.size != 0 )
                    var_0 waittill( "loadingPlayersEmpty" );

                if ( level.gulag.endonshutdown == 1 )
                {
                    foreach ( var_2 in var_0.jailedplayers )
                    {
                        var_2.gulagloser = 1;
                        var_2 kill();
                    }

                    var_0.shutdown = 1;
                    break;
                }
                else if ( level.gulag.endonshutdown == 2 )
                {
                    thread dojailbreak( var_0 );
                    break;
                }
                else if ( level.gulag.endonshutdown == 3 && !ismatchpending( var_0 ) && !anyplayersinlaststandhold() )
                {
                    foreach ( var_2 in var_0.jailedplayers )
                    {
                        if ( isalive( var_2 ) )
                        {
                            var_2 playergulagarenaready();
                            thread gulagvictory( var_0, var_2, 1, "shutdown" );
                        }
                    }

                    var_0.shutdown = 1;
                    break;
                }
            }

            if ( isfightready( var_0 ) )
                beginnewfight( var_0 );
            else
                setjailtimeouthud( var_0 );
        }

        waitframe();
    }
}

circletimer( var_0 )
{
    if ( istrue( level.usegulag ) && !istrue( level.gulag.shutdown ) )
    {
        var_1 = getgulagclosedcircleindex();

        if ( var_0 >= var_1 )
            shutdowngulag( "circle_index", var_0 );
    }
}

shutdowngulag( var_0, var_1, var_2 )
{
    if ( !istrue( level.usegulag ) || istrue( level.gulag.shutdown ) )
        return;

    setomnvar( "ui_gulag_state", 0 );
    setomnvar( "ui_gulag_show_closing_state", 2 );
    level.gulag.shutdown = 1;
    thread makeac130flyaway();

    if ( !istrue( var_2 ) )
    {
        foreach ( var_4 in level.players )
        {
            if ( istrue( var_4.inlaststand ) )
            {
                level.gulag.laststandplayers[level.gulag.laststandplayers.size] = var_4;
                continue;
            }

            if ( !isdefined( var_4 ) || !isalive( var_4 ) || isdefined( var_4.gulag ) )
                continue;

            var_4 playergulagdonesplash();
        }
    }

    dlog_recordevent( "dlog_event_br_gulag_shutdown", [ "reason", var_0, "reason_count", var_1 ] );
}

anyplayersinlaststandhold()
{
    return level.gulag.laststandplayers.size > 0;
}

playerinlaststandhold()
{
    if ( !istrue( level.usegulag ) || !istrue( level.gulag.shutdown ) )
        return 0;

    return scripts\engine\utility::array_contains( level.gulag.laststandplayers, self );
}

gulaglaststandholdremove( var_0 )
{
    if ( istrue( level.gulag.shutdown ) && anyplayersinlaststandhold() )
    {
        if ( isdefined( var_0 ) && var_0 playerinlaststandhold() )
        {
            level.gulag.laststandplayers = scripts\engine\utility::array_remove( level.gulag.laststandplayers, self );
            return 1;
        }
        else
            level.gulag.laststandplayers = scripts\engine\utility::array_removeundefined( level.gulag.laststandplayers );
    }

    return 0;
}

onplayerdisconnect( var_0 )
{
    if ( !istrue( level.usegulag ) )
        return;

    gulaglaststandholdremove( var_0 );
}

playerexitlaststand( var_0 )
{
    if ( !istrue( level.usegulag ) )
        return;

    if ( istrue( var_0 ) )
    {
        if ( gulaglaststandholdremove( self ) )
            playergulagdonesplash();
    }
}

getgulagclosedcircleindex()
{
    if ( !isdefined( level.br_level ) || !isdefined( level.br_level.br_circledelaytimes ) )
        return 0;

    return level.br_level.br_circledelaytimes.size - 1 - getdvarint( "scr_br_fc_circle_disable", 3 );
}

setupgulagtimer()
{
    var_0 = gettimetogulagclosed();
    var_1 = gettime() + var_0 * 1000;
    setomnvar( "ui_gulag_timer", var_1 );
    thread showclosingmessage( var_0 );
}

showclosingmessage( var_0 )
{
    level endon( "game_ended" );
    self endon( "death_or_disconnect" );
    var_1 = getdvarint( "scr_br_display_gulag_close_message", 90 );
    var_2 = var_0 - var_1;

    if ( 0 < var_2 )
    {
        wait( var_2 );
        setomnvar( "ui_gulag_show_closing_state", 1 );
    }
}

gettimetogulagclosed()
{
    var_0 = 0;

    if ( isdefined( level.br_level ) && isdefined( level.br_level.br_circlecenters ) )
    {
        var_1 = getgulagclosedcircleindex();

        for ( var_2 = 0; var_2 < var_1; var_2++ )
        {
            var_3 = level.br_level.br_circledelaytimes[var_2];
            var_4 = level.br_level.br_circleclosetimes[var_2];
            var_0 = var_0 + var_3 + var_4;
        }
    }

    return int( var_0 );
}

playergulagdonesplash()
{
    if ( istrue( self.gulagdone ) )
        return;

    self.gulagdone = 1;
    scripts\mp\gametypes\br_killstreaks::dangernotifyplayer( self, "gulag_closed", undefined, 2 );
    scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_gulag_close", self );

    if ( istrue( level.br_pickups.respawntokenclosewithgulag ) && scripts\mp\gametypes\br_public::hasrespawntoken() )
    {
        scripts\mp\gametypes\br_pickups::removerespawntoken();
        thread scripts\mp\hud_message::showsplash( "br_respawn_token_disabled" );
        thread scripts\mp\utility\points::giveunifiedpoints( "br_respawn_token_convert" );
    }
}

dojailbreak( var_0 )
{
    if ( var_0.jailedplayers.size == 0 )
    {
        var_0.shutdown = 1;
        return;
    }

    var_0.isjailbreak = 1;
    var_0.arenaplayers = scripts\engine\utility::array_removeundefined( var_0.jailedplayers );
    jailbreaktimerwait( var_0 );
    var_1 = getloadoutindex();

    foreach ( var_3 in var_0.arenaplayers )
    {
        var_3 thread gulagplayerwatchfordeath( var_0 );
        var_3 initplayerarena( var_0, 1, var_1 );

        if ( istrue( level.gulag.gestures_enabled ) && !isbot( var_3 ) )
            var_3 playergulaggesturesdisable();

        if ( getdvarint( "scr_br_fc_outline_countdown", 1 ) > 0 && var_0.arenaplayers.size > 0 )
            var_3 hudoutlinedisableforclients( var_0.arenaplayers );
    }

    playsoundatpos( var_0.center, "iw8_mp_snatch_fight_start" );
    var_0.fightover = 0;

    if ( !isoneteamleft( var_0 ) )
    {
        var_0.time = level.gulag.timelimit;
        updatematchtimerhud( var_0, var_0.time );
        waittillgulagmatchend( var_0, 0 );
    }
    else
    {
        var_0.time = 8;
        updatematchtimerhud( var_0, var_0.time );
        wait( var_0.time );
    }

    foreach ( var_3 in var_0.arenaplayers )
    {
        if ( isdefined( var_3 ) && isdefined( var_3.gulagjailbreakhud ) )
            var_3.gulagjailbreakhud destroy();
    }

    handleendarena( var_0 );
    var_0.shutdown = 1;
}

jailbreaktimerwait( var_0 )
{
    foreach ( var_2 in var_0.arenaplayers )
    {
        if ( !isdefined( var_2 ) )
            continue;

        var_2 thread scripts\mp\hud_message::showsplash( "br_gulag_jail_break" );

        if ( getdvarint( "scr_br_fc_outline_countdown", 1 ) > 0 )
            var_2 hudoutlineenableforclients( var_0.arenaplayers, "outline_nodepth_red" );
    }

    wait 3;

    foreach ( var_2 in var_0.arenaplayers )
    {
        if ( !isdefined( var_2 ) )
            continue;

        var_2 playeraddjailbreaktimer();
    }

    gulagcountdowntimer( var_0, 0 );

    foreach ( var_2 in var_0.arenaplayers )
    {
        if ( !isdefined( var_2 ) )
            continue;

        var_2 setclientomnvar( "ui_match_start_countdown", 0 );
        var_2 setclientomnvar( "ui_match_in_progress", 1 );

        if ( isdefined( var_2 ) && isdefined( var_2.gulagjailbreakhud ) )
            var_2.gulagjailbreakhud.label = &"MP/BR_GULAG_JAILBREAK";
    }
}

playeraddjailbreaktimer()
{
    self.gulagjailbreakhud = scripts\mp\hud_util::createfontstring( "default", 2 );
    self.gulagjailbreakhud scripts\mp\hud_util::setpoint( "CENTER", "CENTER", 0, -150 );
    self.gulagjailbreakhud.label = &"MP/BR_GULAG_JAILBREAK_IN";
}

resetequipment( var_0 )
{
    if ( !istrue( level.gulag.spawnloot ) )
        return;

    for ( var_1 = 0; var_1 < var_0.weapons.size; var_1++ )
    {
        var_2 = var_0.weapons[var_1];
        var_2 setscriptablepartstate( var_2.part, "visible" );
    }
}

validateplayers( var_0 )
{
    return var_0.arenaplayers.size >= 2;
}

beginnewfight( var_0 )
{
    level endon( "game_ended" );
    var_0.fightover = 0;
    resetequipment( var_0 );
    resetarenaomnvardata( var_0 );
    var_0.arenaplayers = popnextmatch( var_0 );
    var_1 = 0;

    foreach ( var_4, var_3 in var_0.arenaplayers )
    {
        if ( isdefined( var_3 ) )
            var_0.jailedplayers = scripts\engine\utility::array_remove( var_0.jailedplayers, var_3 );

        if ( !isdefined( var_3 ) )
        {
            var_1 = 1;
            continue;
        }

        if ( var_3.gulag == 0 || var_3.jailed == 0 || var_3.gulagarena == 1 )
        {
            scripts\mp\utility\script::demoforcesre( "Player: " + var_3.name + " - invalid for gulag - p.gulag = " + var_3.gulag + ", p.jailed = " + var_3.jailed + ", p.gulagArena = " + var_3.gulagarena );
            var_1 = 1;
            continue;
        }

        var_3.fighterindex = var_4;
    }

    foreach ( var_6 in var_0.jailedplayers )
    {
        if ( !isdefined( var_6 ) )
            continue;

        var_6.gulagposition--;
        var_6 setweaponammoclip( "rock_mp", 5 );
    }

    if ( var_1 )
    {
        var_0.jailedplayers = scripts\engine\utility::array_removeundefined( var_0.jailedplayers );
        var_0.arenaplayers = scripts\engine\utility::array_removeundefined( var_0.arenaplayers );

        if ( !validateplayers( var_0 ) )
        {
            handleendarena( var_0, undefined, 1 );
            return;
        }
    }

    var_8 = startbetting( var_0, var_0.arenaplayers );

    if ( getdvarint( "scr_br_fc_spectate_outlines", 0 ) )
        thread manageoutlines( var_0, var_0.arenaplayers, var_8 );

    var_0.arenaspawncounter = 0;
    var_9 = getloadoutindex();

    foreach ( var_6 in var_0.arenaplayers )
    {
        if ( !isdefined( var_6 ) )
            continue;

        var_6 thread gulagplayerwatchfordeath( var_0, var_8 );
        var_6 thread initplayerarena( var_0, 0, var_9 );
    }

    thread watchlethaldelay( var_0 );
    wait 2;

    if ( !validateplayers( var_0 ) )
    {
        handleendarena( var_0, var_8, 1 );
        return;
    }

    updatearenaomnvarplayers( var_0 );
    updatearenaomnvardata( var_0 );
    var_12 = gulagcountdowntimer( var_0, 1, var_8 );

    if ( !var_12 )
        return;

    endbetting( var_0, var_8 );

    foreach ( var_6 in var_0.arenaplayers )
    {
        var_6 playergulagarenaready();
        var_6 thread playershowarenastartobjectivetext();
    }

    thread startfightvo( var_0 );
    var_0.time = level.gulag.timelimit;
    updatematchtimerhud( var_0, var_0.time );
    waittillgulagmatchend( var_0, 1 );
    handleendarena( var_0, var_8 );

    if ( !isfightready( var_0 ) )
        updateplayeromnvarsallmatches( var_0 );
}

watchlethaldelay( var_0 )
{
    var_0 endon( "fight_over" );
    var_0 endon( "matchEnded" );
    level endon( "game_ended" );

    if ( level.gulag.lethaldelay <= 0 )
        return;

    var_0.lethaldelaystarttime = gettime();
    var_0.lethaldelayendtime = var_0.lethaldelaystarttime + level.gulag.lethaldelay * 1000 + 3000 + 2000;

    while ( gettime() < var_0.lethaldelayendtime )
        waitframe();

    var_0 notify( "lethal_delay_end" );
}

watchlethaldelayplayer( var_0 )
{
    self endon( "death_or_disconnect" );
    level endon( "game_ended" );

    if ( level.gulag.lethaldelay == 0 )
        return;

    if ( !isai( self ) )
    {
        self notifyonplayercommand( "lethal_attempt_gulag", "+frag" );
        self notifyonplayercommand( "lethal_attempt_gulag", "+smoke" );
    }

    scripts\mp\equipment::allow_equipment_slot( "primary", 0 );
    scripts\mp\equipment::allow_equipment_slot( "secondary", 0 );
    watchlethaldelayfeedbackplayer( var_0, self );
    scripts\mp\equipment::allow_equipment_slot( "primary", 1 );
    scripts\mp\equipment::allow_equipment_slot( "secondary", 1 );

    if ( scripts\cp_mp\utility\game_utility::isrealismenabled() )
        self playlocalsound( "ui_restock_lethals" );

    self setclientomnvar( "ui_recharge_notify", 2 );

    if ( !isai( self ) )
    {
        self notifyonplayercommandremove( "lethal_attempt_gulag", "+frag" );
        self notifyonplayercommandremove( "lethal_attempt_gulag", "+smoke" );
    }
}

watchlethaldelayfeedbackplayer( var_0, var_1 )
{
    level endon( "game_ended" );
    var_0 endon( "matchEnded" );
    var_0 endon( "lethal_delay_end" );

    for (;;)
    {
        self waittill( "lethal_attempt_gulag" );
        var_2 = ( var_0.lethaldelayendtime - gettime() ) / 1000;
        var_2 = int( max( 0, ceil( var_2 ) ) );
        var_1 scripts\mp\hud_message::showerrormessage( "MP/LETHALS_UNAVAILABLE_FOR_N", var_2 );
    }
}

startfightvo( var_0 )
{
    var_0 endon( "matchEnded" );
    playsoundatpos( var_0.center, "iw8_mp_snatch_fight_start" );
    wait 1;
    scripts\mp\gametypes\br_public::brleaderdialog( "gulag_gulag_active", 0, var_0.jailedplayers );
    scripts\mp\gametypes\br_public::brleaderdialog( "gulag_objective", 0, var_0.arenaplayers );
    wait 2;

    foreach ( var_2 in var_0.jailedplayers )
    {
        if ( var_2.gulagposition <= 1 )
        {
            scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_next", var_2, 0 );
            continue;
        }

        if ( var_2.gulagposition == 2 )
            scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_taunt", var_2, 0 );
    }
}

playershowarenastartobjectivetext()
{
    self endon( "disconnect" );
    self setclientomnvar( "ui_objective_text", 0 );
    wait 3;
    self setclientomnvar( "ui_objective_text", -1 );
}

handleonekillwin( var_0, var_1, var_2, var_3 )
{
    if ( isdefined( var_2 ) && isplayer( var_2 ) && var_1 != var_2 && isalive( var_2 ) && scripts\engine\utility::array_contains( var_0.arenaplayers, var_2 ) )
    {
        thread gulagvictory( var_0, var_2, 0, "winner" );
        payoutbet( var_3, var_2, 1 );
    }

    payoutbet( var_3, var_1, 0 );
}

manageoutlines( var_0, var_1, var_2 )
{
    manageoutlineactive( var_0, var_1, var_2 );

    if ( istrue( level.gulag.onekillwin ) )
        return;

    manageoutlinecleanup( var_0, var_1 );
}

manageoutlineswatchplayersaddedtojail( var_0 )
{
    var_0 endon( "fight_over" );

    for (;;)
    {
        var_0 waittill( "player_added_to_jail" );
        updateoutlines( var_0 );
    }
}

manageoutlineactive( var_0, var_1, var_2 )
{
    var_0 endon( "fight_over" );
    thread manageoutlineswatchplayersaddedtojail( var_0 );

    for (;;)
    {
        if ( var_0.jailedplayers.size )
        {
            var_3 = scripts\engine\utility::array_removeundefined( var_0.jailedplayers );

            foreach ( var_5 in var_1 )
            {
                if ( !isdefined( var_5 ) )
                    continue;

                var_5 hudoutlineenableforclients( var_3, "outline_nodepth_white" );
            }
        }

        if ( isdefined( var_2 ) )
        {
            foreach ( var_8 in var_2.bets )
            {
                if ( !isdefined( var_8.owner ) )
                    continue;

                var_9 = scripts\engine\utility::ter_op( var_2.bettingopen, var_8.playerfocus, var_8.playerbeton );

                if ( var_9 != -1 )
                {
                    var_10 = var_2.fighters[var_9];

                    if ( isdefined( var_10 ) )
                        var_10 hudoutlineenableforclient( var_8.owner, "outline_nodepth_green" );
                }
            }
        }

        var_0 waittill( "update_outlines" );
    }
}

manageoutlinecleanup( var_0, var_1 )
{
    var_2 = scripts\engine\utility::array_removeundefined( var_0.jailedplayers );

    if ( !var_2.size )
        return;

    foreach ( var_4 in var_1 )
    {
        if ( !isdefined( var_4 ) )
            continue;

        var_4 hudoutlinedisableforclients( var_2 );
    }
}

updateoutlines( var_0 )
{
    var_0 notify( "update_outlines" );
}

playergulagarenaready()
{
    playerstartarenasetcontrols( 1 );
    self setclientomnvar( "ui_br_infil_started", 1 );
    self setclientomnvar( "ui_match_start_countdown", 0 );
    self setclientomnvar( "ui_match_in_progress", 1 );

    if ( istrue( level.gulag.gestures_enabled ) && !isbot( self ) )
        playergulaggesturesdisable();
}

gulagcountdowntimer( var_0, var_1, var_2 )
{
    var_3 = 3;

    while ( var_3 > 0 )
    {
        foreach ( var_5 in var_0.arenaplayers )
        {
            var_5 setclientomnvar( "ui_match_in_progress", 0 );
            var_5 setclientomnvar( "ui_match_start_countdown", var_3 );
        }

        var_3 = var_3 - 1;
        wait 1;

        if ( istrue( var_1 ) && !validateplayers( var_0 ) )
        {
            handleendarena( var_0, var_2, 1 );
            return 0;
        }
    }

    return 1;
}

gulagmatchclocksounds( var_0, var_1, var_2 )
{
    var_0 endon( "matchEnded" );

    while ( var_1 > 0 )
    {
        if ( level.gameended )
            return;

        var_3 = var_1;

        if ( !istrue( var_0.overtime ) )
            var_3 = var_3 - var_2;

        if ( var_3 <= 5 )
        {
            var_4 = scripts\mp\gamelogic::getclocksoundaliasfortimeleft( var_3 );
            doclocksound( var_0, var_4 );
        }

        if ( var_1 > 1 )
            var_1 = var_1 - 1;

        wait 1;
    }
}

doclocksound( var_0, var_1 )
{
    foreach ( var_3 in var_0.jailedplayers )
    {
        if ( isdefined( var_3 ) )
            var_3 playlocalsound( var_1 );
    }

    foreach ( var_3 in var_0.arenaplayers )
    {
        if ( isdefined( var_3 ) )
            var_3 playlocalsound( var_1 );
    }
}

getovertimelength()
{
    return getdvarint( "scr_br_fc_overtime", 15.0 );
}

waittillgulagmatchend( var_0, var_1 )
{
    var_2 = getovertimelength();
    var_0.time = var_0.time + var_2;
    thread gulagmatchclocksounds( var_0, var_0.time, var_2 );

    for (;;)
    {
        if ( !isanyonealive( var_0 ) || isoneteamleft( var_0 ) )
            break;

        var_0.time = var_0.time - level.framedurationseconds;

        if ( istrue( level.gulag.arenaflag ) && !istrue( var_0.overtime ) && var_0.time <= var_2 )
        {
            var_0.overtime = 1;
            var_0.dom.arenaflag arenaflag_setenabled( 1 );
            var_0.dom.arenaflag arenaflag_setvisible( var_0, 1 );
            var_0.dom.arenaflag.flagmodel playsoundonmovingent( "flag_spawned" );
        }

        if ( istrue( var_0.overtime ) )
        {
            var_3 = clamp( var_0.time / var_2, 0.0, 1.0 );
            setovertimeomnvarprogress( var_0, var_3 );
        }

        if ( var_0.time <= 0 )
        {
            waittillarenaplayersnotcapturing( var_0 );

            if ( istrue( var_1 ) )
            {
                foreach ( var_5 in var_0.arenaplayers )
                {
                    var_5 thread gulagplayerlost( var_0 );
                    var_5 setplayermusicstate( "br_gulag_lose" );
                    var_5 playsoundtoplayer( "gulag_crowd_boo_loser", var_5 );
                    var_5 clearclienttriggeraudiozone( 2 );
                }
            }

            break;
        }

        waitframe();
    }

    var_0 notify( "matchEnded" );
}

waittillarenaplayersnotcapturing( var_0 )
{
    while ( isdefined( var_0.dom.arenaflag.claimteam ) && var_0.dom.arenaflag.claimteam != "none" && !istrue( var_0.dom.arenaflag.stalemate ) )
        waitframe();
}

isanyonealive( var_0 )
{
    foreach ( var_2 in var_0.arenaplayers )
    {
        if ( isalive( var_2 ) )
            return 1;
    }

    return 0;
}

isoneteamleft( var_0 )
{
    var_1 = undefined;

    foreach ( var_3 in var_0.arenaplayers )
    {
        if ( isalive( var_3 ) )
        {
            if ( !isdefined( var_1 ) )
            {
                var_1 = var_3.team;
                continue;
            }

            if ( var_1 != var_3.team )
                return 0;
        }
    }

    return 1;
}

handleendarena( var_0, var_1, var_2 )
{
    var_0 notify( "fight_over" );
    var_0.fightover = 1;
    endbetting( var_0, var_1 );
    updatematchtimerhud( var_0, 0 );
    var_3 = undefined;

    foreach ( var_5 in var_0.arenaplayers )
    {
        if ( !isdefined( var_5 ) )
            continue;

        if ( isalive( var_5 ) )
        {
            var_3 = var_5;

            if ( istrue( var_2 ) )
                var_3 playergulagarenaready();

            thread gulagvictory( var_0, var_5, 0, "winner" );
        }
    }

    if ( istrue( level.gulag.arenaflag ) && istrue( var_0.overtime ) )
        var_0.dom.arenaflag arenaflag_setenabled( 0 );

    payoutremainingbets( var_3, var_1 );
    wait 2.0;

    if ( istrue( level.gulag.arenaflag ) && istrue( var_0.overtime ) )
    {
        var_0.overtime = 0;
        var_0.dom.arenaflag arenaflag_setvisible( var_0, 0 );
    }

    cleanuparenamolotovs( var_0 );
    wait 1;
}

getnextjailspawn( var_0 )
{
    if ( isdefined( var_0.jailspawncounter ) )
    {
        var_0.jailspawncounter++;
        var_0.jailspawncounter = var_0.jailspawncounter % var_0.jailspawns.size;
    }
    else
        var_0.jailspawncounter = 0;

    var_1 = var_0.jailspawns[var_0.jailspawncounter];
    return var_1;
}

outlinewatchplayerprox( var_0 )
{
    self endon( "death" );
    self endon( "trigger" );
    self.outlinedplayers = [];

    switch ( var_0 )
    {
        case 0:
        default:
            var_1 = "outline_depth_white";
            break;
        case 1:
            var_1 = "outline_depth_green";
            break;
        case 2:
            var_1 = "outline_depth_cyan";
            break;
        case 3:
            var_1 = "outline_depth_red";
            break;
        case 4:
            var_1 = "outline_depth_orange";
            break;
    }

    for (;;)
    {
        var_2 = scripts\common\utility::playersinsphere( self.origin, 2000 );
        var_3 = scripts\engine\utility::array_difference( level.players, var_2 );

        foreach ( var_5 in var_2 )
        {
            var_6 = distancesquared( self.origin, var_5.origin );
            var_7 = var_5 getentitynumber();

            if ( !isdefined( self.outlinedplayers[var_7] ) )
                self.outlinedplayers[var_7] = scripts\mp\utility\outline::outlineenableforplayer( self, var_5, var_1, "level_script" );
        }

        foreach ( var_5 in var_3 )
        {
            var_7 = var_5 getentitynumber();

            if ( isdefined( self.outlinedplayers[var_7] ) )
            {
                scripts\mp\utility\outline::outlinedisable( self.outlinedplayers[var_7], self );
                self.outlinedplayers[var_7] = undefined;
            }
        }

        waitframe();
    }
}

gulagstreamlocationstart( var_0 )
{
    if ( ( isbot( self ) || self scriptablegetmidpoint() ) && !istrue( self.longgulagstream ) )
        return 0;

    self setpredictedstreamloaddist( 10000.0 );
    self setadditionalstreampos( var_0, 1 );
    return !self isadditionalstreamposready();
}

gulagstreamlocationwait()
{
    if ( !istrue( self.longgulagstream ) )
    {
        self endon( "gulagStreamLocationComplete" );
        thread gulagstreamlocationwaittimeout( level.gulag.streamtimeout );

        while ( !self isadditionalstreamposready() )
            waitframe();

        self notify( "gulagStreamLocationComplete" );
    }
    else
        wait( level.gulag.streamtimeout );
}

gulagstreamlocationwaittimeout( var_0 )
{
    self endon( "disconnect" );
    self endon( "gulagStreamLocationComplete" );
    wait( var_0 );
    self notify( "gulagStreamLocationComplete" );
}

gulagstreamlocationend()
{
    self clearadditionalstreampos();
    self notify( "gulagStreamLocationComplete" );
}

gulagstreamexit()
{
    self setpredictedstreamloaddist( 0.0 );
}

addloadingplayer( var_0, var_1 )
{
    var_1.entergulagwait = 1;
    var_0.loadingplayers[var_0.loadingplayers.size] = var_1;
    thread addloadingplayerdisconnectwatch( var_0, var_1 );
}

addloadingplayerdisconnectwatch( var_0, var_1 )
{
    var_1 endon( "removeLoadingPlayer" );
    var_1 waittill( "disconnect" );
    thread removeloadingplayer( var_0, var_1 );
}

removeloadingplayer( var_0, var_1 )
{
    var_1 notify( "removeLoadingPlayer" );
    var_0.loadingplayers = scripts\engine\utility::array_remove( var_0.loadingplayers, var_1 );

    if ( var_0.loadingplayers.size == 0 )
        var_0 notify( "loadingPlayersEmpty" );
}

isplayerloading( var_0, var_1 )
{
    return scripts\engine\utility::array_contains( var_1.loadingplayers, var_0 );
}

entergulag( var_0 )
{
    var_0 notify( "enter_gulag" );
    var_0.entergulagwait = 0;
    scripts\mp\deathicons::hidedeathicon( var_0 );
}

entergulagwait( var_0 )
{
    if ( var_0.entergulagwait )
        var_0 waittill( "enter_gulag" );
}

playergetnextarena()
{
    if ( !istrue( level.gulag.multiarena ) )
        return level.gulag.arenas[0];

    var_1 = undefined;
    var_2 = undefined;

    foreach ( var_4 in level.gulag.arenas )
    {
        foreach ( var_6 in var_4.matches )
        {
            if ( var_6.size == 1 && var_6[0].team != self.team && ( !isdefined( var_1 ) || var_6[0].jailtime < var_2 ) )
            {
                var_1 = var_4;
                var_2 = var_6[0].jailtime;
            }
        }
    }

    if ( isdefined( var_1 ) )
        return var_1;

    var_9 = [];

    for ( var_10 = 0; var_10 < level.gulag.arenas.size; var_10++ )
    {
        var_4 = level.gulag.arenas[var_10];

        if ( var_4.matches.size > 0 && var_4.matches.size < level.gulag.maxqueue )
            var_9[var_9.size] = var_4;
    }

    if ( var_9.size > 0 )
    {
        foreach ( var_4 in var_9 )
        {
            foreach ( var_6 in var_4.matches )
            {
                foreach ( var_14 in var_6 )
                {
                    if ( var_14.team == self.team )
                        return var_4;
                }
            }
        }

        var_4 = var_9[randomint( var_9.size )];
        return var_4;
    }

    var_1 = undefined;
    var_18 = undefined;

    for ( var_10 = 0; var_10 < level.gulag.arenas.size; var_10++ )
    {
        var_4 = level.gulag.arenas[var_10];

        if ( var_4.matches.size == 0 )
            return var_4;
        else if ( !isdefined( var_18 ) || var_4.matches.size < var_18 )
        {
            var_1 = var_4;
            var_18 = var_4.matches.size;
        }
    }

    return var_1;
}

isplayermatched( var_0, var_1 )
{
    if ( !ismatchpending( var_0 ) )
        return 0;

    var_2 = var_1.gulagposition - 1;

    if ( var_2 >= 0 && var_0.matches[var_2].size > 1 )
        return 1;

    return 0;
}

setjailtimeouthud( var_0 )
{
    foreach ( var_2 in var_0.jailedplayers )
    {
        if ( !isplayermatched( var_0, var_2 ) && isdefined( var_2.jailtimeoutend ) && !isdefined( var_2.jailtimeouthud ) )
        {
            var_2 setclientomnvar( "ui_br_gulag_match_end_time", var_2.jailtimeoutend );
            var_2.jailtimeouthud = 1;
        }
    }
}

playerclearjailtimeouthud( var_0 )
{
    self.jailtimeoutend = undefined;
    self.jailtimeouthud = undefined;
    self setclientomnvar( "ui_br_gulag_match_end_time", 0 );
}

playerjailtimeout( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self endon( "initPlayerArena" );
    self endon( "gulag_end" );
    var_1 = getdvarint( "scr_br_fc_jailTimeout", 95 );

    if ( var_1 <= 0 )
        return;

    self.jailtimeoutend = gettime() + var_1 * 1000;
    wait( var_1 );

    while ( isplayermatched( var_0, self ) )
        waitframe();

    thread gulagvictory( var_0, self, 1, "timeout" );
}

initplayerjail( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self.jailtime = gettime();
    scripts\mp\gametypes\br_analytics::branalytics_gulagstart( self, scripts\engine\utility::ter_op( istrue( var_0 ), "default", "debug" ) );
    playerpreloadintrocinematic();
    var_1 = playergetnextarena();
    self.arena = var_1;

    if ( !scripts\engine\utility::array_contains( var_1.jailedplayers, self ) )
    {
        var_1 notify( "player_added_to_jail", self );
        var_1.jailedplayers[var_1.jailedplayers.size] = self;
    }

    playergulaghud( var_1 );
    thread playerwatchdisconnect( var_1 );
    updatematchqueuepositions( var_1 );
    addloadingplayer( var_1, self );
    gulaglaststandholdremove( self );

    if ( !isdefined( self.gulaguses ) )
        self.gulaguses = 0;

    self.gulaguses++;
    setplayervargulag( 1 );
    setplayervargulagarena( 0 );
    setplayeringulagjailextrainfo( 1 );
    setplayergulagindex( var_1 );
    scripts\mp\outofbounds::enableoobimmunity( self );

    if ( isdefined( level.checkforlaststandwipe ) )
        [[ level.checkforlaststandwipe ]]();

    var_2 = getnextjailspawn( var_1 );
    var_3 = getgroundposition( var_2.origin, 12 );
    var_4 = ( 0, 0, 0 );

    if ( isdefined( var_2.angles ) )
        var_4 = var_2.angles;

    var_5 = gulagstreamlocationstart( var_3 );
    self.gulagloading = 1;
    self.streampoint = spawnstruct();
    self.streampoint.origin = var_3;
    self.streampoint.angles = var_4;

    if ( istrue( var_0 ) )
        entergulagwait( self );
    else
        entergulag( self );

    scripts\mp\gametypes\br_quest_util::onplayerentergulag();
    var_6 = gettime();

    if ( scripts\mp\gametypes\br_public::isbotpracticematch() )
        self notify( "play_gulag_vo" );

    playerplayintrocinematic();

    if ( var_5 )
    {
        scripts\mp\gametypes\br::spawnintermission( var_3 + ( 0, 0, 100 ), self.angles );
        scripts\mp\spectating::setdisabled();
        gulagstreamlocationstart( var_3 );
    }

    if ( var_5 )
        gulagstreamlocationwait();

    if ( scripts\mp\gametypes\br_gametypes::isbrgametypefuncdefined( "playerPreSpawnGulagJail" ) )
        scripts\mp\gametypes\br_gametypes::runbrgametypefunc( "playerPreSpawnGulagJail" );

    scripts\mp\class::loadout_emptycacheofloadout( "gamemode" );
    self.pers["gamemodeLoadout"] = level.gulag.jailloadout;
    self.class = "gamemode";
    self.forcespawnangles = var_4;
    self.forcespawnorigin = var_3;
    scripts\mp\playerlogic::spawnplayer( undefined, 0 );
    scripts\cp_mp\execution::_clearexecution();
    self setclientomnvar( "ui_gulag", 1 );
    self.streampoint = undefined;
    self.gulagloading = 0;
    resetplayermovespeedscale();

    if ( var_5 )
        gulagstreamlocationend();

    playershowskippromptcinematic();
    playerwaittillcinematiccomplete( var_6 );

    if ( scripts\mp\gametypes\br_public::isbotpracticematch() )
        self clearsoundsubmix( "iw8_br_gulag_tutorial", 2 );
    else
        self clearsoundsubmix( "fade_to_black_all_except_music_and_scripted5", 2 );

    if ( isdefined( self.gulag_intro_vo ) )
        self stoplocalsound( self.gulag_intro_vo );

    self setclientomnvar( "ui_br_infil_started", 1 );
    var_7 = var_1.fightover && playerismatchedplayerready( var_1 );

    if ( !var_7 )
        gulagfadefromblack();

    gulagloadingtextclear();

    if ( level.gulag.betting )
    {
        var_8 = 3;

        if ( isdefined( self.plundercountondeath ) )
            var_8 = int( max( var_8, int( self.plundercountondeath / 2 ) ) );

        scripts\mp\gametypes\br_plunder::playersetplundercount( var_8 );
    }

    if ( !var_7 )
        scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_spawn", self, 0 );

    foreach ( var_10 in level.teamdata[self.team]["players"] )
    {
        if ( !isdefined( var_10 ) || !isalive( var_10 ) )
            continue;

        if ( var_10 != self )
        {
            var_10 thread scripts\mp\hud_message::showsplash( "br_gulag_teammate_in", undefined, self );
            scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_teammate_gulag", var_10 );
        }
    }

    if ( !istrue( self.jailed ) )
    {
        setplayervargulagjail( 1 );
        scripts\mp\utility\perk::blockperkfunction( "specialty_scavenger" );
    }

    removeloadingplayer( var_1, self );
    scripts\mp\gametypes\br_pickups::initplayer( 1 );

    if ( istrue( level.gulag.gestures_enabled ) && !isbot( self ) )
        thread playergulaggestures();

    playerstartjailsetcontrols( 0 );

    if ( istrue( level.gulag.arenaflag ) && istrue( var_1.overtime ) )
        var_1.dom.arenaflag thread arenaflag_setvisibleplayer( 1, self );

    thread playerjailtimeout( var_1 );
    thread playerjailwaitvo( var_1 );
}

playerjailwaitvo( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self endon( "initPlayerArena" );
    self endon( "gulag_end" );
    wait 5;

    if ( ismatchpending( var_0 ) )
        return;

    scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_obj_wait", self, 0 );
}

playerstartjailsetcontrols( var_0 )
{
    if ( var_0 )
        self enableoffhandthrowback();
    else
        self disableoffhandthrowback();
}

playergulaggestures()
{
    level endon( "game_ended" );
    self endon( "death" );
    self endon( "disconnect" );
    self endon( "gulag_gestures_stop" );
    self enableoffhandweapons();
    self allowfire( 0 );
    self allowads( 0 );
    self notifyonplayercommand( "fc_gesture_neg", "+attack" );
    self notifyonplayercommand( "fc_gesture_pos", "+speed_throw" );
    var_0 = 0;

    for (;;)
    {
        var_1 = scripts\engine\utility::waittill_any_return_2( "fc_gesture_neg", "fc_gesture_pos" );

        if ( self isgestureplaying() || self isswitchingweapon() || self isreloading() || self ismantling() || self isthrowinggrenade() || self israisingweapon() || self ismeleeing() )
            continue;

        if ( getdvarint( "scr_br_fc_gestures_test", 0 ) > 0 )
        {
            if ( var_0 >= level.gulag.gestures[var_1].size )
                var_0 = 0;

            var_2 = level.gulag.gesturesounds[var_1][var_0];
            var_3 = level.gulag.gestures[var_1][var_0];
            var_0++;
        }
        else
        {
            var_0 = randomint( level.gulag.gestures[var_1].size );
            var_2 = level.gulag.gesturesounds[var_1][var_0];
            var_3 = level.gulag.gestures[var_1][var_0];
        }

        if ( isdefined( var_2 ) && var_2 != "" )
            self playsound( var_2 );

        var_4 = makeweapon( var_3 );

        if ( isdefined( var_4 ) && !isnullweapon( var_4 ) )
            scripts\cp_mp\gestures::watchradialgesture( var_4 );
    }
}

playergulaggesturesdisable()
{
    self notify( "gulag_gestures_stop" );
    self notifyonplayercommandremove( "fc_gesture_neg", "+attack" );
    self notifyonplayercommandremove( "fc_gesture_pos", "+speed_throw" );
    self allowfire( 1 );
    self allowads( 1 );
}

fadeoutin( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );

    if ( !isdefined( var_0 ) )
        var_0 = 1.0;

    gulagfadetoblack();
    wait( var_0 );
    gulagfadefromblack();
}

gulagfadetoblack( var_0 )
{
    playercinematicfadein();

    if ( istrue( var_0 ) )
        gulagfadetoblackspectatorsofplayer();
}

gulagfadefromblack()
{
    thread playercinematicfadeout();
    gulagfadefromblackspectatorsofplayer();
}

gulagfadetoblackspectatorsofplayer()
{
    var_0 = scripts\mp\gametypes\br_spectate::getspectatorsofplayer( self );

    foreach ( var_2 in var_0 )
        var_2 playercinematicfadein();
}

gulagfadefromblackspectatorsofplayer()
{
    var_0 = scripts\mp\gametypes\br_spectate::getspectatorsofplayer( self );

    foreach ( var_2 in var_0 )
        var_2 thread playercinematicfadeout();
}

fadeoutinspectatorsofplayer( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );

    if ( !isdefined( var_0 ) )
        var_0 = 1.0;

    gulagfadetoblackspectatorsofplayer();
    wait( var_0 );
    gulagfadefromblackspectatorsofplayer();
}

gulagisfaded()
{
    if ( playeriscinematiclayeron() )
        return 1;

    return 0;
}

gulagloadingtext()
{
    var_0 = scripts\mp\hud_util::createfontstring( "default", 1.5 );
    var_0 scripts\mp\hud_util::setpoint( "CENTER", "CENTER", 0, -100 );
    var_0.label = &"MP/BR_GULAG_TRAVEL";
    self.gulagloadingtext = var_0;
}

gulagloadingtextclear()
{
    if ( isdefined( self.gulagloadingtext ) )
        self.gulagloadingtext destroy();
}

playerteleportgulag( var_0, var_1 )
{
    self cancelmantle();
    self setorigin( var_0, 1 );
    self setplayerangles( var_1 );
}

initplayerarena( var_0, var_1, var_2 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self notify( "initPlayerArena" );
    scripts\mp\gametypes\br_pickups::initplayer();
    self allowprone( 0 );
    self allowcrouch( 0 );
    playerstartjailsetcontrols( 1 );
    playerstartarenasetcontrols( 0 );
    playertakeawayrock( var_0 );
    playerclearjailtimeouthud( var_0 );
    scripts\mp\equipment::allow_equipment_slot( "primary", 0 );
    scripts\mp\equipment::allow_equipment_slot( "secondary", 0 );
    thread playerhealthomnvarwatcher( var_0 );

    if ( istrue( var_1 ) )
    {
        setplayervargulagjail( 0 );
        setplayervargulagarena( 1 );
        playergivearenaloadout( var_0, var_2 );

        if ( level.gulag.lethaldelay > 0 )
            thread watchlethaldelayplayer( var_0 );

        return;
    }

    scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_spawn_rules", self, 0 );
    var_3 = gulagisfaded();

    if ( !var_3 )
        thread fadeoutin();

    thread fadeoutinspectatorsofplayer();
    wait 1;
    playergivearenaloadout( var_0, var_2 );
    thread playertimedinvunerable( 3 );

    if ( level.gulag.lethaldelay > 0 )
        thread watchlethaldelayplayer( var_0 );

    var_4 = getnextarenaspawn( var_0 );
    var_5 = getgroundposition( var_4.origin, 1 );
    var_6 = var_4.angles;

    if ( !isdefined( var_4.angles ) )
        var_6 = ( 0, 0, 0 );

    playerteleportgulag( var_5, var_6 );
    setplayervargulagjail( 0 );
    setplayervargulagarena( 1 );
    self.health = self.maxhealth;
    scripts\mp\gametypes\br_armor::givearmorvalue( level.gulag.numarmorhealth );

    if ( getdvarint( "scr_br_fc_outline_countdown", 1 ) > 0 )
        self hudoutlineenableforclients( var_0.arenaplayers, "outline_nodepth_red" );

    if ( var_3 )
        gulagfadefromblack();

    wait 1;
    self allowprone( 1 );
    self allowcrouch( 1 );
    wait 2;
    wait 1;

    if ( getdvarint( "scr_br_fc_outline_countdown", 1 ) > 0 && var_0.arenaplayers.size > 0 )
        self hudoutlinedisableforclients( var_0.arenaplayers );
}

playerstartarenasetcontrols( var_0 )
{
    if ( var_0 )
    {
        self allowmelee( 1 );
        self allowmovement( 1 );
        self enableusability();
        self enableoffhandweapons();
        self allowads( 1 );
        self allowfire( 1 );
    }
    else
    {
        self allowmelee( 0 );
        self allowmovement( 0 );
        self disableusability();
        self disableoffhandweapons();
        self allowads( 0 );
        self allowfire( 0 );
    }
}

getnextarenaspawn( var_0 )
{
    if ( !isdefined( var_0.arenaspawncounter ) )
        var_0.arenaspawncounter = 0;

    var_1 = undefined;

    if ( usecellspawns( var_0 ) )
        var_1 = var_0.cellspawns[var_0.arenaspawncounter];
    else
        var_1 = var_0.fightspawns[var_0.arenaspawncounter];

    var_0.arenaspawncounter++;
    var_0.arenaspawncounter = var_0.arenaspawncounter % var_0.fightspawns.size;
    return var_1;
}

playertimedinvunerable( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self.plotarmor = 1;
    scripts\engine\utility::waittill_any_timeout_no_endon_death_2( var_0, "death", "gulagRespawn" );
    self.plotarmor = undefined;
}

gulagvictory( var_0, var_1, var_2, var_3 )
{
    level endon( "game_ended" );
    var_1 endon( "death_or_disconnect" );
    var_1 notify( "gulag_end" );
    var_1 playerclearjailtimeouthud();
    var_1 thread playertimedinvunerable( 5 );
    var_1.gulagloser = 0;

    if ( var_0.jailedplayers.size > 0 && getdvarint( "scr_br_fc_spectate_outlines", 0 ) )
        var_1 hudoutlinedisableforclients( var_0.jailedplayers );

    if ( var_0.arenaplayers.size > 0 && getdvarint( "scr_br_fc_outline_countdown", 1 ) > 0 )
        var_1 hudoutlinedisableforclients( var_0.arenaplayers );

    var_0.arenaplayers = scripts\engine\utility::array_remove( var_0.arenaplayers, var_1 );

    foreach ( var_5 in level.teamdata[var_1.team]["players"] )
    {
        if ( !isdefined( var_5 ) || !isalive( var_5 ) )
            continue;

        if ( var_5 != var_1 )
        {
            var_5 thread scripts\mp\hud_message::showsplash( "br_gulag_teammate_out", undefined, var_1 );
            scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_teammate_win", var_5 );
        }
    }

    var_7 = "";

    if ( isdefined( var_3 ) )
        var_7 = var_3;

    var_1 thread triggerrespawnoverlay( 3 );

    if ( var_2 )
    {
        if ( var_3 == "timeout" )
            scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_timeout", var_1, 0 );
        else
            scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_noenemy", var_1, 0 );

        var_0.jailedplayers = scripts\engine\utility::array_remove( var_0.jailedplayers, var_1 );
        updatematchqueuepositions( var_0 );
    }
    else
        scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_win", var_1, 0 );

    var_1 playsoundtoplayer( "gulag_crowd_cheer_winner", var_1 );
    var_1 setplayermusicstate( "br_gulag_win" );

    if ( istrue( level.gulag.onekillwin ) && ( istrue( var_0.isjailbreak ) || level.gulag.maxplayers > 2 ) )
        var_1 playerhide();

    var_1 playerstartjailsetcontrols( 1 );
    var_1 playerstartarenasetcontrols( 1 );
    var_1 playergulagvictorysetcontrols( 0 );
    var_1 scripts\mp\gametypes\br_public::updatebrscoreboardstat( "isRespawning", 1 );
    var_1 scripts\mp\weapons::deleteplacedequipment();
    var_8 = scripts\mp\gametypes\br_public::getdefaultstreamhinttimeoutms() / 1000;
    var_9 = var_1 playergulaggetrespawnpoint( 0, var_8 );
    var_10 = var_1 playerprestreamrespawnorigin( var_9 );
    wait 2.0;

    if ( istrue( level.gulag.arenaflag ) )
        var_0.dom.arenaflag thread arenaflag_setvisibleplayer( 0, var_1 );

    var_1 clearclienttriggeraudiozone( 2 );
    var_1 gulagfadetoblack( 1 );
    wait 1;
    var_1 gulagwinnerrespawn( 0, var_3, var_9, 1, var_10 );
}

playerprestreamrespawnorigin( var_0 )
{
    var_1 = scripts\mp\gametypes\br_public::playerstreamhintdroptoground( var_0.origin, var_0.height );
    self calloutmarkerping_getorigin( 0.0 );
    scripts\mp\gametypes\br_public::playerstreamhintlocation( var_1 );
    return var_1;
}

gulagplayerwatchfordeath( var_0, var_1 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self endon( "gulag_end" );
    self endon( "gulagLost" );
    self waittill( "death", var_2 );

    if ( istrue( level.gulag.onekillwin ) )
        handleonekillwin( var_0, self, var_2, var_1 );

    thread gulagplayerlost( var_0 );
}

gulagplayerlost( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self notify( "gulagLost" );

    if ( istrue( self.gulagloser ) )
        return;

    self.gulagloser = 1;
    self.spectatepoint = var_0.spectatepoint;
    thread playersetwasingulag( 1 );
    var_0.jailedplayers = scripts\engine\utility::array_removeundefined( var_0.jailedplayers );

    if ( var_0.jailedplayers.size > 0 && getdvarint( "scr_br_fc_spectate_outlines", 0 ) )
        self hudoutlinedisableforclients( var_0.jailedplayers );

    var_0.arenaplayers = scripts\engine\utility::array_removeundefined( var_0.arenaplayers );

    if ( var_0.arenaplayers.size > 0 && getdvarint( "scr_br_fc_outline_countdown", 1 ) > 0 )
        self hudoutlinedisableforclients( var_0.arenaplayers );

    self setclientomnvar( "ui_gulag", 0 );
    var_0.arenaplayers = scripts\engine\utility::array_remove( var_0.arenaplayers, self );

    if ( isdefined( self ) )
        var_1 = self.name;
    else
        var_1 = "<undefined>";

    self setplayermusicstate( "br_gulag_lose" );
    scripts\mp\weapons::deleteplacedequipment();
    scripts\mp\gametypes\br::markplayeraseliminated( self );
    scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_lose", self, 0, 1 );
    scripts\mp\gametypes\br_public::brleaderdialogteamexcludeplayer( "gulag_teammate_lose", self.team, self, 0, 0 );

    if ( isalive( self ) )
    {
        thread scripts\mp\gametypes\br_spectate::triggereliminatedoverlay();
        self.plotarmor = 1;
        self freezecontrols( 1 );
    }

    wait 2.0;
    self clearclienttriggeraudiozone( 2 );

    if ( istrue( level.gulag.arenaflag ) )
        var_0.dom.arenaflag thread arenaflag_setvisibleplayer( 0, self );

    scripts\cp_mp\challenges::ongulagendmatch( 2 );
    scripts\mp\gametypes\br_analytics::branalytics_gulagend( self, "loser" );
    playerdestroyhud( var_0 );
    setplayervargulag( 0 );
    setplayervargulagarena( 0, 1 );
    setplayervargulagjail( 0 );

    if ( isalive( self ) )
    {
        if ( !scripts\mp\gametypes\br_gametypes::isfeaturedisabled( "teamSpectate" ) )
            scripts\mp\gametypes\br_spectate::migratespectators( self, undefined, 1 );

        gulagfadetoblack();
        wait 1;

        if ( !scripts\mp\gametypes\br_gametypes::isfeaturedisabled( "teamSpectate" ) )
            scripts\mp\gametypes\br_spectate::migratespectators( self, undefined, 1 );

        self.plotarmor = undefined;
        var_2 = spawnstruct();
        var_2.origin = self.origin;
        var_2.angles = self.angles;
        var_2.attacker = self.lastattacker;
        self.health = 0;
        scripts\mp\gametypes\br_spectate::spawnspectator( var_2, 1, 1 );
        scripts\mp\playerlogic::removefromalivecount( 0 );
        gulagfadefromblack();
    }
}

playersetwasingulag( var_0 )
{
    if ( istrue( level.usegulag ) )
    {
        if ( var_0 )
            self.wasingulag = var_0;
        else
            self.wasingulag = undefined;

        setplayeringulagarenaextrainfo( var_0 );
    }
}

playerspawningasspectator()
{
    if ( istrue( level.usegulag ) && istrue( self.wasingulag ) && level.teamdata[self.team]["aliveCount"] > 0 )
        scripts\mp\gametypes\br_public::brleaderdialogplayer( "gulag_buyback", self, 0, 1 );
}

recordmolotov( var_0, var_1 )
{
    if ( istrue( var_1.gulagarena ) )
    {
        var_2 = var_1.arena;
        var_2.molotovs[var_2.molotovs.size] = var_0;
    }
}

cleanuparenamolotovs( var_0 )
{
    foreach ( var_2 in var_0.molotovs )
    {
        if ( isdefined( var_2 ) )
            thread scripts\mp\equipment\molotov::molotov_cleanup_pool( var_2 );
    }

    var_0.molotovs = [];
}

gulagisspawnpositionwithinsafecircle( var_0 )
{
    var_1 = isdefined( level.br_circle ) && isdefined( level.br_circle.safecircleent );

    if ( !var_1 )
        return 1;

    var_2 = scripts\mp\gametypes\br_circle::getsafecircleorigin();
    var_3 = scripts\mp\gametypes\br_circle::getsafecircleradius();
    var_4 = distance2d( var_0, var_2 );
    return var_4 < var_3;
}

gulagisspawnpositionwithindangercircle( var_0 )
{
    var_1 = isdefined( level.br_circle ) && isdefined( level.br_circle.dangercircleent );

    if ( !var_1 )
        return 1;

    var_2 = scripts\mp\gametypes\br_circle::getdangercircleorigin();
    var_3 = scripts\mp\gametypes\br_circle::getdangercircleradius();
    var_4 = distance2d( var_0, var_2 );
    return var_4 < var_3;
}

gulagisspawnpositionreasonablysafe( var_0, var_1 )
{
    if ( !scripts\mp\gametypes\br_c130::ispointinbounds( var_0, 1 ) )
        return 0;

    if ( !isdefined( level.br_circle ) || !isdefined( level.br_circle.safecircleent ) || !isdefined( level.br_circle.dangercircleent ) )
        return 1;

    if ( gulagisspawnpositionwithinsafecircle( var_0 ) )
        return 1;

    if ( !gulagisspawnpositionwithindangercircle( var_0 ) )
        return 0;

    if ( isdefined( var_1 ) )
    {
        var_2 = scripts\mp\gametypes\br_circle::getmintimetillpointindangercircle( var_0 );

        if ( var_1 > var_2 )
            return 0;
    }

    var_3 = scripts\mp\gametypes\br_circle::getsafecircleorigin();
    var_4 = scripts\mp\gametypes\br_circle::getsafecircleradius();
    var_5 = scripts\mp\gametypes\br_circle::getdangercircleorigin();
    var_6 = scripts\mp\gametypes\br_circle::getdangercircleradius();
    var_7 = length( var_5 - var_3 );
    var_8 = vectornormalize( var_5 - var_3 );
    var_9 = level.respawncircleinterppct;
    var_10 = var_3 + var_8 * var_7 * var_9;
    var_11 = var_4 + ( var_6 - var_4 ) * var_9;
    var_12 = distance2d( var_0, var_10 );
    return var_12 < var_11;
}

isplayervalidrespawntarget( var_0, var_1 )
{
    if ( var_0 == self )
        return 0;

    if ( !isalive( var_0 ) || var_0 scripts\mp\gametypes\br_public::isplayerinorgoingtogulag() || istrue( var_0.br_iseliminated ) )
        return 0;

    if ( !gulagisspawnpositionreasonablysafe( var_0.origin, var_1 ) )
        return 0;

    return 1;
}

playergetbestrespawnteammate( var_0, var_1 )
{
    var_2 = undefined;

    if ( istrue( level.onlinegame ) && self getprivatepartysize() )
    {
        var_3 = undefined;

        foreach ( var_5 in self getfireteammembers() )
        {
            if ( !isplayervalidrespawntarget( var_5, var_1 ) )
                continue;

            var_3 = var_5;

            if ( var_5 isfireteamleader() )
                break;
        }

        if ( isdefined( var_3 ) && istrue( var_0 ) )
        {
            var_2 = var_3;
            var_3 = undefined;

            foreach ( var_5 in self getfireteammembers() )
            {
                if ( isdefined( var_2 ) && var_2 == var_5 )
                    continue;

                if ( !isplayervalidrespawntarget( var_5, var_1 ) )
                    continue;

                var_3 = var_5;

                if ( var_5 isfireteamleader() )
                    break;
            }
        }

        if ( isdefined( var_3 ) )
            return var_3;
    }

    if ( isdefined( self.lastdeathpos ) )
    {
        var_9 = undefined;
        var_10 = undefined;
        var_11 = scripts\mp\utility\teams::getfriendlyplayers( self.team, 1 );

        foreach ( var_13 in var_11 )
        {
            if ( isdefined( var_2 ) && var_2 == var_13 )
                continue;

            if ( !isplayervalidrespawntarget( var_13, var_1 ) )
                continue;

            if ( var_13 isparachuting() || var_13 isskydiving() )
                continue;

            var_14 = distance2dsquared( self.lastdeathpos, var_13.origin );

            if ( !isdefined( var_10 ) || var_14 < var_10 )
            {
                var_9 = var_13;
                var_10 = var_14;
            }
        }

        if ( isdefined( var_9 ) && istrue( var_0 ) && !isdefined( var_2 ) )
        {
            var_2 = var_9;
            var_9 = undefined;
            var_10 = undefined;

            foreach ( var_13 in var_11 )
            {
                if ( isdefined( var_2 ) && var_2 == var_13 )
                    continue;

                if ( !isplayervalidrespawntarget( var_13, var_1 ) )
                    continue;

                if ( var_13 isparachuting() || var_13 isskydiving() )
                    continue;

                var_14 = distance2dsquared( self.lastdeathpos, var_13.origin );

                if ( !isdefined( var_10 ) || var_14 < var_10 )
                {
                    var_9 = var_13;
                    var_10 = var_14;
                }
            }
        }

        if ( isdefined( var_9 ) )
            return var_9;
    }

    var_9 = undefined;
    var_18 = scripts\engine\utility::array_randomize( level.teamdata[self.team]["alivePlayers"] );

    foreach ( var_20 in var_18 )
    {
        if ( isdefined( var_2 ) && var_2 == var_20 )
            continue;

        if ( !isplayervalidrespawntarget( var_20, var_1 ) )
            continue;

        var_9 = var_20;

        if ( istrue( var_20 scripts\mp\gametypes\br_public::isplayerbrsquadleader() ) )
            break;
    }

    if ( isdefined( var_9 ) )
        return var_9;
    else if ( scripts\mp\utility\game::getsubgametype() == "dmz" )
    {
        var_22 = scripts\mp\utility\teams::getteamdata( self.team, "teamCount" );

        if ( var_22 == 1 && !istrue( self.disconnecting ) )
            return self;
    }

    return undefined;
}

getsafeoriginaroundpoint( var_0, var_1, var_2 )
{
    var_3 = 3.14159;
    var_4 = scripts\mp\gametypes\br_circle::getsafecircleorigin();
    var_5 = vectornormalize( var_0 - var_4 );
    var_6 = vectortoangles( var_5 );
    var_7 = randomfloatrange( getdvarfloat( "scr_br_respawn_rand_ang_min", 10.0 ), getdvarfloat( "scr_br_respawn_rand_ang_max", 60.0 ) );
    var_8 = var_5;
    var_9 = var_0 + var_8 * var_1;

    if ( gulagisspawnpositionreasonablysafe( var_9, var_2 ) )
        return var_9;

    var_8 = var_8 * -1;
    var_9 = var_0 + var_8 * var_1;

    if ( gulagisspawnpositionreasonablysafe( var_9, var_2 ) )
        return var_9;

    var_8 = vectornormalize( var_4 - var_0 );
    var_9 = var_0 + var_8 * var_1;

    if ( gulagisspawnpositionreasonablysafe( var_9, var_2 ) )
        return var_9;

    var_10 = var_1;
    var_11 = distance2d( var_0, var_4 );
    var_12 = var_10 / var_11;

    if ( var_12 > var_3 )
        var_12 = var_3;

    var_13 = var_12 * 180 / var_3;
    var_9 = rotatepointaroundvector( ( 0, 0, 1 ), var_0 - var_4, var_13 ) + var_4;

    if ( gulagisspawnpositionreasonablysafe( var_9, var_2 ) )
        return var_9;

    var_9 = level.br_level.br_circlecenters;
    return var_9;
}

playergetbestrespawnmissionorigin()
{
    foreach ( var_5, var_1 in level.questinfo.quests )
    {
        foreach ( var_4, var_3 in var_1.instances )
        {
            if ( var_4 == self.team && !scripts\mp\gametypes\br_quest_util::isquestinstancealocale( var_3 ) )
            {
                switch ( var_3.category )
                {
                    case "assassination":
                        if ( isdefined( var_3.targetplayer ) )
                            return var_3.targetplayer.origin;

                        break;
                    case "domination":
                        if ( isdefined( var_3.subscribedlocale ) && isdefined( var_3.subscribedlocale.domflag ) && isdefined( var_3.subscribedlocale.domflag.curorigin ) )
                            return var_3.subscribedlocale.domflag.curorigin;

                        break;
                    case "scavenger":
                        if ( isdefined( var_3.subscribedlocale.cacheentity.origin ) && isdefined( var_3.subscribedlocale.cacheentity ) )
                            return var_3.subscribedlocale.cacheentity.origin;

                        break;
                    default:
                        break;
                }
            }
        }
    }

    return undefined;
}

playergetbestdropbagorigin( var_0 )
{
    foreach ( var_2 in level.br_pickups.crates )
    {
        if ( !isdefined( var_2 ) || !isdefined( var_2.team ) || var_2.team != self.team )
            continue;

        if ( isdefined( var_2.playerscaptured ) && isdefined( var_2.playerscaptured[self getentitynumber()] ) )
            continue;

        if ( gulagisspawnpositionreasonablysafe( var_2.origin, var_0 ) )
            return var_2.origin;
    }
}

playergulaggetrespawnpoint( var_0, var_1 )
{
    var_2 = undefined;
    var_3 = undefined;
    var_4 = undefined;
    var_5 = 1;

    if ( isdefined( self.setspawnpoint ) )
    {
        var_2 = self.setspawnpoint.playerspawnpos;
        var_3 = self.setspawnpoint.playerspawnangles;
    }

    var_6 = getdvarfloat( "scr_br_respawnMaxTeammateOffset", 1000 );

    if ( !isdefined( var_2 ) && var_6 >= 0 )
    {
        var_7 = playergetbestrespawnteammate( var_0, var_1 );

        if ( isdefined( var_7 ) )
        {
            var_8 = getdvarfloat( "scr_bmo_respawn_intermission_time", 5.0 ) * 1000;
            var_9 = getdvarfloat( "scr_bmo_redeploy_window", 30.0 ) * 1000;

            if ( getdvar( "scr_br_gametype", "" ) == "dmz" && isdefined( level.teamdata[self.team]["lastParachuteTime"] ) && level.teamdata[self.team]["lastParachuteTime"] + var_9 > gettime() + var_8 && distance2d( level.teamdata[self.team]["lastParachuteOrigin"], var_7.origin ) < getdvarfloat( "scr_br_respawnMaxTeammateOffset", 1000 ) * 1.25 )
            {
                var_2 = level.teamdata[self.team]["lastParachuteOrigin"];
                var_3 = level.teamdata[self.team]["lastParachuteAngles"];
            }
            else
            {
                var_2 = scripts\engine\utility::drop_to_ground( var_7.origin, 1500, -20000 );
                var_2 = getsafeoriginaroundpoint( var_2, var_6, var_1 );
                var_3 = getanglesfacingorigin( var_2, var_7.origin );
                level.teamdata[self.team]["lastParachuteOrigin"] = var_2;
                level.teamdata[self.team]["lastParachuteAngles"] = var_3;
                level.teamdata[self.team]["lastParachuteTime"] = gettime();
            }
        }
    }

    var_6 = getdvarfloat( "scr_br_respawnMaxMissionOffset", 3000 );

    if ( !isdefined( var_2 ) && var_6 >= 0 )
    {
        var_10 = playergetbestrespawnmissionorigin();

        if ( isdefined( var_10 ) )
        {
            var_2 = getsafeoriginaroundpoint( var_10, var_6, var_1 );
            var_3 = getanglesfacingorigin( var_2, var_10 );
        }
    }

    var_6 = getdvarfloat( "scr_br_respawnMaxCrateOffset", 3000 );

    if ( !isdefined( var_2 ) && var_6 >= 0 )
    {
        var_11 = playergetbestdropbagorigin( var_1 );

        if ( isdefined( var_11 ) )
        {
            var_2 = getsafeoriginaroundpoint( var_11, var_6, var_1 );
            var_3 = getanglesfacingorigin( var_2, var_11 );
        }
    }

    if ( !isdefined( var_2 ) )
    {
        if ( isdefined( level.br_circle ) && isdefined( level.br_circle.safecircleent ) )
        {
            var_12 = scripts\mp\gametypes\br_circle::getsafecircleorigin();
            var_13 = scripts\mp\gametypes\br_circle::getsafecircleradius();
            var_14 = getdvarfloat( "scr_br_minRespawnRadiusPct", 0.5 );
            var_15 = getdvarfloat( "scr_br_maxRespawnRadiusPct", 0.9 );
            var_16 = getdvarint( "scr_br_maxRespawnDropToGround", 1 );
            var_17 = getdvarint( "scr_br_maxRespawnSnapToNavMesh", 1 );
            var_2 = scripts\mp\gametypes\br_circle::getrandompointinboundscircle( var_12, var_13, var_14, var_15, var_16, var_17, 0, var_1 );
        }
        else if ( isdefined( level.prematchspawnorigins ) )
        {
            if ( isdefined( level.teamdata[self.team]["chosenSpawnWipeOrigin"] ) && isdefined( level.teamdata[self.team]["spawnWipeOriginUseStartTime"] ) && isdefined( level.autorespawnwaittime ) && level.teamdata[self.team]["spawnWipeOriginUseStartTime"] + level.autorespawnwaittime * 1000 > gettime() )
                var_2 = level.teamdata[self.team]["chosenSpawnWipeOrigin"];
            else
            {
                var_18 = [];

                foreach ( var_20 in level.prematchspawnorigins )
                {
                    if ( distance2dsquared( var_20.origin, self.origin ) > var_6 )
                        var_18[var_18.size] = var_20;
                }

                if ( var_18.size == 0 )
                    var_18 = level.prematchspawnorigins;

                var_18 = scripts\engine\utility::array_randomize( var_18 );
                var_2 = var_18[0].origin;
                var_2 = var_2 + scripts\engine\math::random_vector_2d() * randomfloatrange( 100, 500 );
                level.teamdata[self.team]["chosenSpawnWipeOrigin"] = var_2;
                level.teamdata[self.team]["spawnWipeOriginUseStartTime"] = gettime();
            }

            var_3 = ( 0, 0, 0 );

            if ( var_2[2] > 10000 )
            {
                var_5 = 0;
                var_4 = scripts\mp\gametypes\br_infils::getinfilspawnoffset();
            }
        }
        else
        {
            var_2 = ( 0, 0, 0 );
            var_3 = ( 0, 0, 0 );
        }
    }

    if ( !isdefined( var_3 ) )
    {
        var_22 = scripts\mp\gametypes\br_circle::getsafecircleorigin();
        var_23 = vectortoyaw( var_22 - var_2 );
        var_3 = ( 0, var_23, 0 );
    }

    if ( var_5 )
    {
        var_4 = scripts\cp_mp\parachute::getc130height();

        if ( isdefined( level.br_circle ) )
        {
            var_24 = level.br_circle.circleindex;
            var_25 = getgulagclosedcircleindex();
            var_26 = isdefined( var_24 ) && var_24 >= var_25;

            if ( var_26 )
                var_4 = var_4 * getdvarfloat( "scr_br_gulagClosedSpawnOffsetScaler", 0.55 );
        }

        if ( isdefined( level.respawnheightoverride ) )
            var_4 = level.respawnheightoverride;

        var_27 = ( 0, 0, var_4 );
        var_2 = scripts\mp\gametypes\br::getoffsetspawnorigin( var_2, var_27 );
    }
    var_28 = spawnstruct();
    var_28.origin = self.origin + ( 1, 1, 5200 );
    if ( level.mzsmode == "dmzsolo" || level.mzsmode == "dmzduo" )
      var_28.origin = self.origin + ( 1, 1, 3500 ); // SPAWNS SYSTEM
    var_28.angles = var_3;
    var_28.height = var_4;
    return var_28;
}

getanglesfacingorigin( var_0, var_1 )
{
    var_2 = vectortoyaw( var_1 - var_0 );
    var_3 = ( 0, var_2, 0 );
    return var_3;
}

gulagwinnerrespawn( var_0, var_1, var_2, var_3, var_4, var_5, var_6 )
{
    var_7 = !istrue( var_0 );

    if ( !istrue( var_0 ) )
        scripts\cp_mp\challenges::ongulagendmatch( 1 );

    if ( isdefined( var_1 ) )
        scripts\mp\gametypes\br_analytics::branalytics_gulagend( self, var_1 );

    playersetwasingulag( 0 );
    setplayervargulag( 0 );
    setplayervargulagarena( 0 );
    playerdestroyhud( self.arena );
    setplayervargulagjail( 0 );
    level notify( "update_circle_hide" );
    self.jailtime = undefined;

    if ( isdefined( self.oobimmunity ) )
        scripts\mp\outofbounds::disableoobimmunity( self );

    scripts\mp\gametypes\br::givelaststandifneeded( self );
    playertakeawayrock( self.arena );
    gulagwinnerloadout();

    if ( isdefined( level.gulag ) && istrue( level.gulag.planerespawn ) )
    {
        playerrespawngulagcleanup( var_0 );
        gulagstreamexit();
        playersetupac130();
        return;
    }

    if ( !isdefined( var_2 ) )
        var_2 = playergulaggetrespawnpoint();

    var_8 = var_2.origin;
    var_9 = var_2.angles;
    var_10 = var_8;

    if ( isdefined( var_4 ) )
        var_10 = var_4;

    gulagstreamexit();
    playerteleportgulag( var_10, var_9 );
    var_11 = spawn( "script_model", var_10 );
    var_11 setmodel( "tag_origin" );
    var_11.angles = var_9;
    var_11 hide();
    var_11 showtoplayer( self );
    self playerlinktoabsolute( var_11, "tag_origin" );
    self playerhide();
    thread playercleanupentondisconnect( var_11 );
    waitframe();
    playerrespawngulagcleanup( var_0 );

    if ( getdvarint( "scr_skip_respawn_gate", 1 ) == 0 )
        scripts\mp\gametypes\br_public::playerwaittillstreamhintcomplete();

    scripts\mp\gametypes\br_public::playerclearstreamhintorigin();

    if ( isdefined( var_4 ) )
        var_11.origin = var_8;

    var_11 playsoundtoplayer( "br_ac130_flyby", self );
    wait 1.5;
    self unlink();
    self clearsoundsubmix( "deaths_door_mp" );
    self clearclienttriggeraudiozone( 1 );
    self playershow();
    playergulagvictorysetcontrols( 1 );
    var_12 = 0;

    if ( isdefined( level.parachutedeploydelay ) )
        var_12 = level.parachutedeploydelay;

    if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
        thread scripts\cp_mp\parachute::startfreefall( var_12, 0, undefined, undefined, 1 );

    if ( !istrue( var_3 ) )
        thread triggerrespawnoverlay( 7 );

    if ( istrue( var_5 ) )
        self setclientomnvar( "ui_br_transition_type", 0 );

    self setclientomnvar( "ui_show_spectateHud", -1 );
    resetplayermovespeedscale();

    if ( !scripts\mp\gametypes\br_gametypes::isfeaturedisabled( "armor" ) )
        scripts\mp\gametypes\br_armor::givestartingarmor();

    scripts\mp\gametypes\br_quest_util::onplayerrespawn();
    scripts\mp\gametypes\br_rewards::onplayerrespawn();
    wait 0.5;
    gulagfadefromblack();
    waitframe();
    var_11 delete();

    if ( scripts\mp\gametypes\br_public::isbotpracticematch() )
        self notify( "respawn_from_gulag" );

    self notify( "can_show_splashes" );

    if ( istrue( getdvar( "scr_br_gametype", "" ) == "dmz" ) )
    {
        var_13 = "br_gulag_winner_redeploy_mogulag";
        var_14 = undefined;
    }
    else if ( istrue( var_0 ) )
    {
        var_13 = "br_gulag_kiosk_redeploy";
        var_14 = var_6;
    }
    else
    {
        if ( istrue( isdefined( level.gulag ) && !istrue( level.gulag.shutdown ) ) && checkgulagusecount() )
            var_13 = "br_gulag_winner_redeploy_mogulag";
        else
            var_13 = "br_gulag_winner_redeploy";

        var_14 = undefined;
    }

    thread scripts\mp\hud_message::showsplash( var_13, undefined, var_14 );

    if ( isdefined( level.gulag ) && istrue( level.gulag.shutdown ) && !istrue( self.gulagdone ) )
    {
        wait 2.0;
        playergulagdonesplash();
    }
}

playercleanupentondisconnect( var_0 )
{
    var_0 endon( "death" );
    self waittill( "disconnect" );

    if ( isdefined( var_0 ) )
        var_0 delete();
}

playergulagvictorysetcontrols( var_0 )
{
    if ( var_0 )
    {
        self enableoffhandweapons();
        self enableusability();
    }
    else
    {
        self disableoffhandweapons();
        self disableusability();
    }
}

triggerrespawnoverlay( var_0 )
{
    self endon( "disconnect" );
    var_1 = scripts\mp\gametypes\br_gametypes::runbrgametypefunc( "triggerRespawnOverlay" );

    if ( istrue( var_1 ) )
        return;

    wait 0.5;
    thread scripts\mp\hud_message::showsplash( "br_gulag_winner" );
}

playerrespawngulagcleanup( var_0 )
{
    self notify( "gulagRespawn" );
    scripts\mp\equipment\molotov::molotov_clear_burning();
    self.health = self.maxhealth;
    scripts\mp\healthoverlay::onexitdeathsdoor( 1 );

    if ( !istrue( var_0 ) )
    {
        scripts\mp\utility\player::enableplayerforspawnlogic( 0 );
        self setclientomnvar( "ui_gulag", 0 );
        scripts\mp\gametypes\br_public::updatebrscoreboardstat( "isRespawning", 0 );
        self.arena = undefined;
    }
}

gulagwinnerloadout()
{
    if ( getdvarint( "scr_br_fc_keep_gun", 0 ) != 0 )
        gulagwinnerremembergunandammo();
    else if ( isdefined( level.br_standard_loadout ) || getdvarint( "scr_br_fc_loadouts", 1 ) != 0 && getdvarint( "scr_br_fc_winner_loadout", -1 ) > -1 )
        self.gulagtableloadout = 1;
}

gulagwinnerrestoreloadout()
{
    if ( !istrue( level.usegulag ) && !scripts\mp\gametypes\br_gametypes::isfeaturedisabled( "gulagWinnerRestoreLoadoutUseGulag" ) )
        return 0;

    var_0 = 0;

    if ( getdvarint( "scr_br_fc_keep_gun", 0 ) != 0 )
        var_0 = gulagwinnerrestoregunandammo();
    else if ( isdefined( level.br_standard_loadout ) || getdvarint( "scr_br_fc_loadouts", 1 ) != 0 && getdvarint( "scr_br_fc_winner_loadout", -1 ) > -1 )
        var_0 = gulagwinnertableloadout();

    return var_0;
}

gulagwinnerremembergunandammo()
{
    self.br_gulagguncurrent = self getcurrentprimaryweapon();
    var_0 = self getweaponslistprimaries();
    var_0 = scripts\engine\utility::array_remove( var_0, makeweapon( "iw8_knifestab_mp" ) );
    var_0 = scripts\engine\utility::array_remove( var_0, makeweapon( "iw8_fists_mp" ) );
    self.br_gulagguns = [];
    self.br_gulagammo = [];

    foreach ( var_2 in var_0 )
    {
        var_3 = getcompleteweaponname( var_2 );

        if ( getsubstr( var_3, 0, 4 ) == "alt_" )
            continue;

        self.br_gulagguns[self.br_gulagguns.size] = var_2;
        self.br_gulagammo[var_3] = weaponclipsize( var_2 ) * 3;
    }

    var_5 = self getweaponslistoffhands();
    self.br_gulagoffhands = [];

    foreach ( var_7 in var_5 )
    {
        var_8 = self getweaponammoclip( var_7 );

        if ( var_8 <= 0 )
            continue;

        self.br_gulagoffhands[self.br_gulagoffhands.size] = var_7;
        var_9 = getcompleteweaponname( var_7 );
        self.br_gulagammo[var_9] = weaponstartammo( var_7 );
    }
}

gulagwinnerrestoregunandammo()
{
    if ( isdefined( self.br_gulagguns ) && isdefined( self.br_gulagoffhands ) && isdefined( self.br_gulagammo ) )
    {
        self takeallweapons();
        scripts\mp\gametypes\br_weapons::br_ammo_player_clear();
        self.equipment["primary"] = undefined;
        self.equipment["secondary"] = undefined;
        self.equipment["health"] = undefined;
        self.equipment["super"] = undefined;

        if ( self.br_gulagguns.size < 2 )
            self giveweapon( makeweapon( "iw8_fists_mp" ) );

        var_0 = 0;

        foreach ( var_2 in self.br_gulagguns )
        {
            var_3 = getcompleteweaponname( var_2 );
            scripts\cp_mp\utility\inventory_utility::_giveweapon( var_2 );
            self setweaponammostock( var_2, 0 );

            if ( !var_0 )
            {
                self assignweaponprimaryslot( var_3 );
                scripts\cp_mp\utility\inventory_utility::_switchtoweapon( var_2 );
                var_0 = 1;
            }

            scripts\mp\weapons::fixupplayerweapons( self, var_3 );
            thread scripts\mp\gametypes\br_respawn::giveweaponpickup( var_3 );
            var_4 = weaponclipsize( var_2 );
            var_5 = int( min( var_4, 25 ) );

            if ( isdefined( self.br_gulagammo[var_3] ) )
                var_5 = int( max( var_5, self.br_gulagammo[var_3] ) );

            var_6 = 0;

            if ( var_5 > var_4 )
            {
                var_6 = var_5 - var_4;
                var_5 = var_4;
            }

            self setweaponammoclip( var_2, var_5 );

            if ( var_6 > 0 )
            {
                var_7 = scripts\mp\gametypes\br_weapons::br_ammo_type_for_weapon( var_2 );
                scripts\mp\gametypes\br_weapons::br_ammo_give_type( self, var_7, var_6 );
            }
        }

        scripts\mp\gametypes\br_weapons::br_ammo_update_weapons( self );

        foreach ( var_10 in self.br_gulagoffhands )
        {
            var_11 = scripts\mp\equipment::getequipmentreffromweapon( var_10 );

            if ( isdefined( var_11 ) && isdefined( level.br_pickups.br_equipnametoscriptable[var_11] ) )
            {
                var_12 = level.br_pickups.br_equipnametoscriptable[var_11];
                scripts\mp\gametypes\br_pickups::br_forcegivecustompickupitem( self, var_12, 1 );
                var_3 = getcompleteweaponname( var_10 );

                if ( isdefined( self.br_gulagammo[var_3] ) )
                {
                    var_13 = self.br_gulagammo[var_3];
                    self setweaponammoclip( var_10, var_13 );
                }
            }
        }

        if ( isdefined( self.br_gulagguncurrent ) )
            self switchtoweaponimmediate( self.br_gulagguncurrent );

        self.br_gulaggun = undefined;
        self.br_gulagammo = undefined;
        self.br_gulagoffhands = undefined;
        self.br_gulagguncurrent = undefined;
        return 1;
    }

    return 0;
}

gulagwinnertableloadout()
{
    if ( istrue( self.gulagtableloadout ) )
    {
        var_0 = getdvarint( "scr_br_fc_winner_loadout", -1 );

        if ( var_0 > -1 )
            self.pers["gamemodeLoadout"] = level.prematchloadouts[var_0];
        else
            self.pers["gamemodeLoadout"] = level.br_standard_loadout;

        self.class = "gamemode";
        self.prevweaponobj = undefined;
        var_1 = scripts\mp\class::loadout_getclassstruct();
        var_1 = scripts\mp\class::loadout_updateclass( var_1, "gamemode" );
        scripts\mp\class::preloadandqueueclassstruct( var_1, 1, 1 );
        scripts\mp\class::giveloadout( self.team, "gamemode", 0, 0 );
        self givestartammo( var_1.loadoutprimaryobject );
        self givestartammo( var_1.loadoutsecondaryobject );
        scripts\mp\gametypes\br::givelaststandifneeded( self );
        scripts\mp\gametypes\br_weapons::br_ammo_player_clear();
        scripts\mp\gametypes\br_weapons::br_give_starting_ammo();
        scripts\mp\gametypes\br_weapons::br_ammo_update_weapons( self );
        self notify( "ammo_update" );
        thread scripts\mp\gametypes\br::br_displayperkinfo();
        self.gulagtableloadout = undefined;
        return 1;
    }

    return 0;
}

popnextmatch( var_0 )
{
    var_1 = var_0.matches[0];
    var_0.matches = scripts\engine\utility::array_remove_index( var_0.matches, 0 );
    return var_1;
}

checkgulagusecount()
{
    if ( level.gulag.maxuses >= 0 )
    {
        var_0 = self.gulaguses;

        if ( !isdefined( var_0 ) )
            var_0 = 0;

        if ( var_0 >= level.gulag.maxuses )
            return 0;
    }

    return 1;
}

trygulagspawn()
{
    if ( !istrue( self.br_infilstarted ) || !scripts\mp\flags::gameflag( "prematch_done" ) )
        return 0;

    if ( istrue( self.gulag ) )
        return 0;

    if ( scripts\mp\gametypes\br_public::hasrespawntoken() && !scripts\mp\gametypes\br_pickups::respawntokendisabled() )
    {
        thread playergulagautowin();
        return 1;
    }

    if ( !istrue( level.usegulag ) )
        return 0;

    if ( istrue( level.gulag.shutdown ) && !playerinlaststandhold() )
        return 0;

    if ( getdvarint( "scr_br_all_assassin_version", 0 ) )
        return 0;

    if ( istrue( self.gulagloser ) )
        return 0;

    if ( !checkgulagusecount() )
        return 0;

    thread initplayerjail( 1 );
    return 1;
}

playergulaghud( var_0 )
{
    if ( isdefined( var_0.fightover ) && !var_0.fightover )
        playersetgulagdataomnvar( var_0 );

    if ( isdefined( var_0.fightover ) && !var_0.fightover && isdefined( var_0.time ) && var_0.time > 0 )
    {
        var_1 = var_0.time;

        if ( !istrue( var_0.overtime ) )
        {
            var_2 = getovertimelength();
            var_1 = var_1 - var_2;
        }

        self setclientomnvar( "ui_br_gulag_match_end_time", gettime() + int( var_1 * 1000 ) );
    }
    else
        self setclientomnvar( "ui_br_gulag_match_end_time", 0 );
}

updatematchtimerhud( var_0, var_1 )
{
    _updatematchtimerhudinternal( var_0.arenaplayers, var_1 );
    _updatematchtimerhudinternal( var_0.jailedplayers, var_1 );
}

_updatematchtimerhudinternal( var_0, var_1 )
{
    foreach ( var_3 in var_0 )
    {
        if ( !isdefined( var_3 ) )
            continue;

        if ( var_1 > 0 )
        {
            var_3 setclientomnvar( "ui_br_gulag_match_end_time", gettime() + int( var_1 * 1000 ) );
            continue;
        }

        var_3 setclientomnvar( "ui_br_gulag_match_end_time", 0 );
    }
}

updatematchqueuepositions( var_0 )
{
    for ( var_1 = 2; var_1 <= level.gulag.maxplayers; var_1 = var_1 + 2 )
    {
        var_2 = [];

        foreach ( var_4 in var_0.jailedplayers )
        {
            if ( !isdefined( var_4 ) )
                continue;

            var_5 = 0;

            foreach ( var_12, var_7 in var_2 )
            {
                if ( var_7.size >= var_1 )
                    continue;

                var_8 = 0;

                foreach ( var_10 in var_7 )
                {
                    if ( var_10.team == var_4.team )
                    {
                        var_8 = 1;
                        break;
                    }
                }

                if ( !var_8 )
                {
                    var_5 = 1;
                    var_4.gulagposition = var_12 + 1;
                    var_2[var_12][var_2[var_12].size] = var_4;
                    break;
                }
            }

            if ( !var_5 )
            {
                var_4.gulagposition = var_2.size + 1;
                var_2[var_2.size] = [ var_4 ];
            }
        }

        var_0.matches = var_2;

        if ( var_2.size <= level.gulag.maxqueue )
            break;
    }

    updatearenaomnvarplayers( var_0 );
}

playerwatchdisconnect( var_0 )
{
    self endon( "gulagLost" );
    self endon( "gulag_end" );
    self waittill( "death_or_disconnect" );

    if ( isdefined( self ) && istrue( self.gulagarena ) )
        return;

    if ( isdefined( self ) )
    {
        if ( istrue( self.gulagarena ) )
            var_0.arenaplayers = scripts\engine\utility::array_remove( var_0.arenaplayers, self );
        else if ( istrue( self.jailed ) )
            var_0.jailedplayers = scripts\engine\utility::array_remove( var_0.jailedplayers, self );

        playerdestroyhud( var_0 );
    }
    else
    {
        var_0.jailedplayers = scripts\engine\utility::array_removeundefined( var_0.jailedplayers );
        var_0.arenaplayers = scripts\engine\utility::array_removeundefined( var_0.arenaplayers );
    }

    updatematchqueuepositions( var_0 );
}

playerdestroyhud( var_0 )
{
    self setclientomnvar( "ui_br_gulag_match_end_time", 0 );

    if ( isdefined( var_0 ) )
        playercleargulagomnvars( var_0 );

    if ( isdefined( self.gulagjailbreakhud ) )
        self.gulagjailbreakhud destroy();

    self.gulagloadoutindex = undefined;
}

setplayervargulag( var_0 )
{
    if ( isdefined( self.gulag ) && self.gulag == var_0 )
        return;

    self.gulag = var_0;
    level notify( "update_circle_hide" );
}

setplayervargulagarena( var_0, var_1 )
{
    if ( isdefined( self.gulagarena ) && self.gulagarena == var_0 )
        return;

    if ( !istrue( var_1 ) )
        setplayeringulagarenaextrainfo( var_0 );

    self.gulagarena = var_0;
    level notify( "update_circle_hide" );
}

setplayeringulagarenaextrainfo( var_0 )
{
    if ( istrue( var_0 ) )
        self.game_extrainfo = self.game_extrainfo | 1024;
    else
        self.game_extrainfo = self.game_extrainfo & ~1024;
}

setplayervargulagjail( var_0 )
{
    if ( isdefined( self.jailed ) && self.jailed == var_0 )
        return;

    setplayeringulagjailextrainfo( var_0 );
    self.jailed = var_0;
    level notify( "update_circle_hide" );
}

setplayeringulagjailextrainfo( var_0 )
{
    if ( var_0 )
        self.game_extrainfo = self.game_extrainfo | 512;
    else
        self.game_extrainfo = self.game_extrainfo & ~512;
}

setplayergulagindex( var_0 )
{
    if ( var_0.gulagindex > 7 )
        return;

    var_1 = 3;
    var_2 = 3;
    var_3 = int( pow( 2, var_1 ) ) - 1;
    var_4 = ( var_0.gulagindex & var_3 ) << var_2;
    var_5 = ~( var_3 << var_2 );
    var_6 = self.game_extrainfo;
    var_7 = var_6 & var_5;
    var_8 = var_7 + var_4;
    self.game_extrainfo = var_8;
}

startbetting( var_0, var_1 )
{
    if ( !level.gulag.betting )
        return undefined;

    var_2 = spawnstruct();
    var_2.fighters = var_1;
    var_2.bets = [];
    var_2.bettingopen = 1;
    var_2 thread show_betting_to_players( var_0 );
    return var_2;
}

show_betting_to_players( var_0 )
{
    self endon( "end_betting" );

    for (;;)
    {
        var_1 = getbettingplayers( var_0, self );

        foreach ( var_3 in var_1 )
        {
            if ( isbot( var_3 ) )
                continue;

            if ( isdefined( self.bets[var_3.guid] ) )
                continue;

            thread showbettinghud( var_0, var_3 );
        }

        var_0 waittill( "player_added_to_jail" );
    }
}

showbettinghud( var_0, var_1 )
{
    self endon( "end_betting" );
    var_2 = spawnstruct();
    var_2.owner = var_1;
    self.bets[var_1.guid] = var_2;
    var_3 = 10;
    var_4 = 0;
    var_2.hudfocusname = var_1 scripts\mp\hud_util::createfontstring( "default", 1.2 );
    var_2.hudfocusname scripts\mp\hud_util::setpoint( "LEFT", "CENTER", var_3, var_4 );
    var_2.hudfocusname.label = &"";
    var_2.hudfocusname.alpha = 0.0;
    var_5 = 5;
    var_6 = -50;
    var_2.hudavailable = var_1 scripts\mp\hud_util::createfontstring( "default", 1.2 );
    var_2.hudavailable scripts\mp\hud_util::setpoint( "LEFT", "LEFT", var_5, var_6 );
    var_2.hudavailable.label = &"MP/BR_GULAG_BET_AVAILABLE";
    var_2.hudavailable scripts\mp\hud::fontpulseinit();
    var_2.hudbetonplayer = var_1 scripts\mp\hud_util::createfontstring( "default", 1.5 );
    var_2.hudbetonplayer scripts\mp\hud_util::setpoint( "LEFT", "LEFT", var_5, var_6 + 25 );
    var_2.hudbetonplayer.label = &"";
    var_2.hudammount = var_1 scripts\mp\hud_util::createfontstring( "default", 1.5 );
    var_2.hudammount scripts\mp\hud_util::setpoint( "LEFT", "LEFT", var_5, var_6 + 40 );
    var_2.hudammount.label = &"MP/BR_GULAG_BET_AMMOUNT";
    var_2.playerbeton = -1;
    var_2.playerfocus = -1;
    var_2.ammount = 0;
    updatebethud( var_2 );
    thread updatebetplayerfocus( var_0, var_2 );
    thread watchbetplaced( var_2 );
}

updatebetplayerfocus( var_0, var_1 )
{
    self endon( "end_betting" );
    var_1.owner endon( "disconnect" );
    var_2 = 0.9;

    for (;;)
    {
        var_3 = -1;
        var_4 = -1.0;
        var_5 = var_1.owner geteye();
        var_6 = anglestoforward( var_1.owner getplayerangles() );

        foreach ( var_12, var_8 in self.fighters )
        {
            if ( !isdefined( var_8 ) )
                continue;

            var_9 = var_8.origin + ( 0, 0, 30 );
            var_10 = vectornormalize( var_9 - var_5 );
            var_11 = vectordot( var_6, var_10 );

            if ( var_11 < var_2 || var_11 < var_4 )
                continue;

            var_3 = var_12;
            var_4 = var_11;
        }

        if ( var_3 != var_1.playerfocus )
        {
            var_1.playerfocus = var_3;
            updateoutlines( var_0 );
            updatebethud( var_1 );
        }

        wait 0.05;
    }
}

watchbetclear( var_0 )
{
    self endon( "end_betting" );
    var_0.owner endon( "disconnect" );
    var_1 = "betClear";
    thread notifyonplayercommandbetting( var_0.owner, var_1, "+weapnext" );

    for (;;)
    {
        var_0.owner waittill( var_1 );

        if ( var_0.playerbeton == -1 )
            continue;

        var_0.ammount = 0;
        var_0.playerbeton = -1;
        updatebethud( var_0 );
    }
}

watchbetplaced( var_0 )
{
    self endon( "end_betting" );
    var_0.owner endon( "disconnect" );
    var_1 = "betPlaced";
    thread notifyonplayercommandbetting( var_0.owner, var_1, "+usereload" );

    for (;;)
    {
        var_0.owner waittill( var_1 );

        if ( var_0.playerfocus == -1 )
        {
            if ( var_0.playerbeton == -1 )
                continue;

            var_2 = 0;
        }
        else if ( var_0.playerbeton == -1 )
            var_2 = 1;
        else if ( var_0.playerbeton == var_0.playerfocus )
            var_2 = var_0.ammount + 1;
        else
            var_2 = 1;

        if ( var_2 > var_0.owner.plundercount )
        {
            betchangefail( var_0 );
            continue;
        }

        var_0.ammount = var_2;
        var_0.playerbeton = var_0.playerfocus;
        updatebethud( var_0 );
    }
}

notifyonplayercommandbetting( var_0, var_1, var_2 )
{
    var_0 notifyonplayercommand( var_1, var_2 );
    self waittill( "end_betting" );

    if ( isdefined( var_0 ) )
        var_0 notifyonplayercommandremove( var_1, var_2 );
}

betchangefail( var_0 )
{
    var_0.owner playlocalsound( "br_pickup_deny" );
    var_0.hudavailable thread scripts\mp\hud::fontpulse( var_0.owner );
}

updatebethud( var_0 )
{
    var_1 = 1000;
    var_0.hudavailable setvalue( ( var_0.owner.plundercount - var_0.ammount ) * var_1 );

    if ( var_0.playerbeton != -1 )
    {
        var_2 = self.fighters[var_0.playerbeton];

        if ( isdefined( var_2 ) )
        {
            var_0.hudbetonplayer setplayernamestring( var_2 );
            var_0.hudbetonplayer.alpha = 1.0;
        }
        else
            var_0.hudbetonplayer.alpha = 0.0;
    }
    else
        var_0.hudbetonplayer.alpha = 0.0;

    if ( var_0.ammount > 0 )
    {
        var_0.hudammount setvalue( var_0.ammount * var_1 );
        var_0.hudammount.alpha = 1.0;
    }
    else
        var_0.hudammount.alpha = 0.0;

    if ( var_0.playerbeton != -1 )
    {
        if ( var_0.playerfocus == var_0.playerbeton )
        {
            var_0.hudfocusname.alpha = 1.0;
            var_0.hudfocusname.label = &"MP/BR_GULAG_FOCUS_BET_INCREASE";
            var_0.hudfocusname settext( &"" );
        }
        else if ( var_0.playerfocus != -1 )
        {
            var_0.hudfocusname.alpha = 1.0;
            var_0.hudfocusname.label = &"MP/BR_GULAG_FOCUS_BET_CHANGE";
            var_2 = self.fighters[var_0.playerfocus];

            if ( isdefined( var_2 ) )
            {
                var_0.hudfocusname setplayernamestring( var_2 );
                return;
            }
        }
        else
        {
            var_0.hudfocusname.alpha = 1.0;
            var_0.hudfocusname.label = &"MP/BR_GULAG_FOCUS_BET_CLEAR";
            var_0.hudfocusname settext( &"" );
        }
    }
    else if ( var_0.playerfocus != -1 )
    {
        var_0.hudfocusname.alpha = 1.0;
        var_0.hudfocusname.label = &"MP/BR_GULAG_FOCUS_BET";
        var_2 = self.fighters[var_0.playerfocus];

        if ( isdefined( var_2 ) )
        {
            var_0.hudfocusname setplayernamestring( var_2 );
            return;
        }
    }
    else
    {
        var_0.hudfocusname.alpha = 0.0;
        var_0.hudfocusname.label = &"";
    }
}

cleanupbethud( var_0, var_1 )
{
    var_0 notify( "cleanUpBetHud" );

    if ( isdefined( var_1 ) )
    {
        var_0 endon( "cleanUpBetHud" );
        wait( var_1 );
    }

    var_2 = [ var_0.hudbetonplayer, var_0.hudammount, var_0.hudavailable, var_0.hudfocusname ];

    foreach ( var_4 in var_2 )
    {
        if ( isdefined( var_4 ) )
            var_4 destroy();
    }
}

watchbetbutton( var_0, var_1, var_2, var_3 )
{
    self endon( "end_betting" );

    for (;;)
    {
        var_1 waittill( var_2 );
        var_0 notify( "betPlaced", var_3 );
    }
}

endbetting( var_0, var_1 )
{
    if ( !isdefined( var_1 ) || !istrue( var_1.bettingopen ) )
        return;

    var_1 notify( "end_betting" );
    var_1.bettingopen = 0;

    foreach ( var_3 in var_1.bets )
    {
        var_4 = [ var_3.hudavailable, var_3.hudfocusname ];

        if ( var_3.playerbeton == -1 )
        {
            var_4[var_4.size] = var_3.hudbetonplayer;
            var_4[var_4.size] = var_3.hudammount;
        }

        foreach ( var_6 in var_4 )
        {
            if ( isdefined( var_6 ) )
                var_6 destroy();
        }
    }

    updateoutlines( var_0 );
}

payoutremainingbets( var_0, var_1 )
{
    if ( !isdefined( var_1 ) )
        return;

    var_2 = -1;

    if ( isdefined( var_0 ) )
        var_2 = var_0.fighterindex;

    foreach ( var_4 in var_1.bets )
    {
        if ( var_4.playerbeton != -1 && isdefined( var_4.owner ) && !istrue( var_4.paidout ) )
        {
            var_5 = var_4.playerbeton == var_2;
            _completebet( var_1, var_4, var_5 );
        }

        thread cleanupbethud( var_4, 2.5 );
    }
}

_completebet( var_0, var_1, var_2 )
{
    var_3 = ( 0, 1, 0 );
    var_4 = ( 1, 0, 0 );
    var_1.paidout = 1;

    if ( var_2 )
    {
        var_1.owner scripts\mp\gametypes\br_plunder::playersetplundercount( var_1.owner.plundercount + var_1.ammount );
        var_1.hudbetonplayer.color = var_3;
        var_1.hudbetonplayer.label = &"MP/BR_GULAG_BET_FIGHTER_NAME_WINNER";
        var_1.hudammount.color = var_3;
        var_1.hudammount.label = &"MP/BR_GULAG_BET_AMMOUNT_WON";
    }
    else
    {
        var_1.hudbetonplayer.color = var_4;
        var_1.hudbetonplayer.label = &"MP/BR_GULAG_BET_FIGHTER_NAME_LOSER";
        var_1.hudammount.color = var_4;
        var_1.hudammount.label = &"MP/BR_GULAG_BET_AMMOUNT_LOST";

        if ( isalive( var_1.owner ) )
        {
            var_5 = var_1.owner.plundercount - var_1.ammount;
            var_5 = int( max( 0, var_5 ) );
            var_1.owner scripts\mp\gametypes\br_plunder::playersetplundercount( var_5 );
        }
    }

    var_6 = var_0.fighters[var_1.playerfocus];

    if ( isdefined( var_6 ) )
        var_6 hudoutlinedisableforclient( var_1.owner );
}

getbettingplayers( var_0, var_1 )
{
    var_2 = var_0.jailedplayers;

    if ( level.gulag.betting > 1 )
    {
        foreach ( var_4 in var_1.fighters )
            var_2[var_2.size] = var_4;
    }

    return var_2;
}

payoutbet( var_0, var_1, var_2 )
{
    if ( !isdefined( var_0 ) )
        return;

    foreach ( var_4 in var_0.bets )
    {
        if ( var_4.playerbeton == var_1.fighterindex && isdefined( var_4.owner ) && !istrue( var_4.paidout ) )
        {
            _completebet( var_0, var_4, var_2 );
            thread cleanupbethud( var_4, 2.5 );
        }
    }
}

rock_used( var_0 )
{
    var_1 = self.arena;
    var_0 scripts\engine\utility::waittill_notify_or_timeout( "missile_stuck", 4 );
    wait 2;

    if ( isdefined( var_0 ) )
        var_0 delete();

    if ( level.gulag.usefloorrocks )
        spawnrock( var_1 );
}

spawnrocks( var_0 )
{
    var_0.rocks = getentitylessscriptablearray( var_0.target, "targetname" );

    if ( var_0.rocks.size == 0 || !level.gulag.usefloorrocks )
    {
        for ( var_1 = 0; var_1 < var_0.rocks.size; var_1++ )
        {
            var_2 = var_0.rocks[var_1];
            var_2 setscriptablepartstate( "brloot_rock", "hidden" );
        }

        return;
    }

    var_3 = 20;

    if ( var_0.rocks.size < var_3 )
        var_3 = var_0.rocks.size;

    var_0.rocks = scripts\engine\utility::array_randomize( var_0.rocks );
    var_0.rockcounter = var_3;
    var_4 = getdvarint( "scr_br_fc_rocks", 1 ) == 0;

    for ( var_1 = 0; var_1 < var_0.rocks.size; var_1++ )
    {
        var_2 = var_0.rocks[var_1];
        var_2.arena = var_0;

        if ( var_1 >= var_3 || var_4 )
            var_2 setscriptablepartstate( "brloot_rock", "hidden" );
    }
}

rockused( var_0, var_1, var_2, var_3, var_4 )
{
    if ( !isdefined( var_0 ) || !isdefined( var_3 ) )
        return;

    var_5 = makeweapon( "rock_mp" );

    if ( var_3 hasweapon( var_5 ) && var_3 getammocount( var_5 ) > 0 )
        return;

    var_0 setscriptablepartstate( "brloot_rock", "hidden" );
    var_3 thread scripts\mp\gametypes\br_pickups::playerplaypickupanim();
    var_3 scripts\mp\equipment::giveequipment( "equip_rock", "primary" );
    var_3 playlocalsound( "br_rock_pickup" );
}

spawnrock( var_0 )
{
    if ( istrue( var_0.shutdown ) || var_0.rocks.size == 0 )
        return;

    var_1 = var_0.rocks[var_0.rockcounter];
    var_1 setscriptablepartstate( "brloot_rock", "visible" );
    var_0.rockcounter++;

    if ( var_0.rockcounter >= var_0.rocks.size )
        var_0.rockcounter = 0;
}

playertakeawayrock( var_0 )
{
    var_1 = makeweapon( "rock_mp" );

    if ( self hasweapon( var_1 ) )
    {
        self takeweapon( var_1 );
        self clearaccessory();

        if ( level.gulag.usefloorrocks )
            spawnrock( var_0 );
    }
}

gettangentoncirclefrompoint( var_0, var_1, var_2 )
{
    var_3 = var_2[0] - var_0[0];
    var_4 = var_2[1] - var_0[1];
    var_5 = var_1;
    var_6 = var_3 * var_3;
    var_7 = var_4 * var_4;
    var_8 = var_5 * var_5;
    var_9 = var_6 + var_7 - var_8;
    var_10 = undefined;
    var_11 = 1;

    if ( var_9 > 0 )
    {
        var_12 = ( var_8 * var_3 - var_5 * var_4 * sqrt( var_6 + var_7 - var_8 ) ) / ( var_6 + var_7 );
        var_13 = ( var_8 * var_4 + var_5 * var_3 * sqrt( var_6 + var_7 - var_8 ) ) / ( var_6 + var_7 );
        var_10 = ( var_12, var_13, var_2[2] ) + ( var_0[0], var_0[1], 0 );
    }
    else
    {
        var_14 = vectornormalize( ( var_3, var_4, 0 ) );
        var_15 = var_0 + var_14 * var_1;
        var_10 = ( var_15[0], var_15[1], var_2[2] );
        var_11 = 0;
    }

    return [ var_10, var_11 ];
}

spawnac130()
{
    if ( !istrue( level.gulag.planerespawn ) )
        return;

    level waittill( "prematch_started" );

    if ( !istrue( level.br_infils_disabled ) )
        wait 10;

    var_0 = undefined;
    var_1 = undefined;

    if ( isdefined( level.br_ac130 ) )
        var_0 = level.br_ac130.startpt;
    else
    {
        var_2 = scripts\mp\gametypes\br_c130::createtestc130path();
        var_0 = var_2.startpt;
    }

    if ( isdefined( level.br_circle ) && isdefined( level.br_circle.safecircleent ) )
        var_1 = level.br_circle.safecircleent.origin[2];
    else
        var_1 = level.br_level.br_circleradii[0];

    var_1 = var_1 - 100;
    [var_4, var_5] = gettangentoncirclefrompoint( level.br_level.br_mapcenter, var_1, var_0 );
    var_6 = 0;
    var_7 = 0;

    if ( var_5 )
    {
        var_6 = distance( var_0, var_4 );
        var_7 = var_6 / scripts\mp\gametypes\br_c130::getc130speed();
    }

    var_8 = ( level.br_level.br_mapcenter[0], level.br_level.br_mapcenter[1], var_0[2] );
    level.gulag.ac130linker = spawn( "script_model", var_8 );
    level.gulag.ac130linker setmodel( "tag_origin" );
    level.gulag.ac130linker.radius = var_1;
    level.gulag.ac130 = scripts\mp\gametypes\br_c130::gunship_spawn( var_0, var_4, var_7, 0, ::ac130handlemovement );
    level.gulag.ac130 thread ac130setupanim();
    level.gulag.ac130.riders = [];

    if ( var_7 <= 0 )
    {
        var_9 = var_4 - level.br_level.br_mapcenter;
        var_10 = vectornormalize( ( var_9[0], var_9[1], 0 ) );
        var_11 = vectortoangles( var_10 );
        level.gulag.ac130 unlink();
        level.gulag.ac130.angles = ( 0, var_11[1] + 90, 0 );
        level.gulag.ac130.origin = var_4;
        level.gulag.ac130 thread ac130linkandspin();
    }
}

ac130setupanim()
{
    var_0 = spawnstruct();
    self.animstruct = var_0;
    var_0.movingc130 = self;
    scripts\mp\gametypes\br_infils::spawnplayerpositionparentent( var_0, self );
    scripts\mp\gametypes\br_infils::spawnplayerpositionent( var_0, "j_prop_1" );
    scripts\mp\gametypes\br_infils::playac130infilloopanims( var_0 );
}

ac130handlemovement( var_0, var_1 )
{
    level endon( "game_ended" );
    self endon( "death" );
    self playloopsound( "br_ac130_lp" );

    if ( var_1 > 0 )
    {
        self moveto( var_0, var_1, 0, 0 );
        wait( var_1 );
    }

    thread ac130linkandspin();
}

ac130linkandspin()
{
    self notify( "ac130LinkAndSpin" );
    self endon( "ac130LinkAndSpin" );
    level.gulag.ac130linker endon( "death" );
    var_0 = 6.28318;
    var_1 = scripts\mp\gametypes\br_c130::getc130speed();
    var_2 = level.gulag.ac130linker.radius;
    var_3 = var_0 * var_2 / var_1;
    self linkto( level.gulag.ac130linker, "tag_origin" );

    if ( var_3 <= 0 )
        return;

    for (;;)
    {
        level.gulag.ac130linker rotateyaw( 360, var_3 );
        wait( var_3 );
    }
}

waittillallarenasshutdown()
{
    var_0 = level.gulag.arenas.size;

    if ( !istrue( level.gulag.multiarena ) )
        var_0 = 1;

    for (;;)
    {
        var_1 = 0;

        for ( var_2 = 0; var_2 < var_0; var_2++ )
        {
            var_3 = level.gulag.arenas[var_2];

            if ( !istrue( var_3.shutdown ) )
            {
                var_1 = 1;
                break;
            }
        }

        if ( !var_1 )
            return;

        waitframe();
    }
}

makeac130flyaway()
{
    if ( !istrue( level.gulag.planerespawn ) )
        return;

    var_0 = gettime();
    waittillallarenasshutdown();
    var_1 = gettime();
    wait( getdvarint( "scr_br_fc_respawn_wait", 15 ) );
    waittillframeend;

    foreach ( var_3 in level.gulag.ac130.riders )
    {
        if ( isdefined( var_3 ) )
        {
            var_3.jumptype = "solo";
            var_3 notify( "halo_kick_c130" );
        }
    }

    while ( level.gulag.ac130.riders.size > 0 )
        waitframe();

    var_5 = level.gulag.ac130.origin;
    var_6 = anglestoforward( level.gulag.ac130.angles );
    var_7 = level.br_level.br_circleradii[0] * 2;
    var_8 = var_5 + var_6 * var_7;
    var_8 = var_8 + var_6 * scripts\mp\gametypes\br_c130::getc130speed();
    var_9 = distance( var_5, var_8 );
    var_10 = var_9 / scripts\mp\gametypes\br_c130::getc130speed();
    level.gulag.ac130 notify( "ac130LinkAndSpin" );
    level.gulag.ac130 unlink();
    level.gulag.ac130 moveto( var_8, var_10, 0, 0 );
    wait( var_10 );
    scripts\mp\gametypes\br_public::cleanac130struct( level.gulag.ac130.animstruct );

    if ( isdefined( level.gulag.ac130 ) )
        level.gulag.ac130 delete();

    if ( isdefined( level.gulag.ac130linker ) )
        level.gulag.ac130linker delete();
}

transitioncircle( var_0, var_1 )
{
    if ( !istrue( level.gulag.planerespawn ) )
        return;

    if ( !isdefined( level.gulag.ac130 ) || !isdefined( level.gulag.ac130linker ) )
        return;

    var_2 = ( level.br_circle.safecircleent.origin[0], level.br_circle.safecircleent.origin[1], level.gulag.ac130linker.origin[2] );
    var_0 = var_0 - 100;

    if ( level.gulag.ac130linker.radius != var_0 )
    {
        var_3 = vectornormalize( level.gulag.ac130linker.origin - level.gulag.ac130.origin );
        var_4 = level.gulag.ac130.origin + var_3 * var_0;
        level.gulag.ac130 unlink();
        level.gulag.ac130linker.origin = var_4;
        level.gulag.ac130linker.radius = var_0;
        level.gulag.ac130linker dontinterpolate();
        level.gulag.ac130 thread ac130linkandspin();
    }

    level.gulag.ac130linker moveto( var_2, var_1 );
    wait( var_1 );
}

playersetupac130()
{
    self.infilanimindex = 1;
    self.isjumpmaster = 0;
    scripts\mp\gametypes\br_infils::playerlinktopositionent( level.gulag.ac130.animstruct );
    thread scripts\mp\gametypes\br_infils::playerplayinfilloopanim( level.gulag.ac130.animstruct );
    thread playerputinc130( level.gulag.ac130 );
    scripts\mp\gametypes\br_infils::playersetupcontrolsforinfil( 1 );
    thread playerac130cleanup();
    thread playerautodeployaftertime();
    thread playerspawnprotectionac130();
    level.gulag.ac130.riders[level.gulag.ac130.riders.size] = self;
}

playerputinc130( var_0 )
{
    self.angles = var_0.angles;
    thread listenjump( var_0 );
    thread scripts\mp\gametypes\br_c130::listenkick( var_0, 0 );
    self.c130 = level.gulag.ac130;
    self.br_infil_type = "c130";
    thread scripts\mp\gametypes\br_c130::orbitcam( var_0 );
}

listenjump( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self endon( "cancel_c130" );
    self endon( "br_jump" );
    self.redeployenabled = 0;
    scripts\engine\utility::waittill_either( "halo_jump_c130", "halo_jump_solo_c130" );
    self.jumptype = "solo";
    thread scripts\mp\gametypes\br_c130::leaveplane( var_0, 0, self getplayerangles(), 0 );
}

playerspawnprotectionac130()
{
    self endon( "death_or_disconnect" );
    self.plotarmor = 1;
    scripts\mp\gametypes\br_c130::setplayervarinrespawnc130( 1 );
    waittillplayerdoneskydivingac130( self );
    self.plotarmor = undefined;
    scripts\mp\gametypes\br_c130::setplayervarinrespawnc130( 0 );
}

waittillplayerdoneskydivingac130( var_0 )
{
    var_0 endon( "timeout_gulag_ac130" );
    thread _waittillplayerdoneskydivingac130timeout( var_0 );
    var_0 waittill( "infil_jump_done" );

    while ( !var_0 isparachuting() && !var_0 isonground() )
        waitframe();
}

_waittillplayerdoneskydivingac130timeout( var_0 )
{
    var_0 endon( "death_or_disconnect" );
    var_0 scripts\engine\utility::waittill_any_timeout_2( getdvarint( "scr_br_fc_respawn_wait", 15 ), "halo_kick_c130", "halo_jump_solo_c130" );
    wait 15;
    var_0 notify( "timeout_gulag_ac130" );
}

playerautodeployaftertime()
{
    level endon( "game_ended" );
    self endon( "death" );
    self endon( "disconnect" );
    self endon( "cancel_c130" );
    self endon( "br_jump" );
    wait( getdvarint( "scr_br_fc_respawn_wait", 15 ) );
    self.jumptype = "solo";
    self notify( "halo_kick_c130" );
}

playerac130cleanup()
{
    level endon( "game_ended" );
    scripts\engine\utility::waittill_any_4( "disconnect", "death", "cancel_c130", "infil_jump_done" );

    if ( !isdefined( level.gulag.ac130.riders ) )
        return;

    if ( isdefined( self ) )
        level.gulag.ac130.riders = scripts\engine\utility::array_remove( level.gulag.ac130.riders, self );
    else
        level.gulag.ac130.riders = scripts\engine\utility::array_removeundefined( level.gulag.ac130.riders );
}

playergulagautowin( var_0, var_1, var_2 )
{
    var_3 = self;
    level endon( "game_ended" );
    var_3 endon( "disconnect" );

    if ( istrue( var_3.respawningfromtoken ) )
        return;

    var_4 = var_3;
    var_5 = "token";

    if ( isdefined( var_0 ) )
    {
        var_4 = var_0;
        var_5 = "token_sponsored";
        var_3 scripts\mp\playerlogic::addtoalivecount();
        scripts\mp\gametypes\br::unmarkplayeraseliminated( var_3 );

        if ( istrue( var_1 ) )
            var_6 = 6;
        else
            var_6 = 10;

        var_3 thread triggerrespawnoverlay( var_6 );
    }

    scripts\mp\gametypes\br_analytics::branalytics_gulagstart( var_3, var_5 );
    scripts\mp\gametypes\br_analytics::branalytics_respawn( self, int( isdefined( var_0 ) ) );

    if ( istrue( var_4.hasrespawntoken ) )
        var_4 scripts\mp\gametypes\br_pickups::removerespawntoken();

    var_3.respawningfromtoken = 1;
    var_7 = istrue( var_3.prestreamingspectatetarget );

    if ( var_7 )
    {
        var_3 scripts\mp\utility\lower_message::setlowermessageomnvar( 76 );

        while ( istrue( var_3.prestreamingspectatetarget ) )
            waitframe();
    }

    var_8 = scripts\mp\gametypes\br_public::getdefaultstreamhinttimeoutms() / 1000;
    var_9 = var_3 playergulaggetrespawnpoint( 0, var_8 );
    var_10 = var_3 playerprestreamrespawnorigin( var_9 );
    self.forcespawnorigin = var_10;
    var_11 = scripts\mp\gametypes\br_gametypes::runbrgametypefunc( "playerGulagAutoWinWait", var_0, var_1 );

    if ( !istrue( var_11 ) )
    {
        var_12 = 1.0;
        wait( var_12 );
    }

    if ( var_7 )
        var_3 scripts\mp\utility\lower_message::setlowermessageomnvar( 0 );

    var_13 = 1.0;
    var_3 gulagfadetoblack();
    wait( var_13 );
    var_3 scripts\mp\hud_message::clearsplashqueue();
    var_3 scripts\mp\playerlogic::spawnplayer( undefined, 0 );
    var_3 scripts\cp_mp\execution::_clearexecution();
    var_3 scripts\mp\gametypes\br_pickups::initplayer();
    var_3.respawningfromtoken = undefined;

    if ( !isdefined( var_0 ) && !istrue( var_2 ) )
        var_3 thread triggerrespawnoverlay( 4 );

    var_3 gulagwinnerrespawn( 1, var_5, var_9, 1, var_10, undefined, var_4 );
}

gulaginitloadouts()
{
    level.gulag.jailloadout = creategulagjailloadout();
}

creategulagjailloadout()
{
    var_0 = [];
    var_0["loadoutArchetype"] = "archetype_assault";
    var_0["loadoutPrimary"] = "iw8_fists";
    var_0["loadoutPrimaryAttachment"] = "none";
    var_0["loadoutPrimaryAttachment2"] = "none";
    var_0["loadoutPrimaryCamo"] = "none";
    var_0["loadoutPrimaryReticle"] = "none";
    var_0["loadoutSecondary"] = "none";
    var_0["loadoutSecondaryAttachment"] = "none";
    var_0["loadoutSecondaryAttachment2"] = "none";
    var_0["loadoutSecondaryCamo"] = "none";
    var_0["loadoutSecondaryReticle"] = "none";
    var_0["loadoutMeleeSlot"] = "none";
    var_0["loadoutEquipmentPrimary"] = "equip_rock";
    var_0["loadoutEquipmentSecondary"] = "none";
    var_0["loadoutStreakType"] = "assault";
    var_0["loadoutKillstreak1"] = "none";
    var_0["loadoutKillstreak2"] = "none";
    var_0["loadoutKillstreak3"] = "none";
    var_0["loadoutSuper"] = "super_br_extract";
    var_0["loadoutPerks"] = [ "specialty_null" ];
    var_0["loadoutGesture"] = "playerData";
    var_0["loadoutExecution"] = "none";
    return var_0;
}

playergivearenaloadout( var_0, var_1 )
{
    if ( getdvarint( "scr_br_fc_loadouts", 1 ) == 0 )
        return;

    self.pers["gamemodeLoadout"] = level.prematchloadouts[var_1];
    checkforarenaloadoutoverride( var_1 );
    self.class = "gamemode";
    self.prevweaponobj = undefined;
    var_2 = scripts\mp\class::loadout_getclassstruct();
    var_2 = scripts\mp\class::loadout_updateclass( var_2, "gamemode" );
    scripts\mp\class::preloadandqueueclassstruct( var_2, 1, 1 );
    scripts\mp\class::giveloadout( self.team, "gamemode", 0, 0 );
    self givestartammo( var_2.loadoutprimaryobject );
    self givestartammo( var_2.loadoutsecondaryobject );
    self.gulagloadoutindex = level.prematchloadouts[var_1]["tableColumn"];
    playersetomnvargulag( var_0, "loadoutRow", self.gulagloadoutindex );
}

checkforarenaloadoutoverride( var_0 )
{
    if ( getdvarint( "scr_br_alt_mode_gg", 0 ) )
    {
        switch ( var_0 )
        {
            case 0:
                var_1 = "iw8_pi_decho";
                break;
            case 1:
                var_1 = "iw8_pi_cpapa";
                break;
            case 2:
                var_1 = "iw8_pi_decho";
                break;
            case 3:
                var_1 = "iw8_pi_cpapa";
                break;
            case 4:
                var_1 = "iw8_pi_decho";
                break;
            case 5:
                var_1 = "iw8_pi_cpapa";
                break;
            case 6:
                var_1 = "iw8_pi_decho";
                break;
            case 7:
                var_1 = "iw8_pi_cpapa";
                break;
            case 8:
                var_1 = "iw8_pi_decho";
                break;
            case 9:
            default:
                var_1 = "iw8_pi_cpapa";
        }

        self.pers["gamemodeLoadout"]["loadoutPrimary"] = var_0;
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment2"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment3"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment4"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment5"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondary"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment2"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment3"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment4"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment5"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPerks"] = [ "specialty_null" ];
    }
    else if ( getdvarint( "scr_gulag_tactical_loadout", 1 ) )
    {
        switch ( var_0 )
        {
            case 0:
                var_1 = "iw8_pi_golf21";
                break;
            case 1:
                var_1 = "iw8_pi_mike1911";
                break;
            case 2:
                var_1 = "iw8_pi_cpapa";
                break;
            case 3:
                var_1 = "iw8_pi_decho";
                break;
            case 4:
                var_1 = "iw8_pi_papa320";
                break;
            case 5:
                var_1 = "iw8_sh_dpapa12";
                break;
            case 6:
                var_1 = "iw8_sh_charlie725";
                break;
            case 7:
                var_1 = "iw8_sh_oscar12";
                break;
            case 8:
                var_1 = "iw8_pi_decho";
                break;
            case 9:
            default:
                var_1 = "iw8_pi_cpapa";
        }

        self.pers["gamemodeLoadout"]["loadoutPrimary"] = var_0;
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment2"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment3"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment4"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPrimaryAttachment5"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondary"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment2"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment3"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment4"] = "none";
        self.pers["gamemodeLoadout"]["loadoutSecondaryAttachment5"] = "none";
        self.pers["gamemodeLoadout"]["loadoutPerks"] = [ "specialty_null" ];
    }
}

getloadoutindex()
{
    if ( getdvarint( "scr_br_fc_loadouts", 1 ) == 0 )
        return;

    var_0 = getdvarint( "scr_br_fc_loadoutOverride", -1 );

    if ( var_0 > -1 && var_0 < level.prematchloadouts.size )
        return var_0;

    var_1 = randomint( level.prematchloadouts.size );
    var_2 = getdvarint( "scr_br_gulag_force_shotgun", 0 );

    if ( var_2 )
    {
        var_3 = scripts\mp\utility\weapon::getweaponbasenamescript( level.prematchloadouts[var_1]["loadoutPrimary"] + "_mp" );
        var_4 = weaponclass( var_3 );

        if ( var_4 != "spread" && var_3 != "iw8_sn_crossbow_mp" )
        {
            for ( var_1 = 0; var_1 < level.prematchloadouts.size; var_1++ )
            {
                var_3 = scripts\mp\utility\weapon::getweaponbasenamescript( level.prematchloadouts[var_1]["loadoutPrimary"] + "_mp" );
                var_4 = weaponclass( var_3 );

                if ( var_4 == "spread" || var_3 == "iw8_sn_crossbow_mp" )
                    return var_1;
            }
        }
    }

    return var_1;
}

setupdomendflag( var_0 )
{
    var_1 = getdvarint( "scr_br_fc_flag_radius", 50 );
    var_0.dom = spawnstruct();
    var_2 = getgroundposition( var_0.center, 1 );
    var_0.dom.trigger = spawn( "trigger_radius", var_2, 0, int( var_1 ), int( level.br_domheight ) );
    var_3 = scripts\mp\gametypes\obj_dom::setupobjective( var_0.dom.trigger, "neutral", undefined, 1 );
    var_3.onuse = ::arenaflag_onuse;
    var_3.onbeginuse = ::arenaflag_onusebegin;
    var_3.onenduse = ::arenaflag_onuseend;
    var_3.onuseupdate = ::arenaflag_onuseupdate;
    var_3.oncontested = ::arenaflag_oncontested;
    var_3.isarena = 1;
    var_3 scripts\mp\gameobjects::pinobjiconontriggertouch();
    var_3.id = "domFlag";
    var_3 scripts\mp\gameobjects::setcapturebehavior( "persistent" );
    var_3.scriptable delete();
    var_3.ignorestomp = 1;
    var_3 scripts\mp\gameobjects::requestid( 0, 1, undefined, 0, 0 );
    var_3.visibilitymanuallycontrolled = 1;
    var_3 arenaflag_setenabled( 0 );
    var_3 arenaflag_setvisible( var_0, 0 );
    var_3.arena = var_0;
    var_0.dom.arenaflag = var_3;
}

domgulagsounds( var_0 )
{
    wait 1.0;

    if ( isdefined( var_0 ) && isdefined( var_0.dom ) && isdefined( var_0.dom.arenaflag ) && isdefined( var_0.dom.arenaflag.flagmodel ) )
        return;
}

arenaflag_setenabled( var_0 )
{
    if ( var_0 )
    {
        scripts\mp\gameobjects::allowuse( "any" );
        self.trigger scripts\engine\utility::trigger_on();
    }
    else
    {
        scripts\mp\gameobjects::allowuse( "none" );
        self.trigger scripts\engine\utility::trigger_off();
        scripts\mp\gameobjects::resetcaptureprogress();
    }
}

arenaflag_setvisible( var_0, var_1, var_2, var_3 )
{
    self notify( "arenaFlag_setVisible" );

    if ( var_1 )
    {
        var_4 = "waypoint_captureneutral";

        if ( istrue( var_2 ) )
            var_4 = level.iconovertime;

        thread arenaflag_objectiveshow( var_0 );
        thread scripts\mp\gameobjects::setobjectivestatusicons( var_4 );
        thread scripts\mp\gameobjects::setownerteam( "neutral" );
        thread scripts\mp\gametypes\obj_dom::updateflagstate( "idle", 0, "none" );
        self.flagmodel show();
        thread arenaflag_showflagoutline( var_0, 1 );

        if ( !istrue( var_2 ) )
        {
            setovertimeomnvarenabled( var_0, 1 );
            return;
        }
    }
    else
        thread arenaflag_setvisibledisableinternal( var_0, var_2, var_3 );
}

arenaflag_setvisibledisableinternal( var_0, var_1, var_2 )
{
    self endon( "arenaFlag_setVisible" );

    if ( !istrue( var_2 ) )
    {
        objective_setpinned( self.objidnum, 0 );
        wait 1;
    }

    thread arenaflag_showflagoutline( var_0, 0 );
    thread arenaflag_objectivehide( var_2 );
    thread scripts\mp\gametypes\obj_dom::updateflagstate( "off", 0, "none" );
    self.flagmodel hide( 1 );

    if ( !istrue( var_1 ) )
    {
        setovertimeomnvarenabled( var_0, 0 );
        setovertimeomnvarprogress( var_0, 0 );
    }
}

arenaflag_setvisibleplayer( var_0, var_1 )
{
    var_1 endon( "death_or_disconnect" );

    if ( var_0 )
    {
        var_1 setclientomnvar( "ui_overtime_timer_show", 1 );
        objective_addclienttomask( self.objidnum, var_1 );
    }
    else
    {
        objective_unpinforclient( self.objidnum, var_1 );
        wait 1;
        var_1 setclientomnvar( "ui_overtime_timer_show", 0 );
        objective_removeclientfrommask( self.objidnum, var_1 );
    }

    thread arenaflag_showflagoutlineplayer( var_0, var_1 );
}

arenaflag_previewflag( var_0, var_1 )
{
    thread arenaflag_setvisible( var_0, 1, 1 );
    wait( var_1 );
    thread arenaflag_setvisible( var_0, 0, 1, 1 );
}

arenaflag_objectiveshow( var_0 )
{
    objective_removeallfrommask( self.objidnum );

    foreach ( var_2 in var_0.jailedplayers )
        objective_addclienttomask( self.objidnum, var_2 );

    foreach ( var_2 in var_0.arenaplayers )
        objective_addclienttomask( self.objidnum, var_2 );

    objective_showtoplayersinmask( self.objidnum );
}

arenaflag_objectivehide( var_0 )
{
    objective_setshowprogress( self.objidnum, 0 );
    objective_removeallfrommask( self.objidnum );
    objective_showtoplayersinmask( self.objidnum );
}

arenaflag_showflagoutline( var_0, var_1 )
{
    if ( var_1 )
    {
        if ( var_0.jailedplayers.size > 0 )
            self.flagmodel hudoutlineenableforclients( var_0.jailedplayers, "outline_nodepth_orange" );

        if ( var_0.arenaplayers.size > 0 )
            self.flagmodel hudoutlineenableforclients( var_0.arenaplayers, "outline_nodepth_orange" );
    }
    else
    {
        if ( var_0.jailedplayers.size > 0 )
            self.flagmodel hudoutlinedisableforclients( var_0.jailedplayers );

        if ( var_0.arenaplayers.size > 0 )
            self.flagmodel hudoutlinedisableforclients( var_0.arenaplayers );
    }
}

arenaflag_showflagoutlineplayer( var_0, var_1 )
{
    if ( var_0 )
        self.flagmodel hudoutlineenableforclient( var_1, "outline_nodepth_orange" );
    else
        self.flagmodel hudoutlinedisableforclient( var_1 );
}

arenaflag_onusebegin( var_0 )
{
    var_1 = getdvarint( "scr_br_fc_flag_capture_time", 3.0 );
    var_0.iscapturing = 1;
    var_2 = scripts\mp\gameobjects::getownerteam();

    if ( var_2 == "neutral" )
        var_0 setclientomnvar( "ui_objective_state", 1 );

    self.neutralizing = istrue( level.flagneutralization ) && var_2 != "neutral";

    if ( !istrue( self.neutralized ) )
        self.didstatusnotify = 0;

    var_3 = var_1;
    scripts\mp\gameobjects::setusetime( var_3 );

    if ( istrue( level.capturedecay ) )
        thread scripts\mp\gameobjects::useobjectdecay( var_0.team );

    if ( var_3 > 0 )
    {
        self.prevownerteam = scripts\mp\utility\game::getotherteam( var_0.team )[0];
        scripts\mp\gametypes\obj_dom::updateflagcapturestate( var_0.team );
        scripts\mp\gameobjects::setobjectivestatusicons( "waypoint_taking", "waypoint_losing" );
    }
}

arenaflag_onuseupdate( var_0, var_1, var_2, var_3 )
{
    var_4 = scripts\mp\gameobjects::getownerteam();

    if ( var_1 > 0.05 && var_2 && !self.didstatusnotify )
        self.didstatusnotify = 1;
}

arenaflag_onuseend( var_0, var_1, var_2 )
{
    self.didstatusnotify = 0;

    if ( var_2 )
        scripts\mp\objidpoolmanager::objective_show_progress( self.objidnum, 0 );

    if ( isplayer( var_1 ) )
    {
        var_1.iscapturing = 0;
        var_1 setclientomnvar( "ui_objective_state", 0 );
        var_1.ui_dom_securing = undefined;
    }

    var_3 = scripts\mp\gameobjects::getownerteam();

    if ( var_3 == "neutral" )
    {
        scripts\mp\gameobjects::setobjectivestatusicons( "waypoint_captureneutral" );
        thread scripts\mp\gametypes\obj_dom::updateflagstate( "idle", 0 );
    }
    else
    {
        scripts\mp\gameobjects::setobjectivestatusicons( "waypoint_defend", "waypoint_capture" );
        thread scripts\mp\gametypes\obj_dom::updateflagstate( var_3, 0 );
    }

    if ( !var_2 )
        self.neutralized = 0;
}

arenaflag_setcaptured( var_0, var_1 )
{
    scripts\mp\gameobjects::setownerteam( var_0 );
    self notify( "capture", var_1 );
    self notify( "assault", var_1 );
    scripts\mp\gameobjects::setobjectivestatusicons( "waypoint_defending", "waypoint_capture" );
    self.neutralized = 0;
    thread scripts\mp\gametypes\obj_dom::updateflagstate( var_0, 0, var_0 );

    if ( self.touchlist[var_0].size == 0 && isdefined( self.oldtouchlist ) )
        self.touchlist = self.oldtouchlist;

    foreach ( var_3 in level.teamnamelist )
    {
        if ( isdefined( self.assisttouchlist[var_3] ) && var_3 != var_0 )
            self.assisttouchlist[var_3] = [];
    }
}

arenaflag_onuse( var_0 )
{
    var_1 = var_0.team;
    var_2 = scripts\mp\gameobjects::getownerteam();
    var_3 = scripts\mp\utility\game::getotherteam( var_1 )[0];
    self.capturetime = gettime();
    self.neutralized = 0;
    arenaflag_setcaptured( var_1, var_0 );

    if ( !self.neutralized )
    {
        foreach ( var_5 in self.arena.arenaplayers )
        {
            if ( isalive( var_5 ) && var_5.team != var_1 )
                var_5 thread gulagplayerlost( self.arena );
        }

        thread handleendarena( self.arena );
        self.firstcapture = 0;
    }
}

arenaflag_oncontested()
{
    scripts\mp\gameobjects::setobjectivestatusicons( "waypoint_contested" );
    scripts\mp\objidpoolmanager::objective_set_progress_team( self.objidnum, undefined );
    thread scripts\mp\gametypes\obj_dom::updateflagstate( "contested", 0 );
}

getarenaomnvarbitpackinginfo( var_0, var_1 )
{
    var_2 = 0;
    var_3 = 0;
    var_4 = "";

    switch ( var_0 )
    {
        case "playerArena0":
            [var_2, var_3, var_4] = [ 0, 8, "ui_br_gulag_players_1" ];
            var_1 = var_1 + 1;
            break;
        case "playerArena1":
            [var_2, var_3, var_4] = [ 8, 8, "ui_br_gulag_players_1" ];
            var_1 = var_1 + 1;
            break;
        case "playerJail0":
            [var_2, var_3, var_4] = [ 16, 8, "ui_br_gulag_players_1" ];
            var_1 = var_1 + 1;
            break;
        case "playerJail1":
            [var_2, var_3, var_4] = [ 24, 8, "ui_br_gulag_players_1" ];
            var_1 = var_1 + 1;
            break;
        case "loadoutRow":
            [var_2, var_3, var_4] = [ 0, 8, "ui_br_gulag_data" ];
            var_1 = var_1 + 1;
            break;
        case "playerHealth0":
            [var_2, var_3, var_4] = [ 8, 8, "ui_br_gulag_data" ];
            break;
        case "playerHealth1":
            [var_2, var_3, var_4] = [ 16, 8, "ui_br_gulag_data" ];
            break;
        default:
            break;
    }

    return [ var_2, var_3, var_4, var_1 ];
}

packarenaomnvardata( var_0, var_1, var_2, var_3, var_4 )
{
    var_5 = int( pow( 2, var_4 ) ) - 1;
    var_6 = ( var_2 & var_5 ) << var_3;
    var_7 = ~( var_5 << var_3 );
    var_8 = var_0.omnvardata[var_1];
    var_9 = var_8 & var_7;
    var_10 = var_9 + var_6;
    var_0.omnvardata[var_1] = var_10;
}

setarenaomnvar( var_0, var_1, var_2 )
{
    [var_4, var_5, var_6, var_2] = getarenaomnvarbitpackinginfo( var_1, var_2 );

    if ( var_6 == "" )
        return;

    packarenaomnvardata( var_0, var_6, var_2, var_4, var_5 );
}

setarenaomnvarplayertype( var_0, var_1, var_2 )
{
    var_3 = -1;

    if ( isdefined( var_2 ) )
        var_3 = var_2 getentitynumber();

    setarenaomnvar( var_0, var_1, var_3 );
}

setarenaomnvarhealthtype( var_0, var_1, var_2 )
{
    var_3 = 0;

    if ( isdefined( var_2 ) )
        var_3 = var_2.health;

    setarenaomnvar( var_0, var_1, var_3 );
}

updatearenaomnvarplayers( var_0 )
{
    var_0.omnvardata["ui_br_gulag_players_1"] = 0;

    for ( var_1 = 0; var_1 < level.gulag.maxplayers; var_1++ )
    {
        var_2 = var_0.arenaplayers[var_1];
        setarenaomnvarplayertype( var_0, "playerArena" + var_1, var_2 );
    }

    var_3 = var_0.matches[0];

    if ( !isdefined( var_3 ) )
        var_3 = [];

    setarenaomnvarplayertype( var_0, "playerJail0", var_3[0] );
    setarenaomnvarplayertype( var_0, "playerJail1", var_3[1] );
    var_4 = scripts\engine\utility::array_combine( var_0.jailedplayers, var_0.arenaplayers );

    foreach ( var_2 in var_4 )
    {
        var_2 setclientomnvar( "ui_br_gulag_players_1", var_0.omnvardata["ui_br_gulag_players_1"] );

        foreach ( var_11, var_7 in var_0.matches )
        {
            if ( isdefined( var_7[0] ) && var_2 == var_7[0] || isdefined( var_7[1] ) && var_2 == var_7[1] )
            {
                var_8 = !isplayermatched( var_0, var_2 );
                var_9 = var_11 + 1;
                var_10 = var_8 + ( var_9 << 1 );
                var_2 setclientomnvar( "ui_br_gulag_queue_position", var_10 );
                break;
            }
        }
    }
}

playersetgulagdataomnvar( var_0 )
{
    self setclientomnvar( "ui_br_gulag_data", var_0.omnvardata["ui_br_gulag_data"] );
}

updatearenaomnvardata( var_0 )
{
    resetarenaomnvardata( var_0 );

    for ( var_1 = 0; var_1 < level.gulag.maxplayers; var_1++ )
    {
        var_2 = var_0.arenaplayers[var_1];
        setarenaomnvarhealthtype( var_0, "playerHealth" + var_1, var_2 );
    }

    foreach ( var_2 in var_0.jailedplayers )
        var_2 playersetgulagdataomnvar( var_0 );

    foreach ( var_2 in var_0.arenaplayers )
        var_2 playersetarenaomnvarwithloadout( var_0 );
}

resetarenaomnvardata( var_0 )
{
    var_0.omnvardata["ui_br_gulag_data"] = 0;
}

playersetarenaomnvarwithloadout( var_0 )
{
    if ( !isdefined( self.gulagloadoutindex ) )
        self setclientomnvar( "ui_br_gulag_data", var_0.omnvardata["ui_br_gulag_data"] );
    else
        playersetomnvargulag( var_0, "loadoutRow", self.gulagloadoutindex );
}

playersetomnvargulag( var_0, var_1, var_2 )
{
    [var_4, var_5, var_6, var_2] = getarenaomnvarbitpackinginfo( var_1, var_2 );

    if ( var_6 == "" )
        return;

    playerpackdataintogulagomnvar( var_0, var_6, var_2, var_4, var_5 );
}

playerpackdataintogulagomnvar( var_0, var_1, var_2, var_3, var_4 )
{
    var_5 = int( pow( 2, var_4 ) ) - 1;
    var_6 = ( var_2 & var_5 ) << var_3;
    var_7 = ~( var_5 << var_3 );
    var_8 = var_0.omnvardata[var_1];
    var_9 = var_8 & var_7;
    var_10 = var_9 + var_6;
    self setclientomnvar( var_1, var_10 );
}

updateplayeromnvarsallmatches( var_0 )
{
    updatearenaomnvarplayers( var_0 );
    updatearenaomnvardata( var_0 );
}

playercleargulagomnvars( var_0 )
{
    var_1 = getarraykeys( var_0.omnvardata );

    foreach ( var_3 in var_1 )
        self setclientomnvar( var_3, 0 );
}

setovertimeomnvarprogress( var_0, var_1 )
{
    foreach ( var_3 in var_0.arenaplayers )
        var_3 setclientomnvar( "ui_overtime_timer", var_1 );

    foreach ( var_3 in var_0.jailedplayers )
        var_3 setclientomnvar( "ui_overtime_timer", var_1 );
}

setovertimeomnvarenabled( var_0, var_1 )
{
    foreach ( var_3 in var_0.arenaplayers )
        var_3 setclientomnvar( "ui_overtime_timer_show", var_1 );

    foreach ( var_3 in var_0.jailedplayers )
        var_3 setclientomnvar( "ui_overtime_timer_show", var_1 );
}

playerhealthomnvarwatcher( var_0 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self endon( "gulag_end" );
    var_1 = "playerHealth0";

    if ( var_0.arenaplayers[0] != self )
        var_1 = "playerHealth1";

    for (;;)
    {
        updatearenaomnvardata( var_0 );

        if ( self.health <= 0 )
            return;

        scripts\engine\utility::waittill_any_7( "damage", "force_regeneration", "removeAdrenaline", "healed", "healhRegenThink", "vampirism", "spawned_player" );
    }
}

playerpreloadintrocinematic()
{
    if ( level.gulag.introcinematic && !isbot( self ) )
        self playcinematicforplayerlooping( "mp_donetsk_gulag_intro" );
}

playerplayintrocinematic()
{
    self.gulag_intro_vo = undefined;

    if ( scripts\mp\utility\player::isfemale() )
        self.gulag_intro_vo = "br_gulag_intro_vo_plr_female";
    else
        self.gulag_intro_vo = "br_gulag_intro_vo_plr_male";

    if ( level.gulag.introcinematic && !isbot( self ) )
    {
        if ( isdefined( self.gulag_intro_vo ) )
            self playlocalsound( self.gulag_intro_vo );

        self setclientomnvar( "ui_br_bink_overlay_state", 1 );
        self setplayermusicstate( "br_gulag_intro" );

        if ( scripts\mp\gametypes\br_public::isbotpracticematch() )
            self setsoundsubmix( "iw8_br_gulag_tutorial", 0.5 );
        else
            self setsoundsubmix( "fade_to_black_all_except_music_and_scripted5", 0.5 );

        self copyloadoutbuttonpressed( "mp_donetsk_gulag_intro" );
        self.playinggulagbink = 1;
    }
    else
        gulagloadingtext();
}

playershowskippromptcinematic()
{
    self setclientomnvar( "ui_br_bink_overlay_state", 2 );
}

playerwaittillcinematiccomplete( var_0 )
{
    if ( level.gulag.introcinematic && !isbot( self ) && !self scriptablegetmidpoint() )
    {
        self freezecontrols( 1 );
        _playerwaittillcinematiccompleteinternal( var_0 );
        self freezecontrols( 0 );
        self setclientomnvar( "ui_br_bink_overlay_state", 5 );
        self preloadcinematicforplayer();
        self.playinggulagbink = undefined;

        if ( isdefined( self.gulag_intro_vo ) )
            self stoplocalsound( self.gulag_intro_vo );
    }
}

_playerwaittillcinematiccompleteinternal( var_0 )
{
    var_1 = 17000;
    self endon( "bink_complete" );

    while ( gettime() - var_0 < var_1 && !self crouchbuttonpressed() && !self usebuttonpressed() && !self jumpbuttonpressed() )
        waitframe();
}

playercinematiccompletecallback( var_0, var_1 )
{
    if ( var_0 == "bink_complete" )
        self notify( "bink_complete" );
}

playeriscinematiclayeron()
{
    var_0 = self skydive_cutautodeployoff( "ui_br_bink_overlay_state" );

    if ( var_0 != 0 && var_0 != 6 )
        return 1;

    return 0;
}

playeriscinematicblacklayeron()
{
    var_0 = self skydive_cutautodeployoff( "ui_br_bink_overlay_state" );
    return var_0 == 7;
}

playercinematicfadein()
{
    self setclientomnvar( "ui_br_bink_overlay_state", 7 );
}

playercinematicfadeout()
{
    self endon( "disconnect" );

    if ( playeriscinematiclayeron() )
    {
        self setclientomnvar( "ui_br_bink_overlay_state", 6 );
        wait 1;
        self setclientomnvar( "ui_br_bink_overlay_state", 0 );
    }
}

setupcellspawn( var_0, var_1 )
{
    if ( !level.gulag.usecellspawns )
        return;

    var_2 = getentarray( var_1.target, "targetname" );

    if ( var_2.size > 1 )
        level.gulag.usecellspawns = 0;
    else
    {
        var_1.door = getent( var_1.target, "targetname" );

        if ( isdefined( var_1.door ) )
        {
            var_1.door.closed = 1;
            var_3 = getent( var_1.door.target, "targetname" );
            var_3 delete();
            var_4 = anglestoforward( var_1.door.angles );
            var_1.door.closepos = var_1.door.origin;
            var_1.door.openpos = var_1.door.origin + var_4 * 60;
        }
    }
}

usecellspawns( var_0 )
{
    return level.gulag.usecellspawns && var_0.cellspawns.size > 0;
}

resetplayermovespeedscale()
{
    var_0 = self;
    var_0.fastcrouchspeedmod = 0;
    var_0 scripts\mp\weapons::updatemovespeedscale();
}
