// IW8 DBG GSC SOURCE
// Generated by https://github.com/xensik/gsc-tool

init()
{
    scripts\mp\gametypes\br_gametypes::enablefeature( "circle" );
    scripts\mp\gametypes\br_gametypes::disablefeature( "gulag" );
    scripts\mp\gametypes\br_gametypes::disablefeature( "dropbag" );

    if ( getdvarint( "scr_bmo_use_spawn_fix", 1 ) == 0 )
        scripts\mp\gametypes\br_gametypes::disablefeature( "teamSpectate" );

    scripts\mp\gametypes\br_gametypes::enablefeature( "allowLateJoiners" );
    scripts\mp\gametypes\br_gametypes::disablefeature( "oneLife" );

    if ( getdvarint( "scr_bmo_useKiosks", 1 ) == 0 )
        scripts\mp\gametypes\br_gametypes::disablefeature( "kiosk" );

    if ( getdvarint( "scr_bmo_enableTabletReplace", 1 ) == 1 )
        scripts\mp\gametypes\br_gametypes::enablefeature( "tabletReplace" );

    scripts\mp\gametypes\br_gametypes::registerbrgametypefunc( "playerDropPlunderOnDeath", ::playerdropplunderondeath );
    scripts\mp\gametypes\br_gametypes::registerbrgametypefunc( "playerShouldRespawn", ::playershouldrespawn );
    scripts\mp\gametypes\br_gametypes::registerbrgametypefunc( "playerWelcomeSplashes", ::playerwelcomesplashes );
    scripts\mp\gametypes\br_gametypes::registerbrgametypefunc( "spawnHandled", ::spawnhandled );
    level.teamplunderscoremechanic = getdvarint( "scr_dmz_teamPlunder", 0 );

    if ( level.teamplunderscoremechanic )
    {

    }

    level.brgametype.maxextractions = getdvarint( "scr_br_extract_max_extractions", 5 );
    level.brgametype.numextractions = 0;
    level.brgametype.firstteam = "tie";
    level.br_prematchffa = 0;
    level.br_allowloadout = 1;
    level.topteamsever = [];
    level.lootleaders = [];
    level.expiredlootleaders = [];
    level.called50percentprogress = 0;
    level.called75percentprogress = 0;
    level.called90percentprogress = 0;
    var_0 = getdvarint( "scr_dmz_win_cost", 1000 );

    if ( var_0 > 0 )
    {
        var_0 = var_0 * 10;
        var_0 = var_0 * 100;
        setdvar( "scr_br_scorelimit", var_0 );
        scripts\mp\utility\game::registerscorelimitdvar( scripts\mp\utility\game::getgametype(), var_0 );
    }

    setdvar( "scr_br_magcount", 3 );
    setdvar( "scr_br_loadout_option", "nothing" );
    setdvarifuninitialized( "scr_br_respawnMaxTeammateOffset", 10000 );
    setomnvar( "ui_br_circle_state", 4 );
    setomnvar( "ui_gulag_state", 1 );
    setomnvar( "ui_hide_redeploy_timer", 1 );
    setdvarifuninitialized( "scr_br_bank_alarm", 1 );
    level.giveloadouteverytime = getdvarint( "scr_dmz_giveLoadoutEveryTime", 1 );
    level.instantbleedoutsquadwipe = getdvarint( "scr_bmo_instantBleedOutSquadWipe", 1 );
    level.bottompercentagetoadjusteconomy = getdvarint( "scr_bmo_bottomPercentageToAdjustEconomy", 50 );
    level.toppercentagetoadjusteconomy = getdvarint( "scr_bmo_topPercentageToAdjustEconomy", 5 );
    level.usemilestonephases = getdvarint( "scr_bmo_useMilestonePhases", 1 );
    level.milestonephasepercent_vips = getdvarint( "scr_bmo_milestonePhase_VIPs", 10 );
    level.milestonephasepercent_lzs = getdvarint( "scr_bmo_milestonePhase_LZs", 30 );
    level.milestonephasepercent_drops = getdvarint( "scr_bmo_milestonePhase_Drops", 50 );
    level.milestonephasepercent_helis = getdvarint( "scr_bmo_milestonePhase_Helis", 75 );
    level.airdropbasecashamount = getdvarint( "scr_dmz_airdrop_base_cash_amount", 750 );
    level.hideplacementuntilpercent = getdvarint( "scr_bmo_hidePlacementUntilPercent", 10 );
    level.hideleaderhashuntilpercent = getdvarint( "scr_bmo_hideLeaderHashUntilPercent", 0 );
    level.showplacementsplashesandmusic = getdvarint( "scr_bmo_progressSplashesAndMusic", 1 );
    level.secondsbeforeplacementupdates = getdvarint( "scr_bmo_secondsBeforePlacementUpdates", 60 );
    level.disablepercentageannouncements = getdvarint( "scr_bmo_disable_perc_announcements", 0 );
    level.lootleadermarkcount = getdvarint( "scr_dmz_loot_leader_mark_count", 3 );
    level.maxlootleadermarkcount = getdvarint( "scr_dmz_loot_leader_mark_count", 3 );
    level.lootleadermarksize = getdvarint( "scr_bmo_loot_leader_mark_size", 5000 );
    level.lootleaderoneperteam = getdvarint( "scr_dmz_loot_leader_one_per_team", 1 ) == 1;
    level.lootleadermarksizedynamic = getdvarint( "scr_bmo_loot_leader_mark_size_dynamic", 1 );
    level.lootleadermarkstrongsize = getdvarint( "scr_bmo_loot_leader_mark_info_strong_size", 2000 );
    level.lootleadermarkstrongvalue = getdvarint( "scr_bmo_loot_leader_mark_info_strong_value", 5000 );
    level.lootleadermarkweaksize = getdvarint( "scr_bmo_loot_leader_mark_info_weak_size", 5000 );
    level.lootleadermarkweakvalue = getdvarint( "scr_bmo_loot_leader_mark_info_weak_value", 1000 );
    level.expiredlootleaderenabled = getdvarint( "scr_bmo_loot_leader_expired_enabled", 0 ) == 1;
    level.lootleadermarksontopteams = getdvarint( "scr_bmo_loot_leader_mark_top_teams", 2 );
    level.plundermusicfirst = getdvarfloat( "scr_bmo_music_first", 0.3 );
    level.plundermusicsecond = getdvarfloat( "scr_bmo_music_second", 0.5 );
    level.plundermusicthird = getdvarfloat( "scr_bmo_music_third", 0.75 );
    level.plundermusicfourth = getdvarfloat( "scr_bmo_music_fourth", 0.9 );
    level.useautorespawn = getdvarint( "scr_dmz_useAutoRespawn", 1 );
    level.autorespawnwaittime = getdvarint( "scr_dmz_autoRespawnWaitTime", 20 );
    level.tokenrespawnwaittime = getdvarint( "scr_dmz_tokenRespawnWaitTime", level.autorespawnwaittime );
    level.incrementalrespawnpunish = getdvarint( "scr_dmz_respawn_penalty", 0 );
    level.incrementalrespawnpunishmax = getdvarfloat( "scr_dmz_respawn_penalty_max", 15 );
    level.respawnheightoverride = getdvarint( "scr_bmo_respawnHeightOverride", 5000 );
    level.respawntimedisable = getdvarint( "scr_dmz_respawn_time_disable", 0 );
    level.parachutedeploydelay = getdvarfloat( "scr_bmo_parachuteDeployDelay", 0.5 );
    level.bonusdeathplunder = getdvarint( "scr_dmz_bonusDeathPlunder", 200 );
    level.bonusdeathplunderot = getdvarint( "scr_dmz_bonusDeathPlunder_ot", 250 );
    level.maxplunderdropondeath = getdvarint( "scr_bmo_maxPlunderDropOnDeath", 10000 );
    level.minplunderdropondeath = getdvarint( "scr_bmo_minPlunderDropOnDeath", 200 );
    level.percentageplunderdrop = getdvarint( "scr_bmo_percentagePlunderDrop", 50 );
    level.plunderfxondropthreashold = getdvarint( "scr_bmo_plunderFXOnDropThreashold", 750 );
    level.maxplunderdropinovertime = getdvarint( "scr_bmo_maxPlunderDropInOvertime", 2000 );
    level.executioncashmultiplier = getdvarfloat( "scr_bmo_executionCashMultiplier", 1 );
    level.autoassignfirstquest = getdvarint( "scr_dmz_autoAssignFirstQuest", 0 );
    level.quest_domdistmin = getdvarint( "scr_bmo_questDomDistMin", 5000 );
    level.quest_domdistmax = getdvarint( "scr_bmo_questDomDistMax", 10000 );
    level.quest_assdistmin = getdvarint( "scr_bmo_questAssDistMin", 2500 );
    level.quest_assdistmax = getdvarint( "scr_bmo_questAssDistMax", 7500 );
    level.quest_scavdistmin = getdvarint( "scr_bmo_questScavDistMin", 5000 );
    level.quest_scavdistmax = getdvarint( "scr_bmo_questScavDistMax", 8000 );
    level.tabletreplacefrequency = getdvarfloat( "scr_bmo_questTabletReplaceEveryN", 1.5 );
    level.skipweapondropondeath = getdvarint( "scr_bmo_skipWeaponDropOnDeath", 0 );
    level.skipequipmentdropondeath = getdvarint( "scr_bmo_skipEquipmentDropOnDeath", 1 );
    level.forcearmordropondeath = getdvarint( "scr_bmo_forceArmorDropOnDeath", 1 );
    level.allowfultondropondeath = getdvarint( "scr_bmo_allowFultonDropOnDeath", 1 );
    level.teamplunderexfil = getdvarint( "scr_bmo_score_exfil", 0 ) == 1;
    level.teamplunderexfilshowviponly = getdvarint( "scr_bmo_exfil_showvipteamonly", 0 ) == 1;
    level.teamplunderexfilvipuav = getdvarint( "scr_bmo_vipteam_uav", 0 ) == 1;
    level.teamplunderexfiltimer = getdvarint( "scr_bmo_exfil_timer", 180 );
    level.showplunderextracticonsinworld = getdvarint( "scr_bmo_plunderextract_objicon_inworld", 1 ) == 1;
    level.shownonscriptableextracticons = getdvarint( "scr_bmo_extract_objicon_nonscriptable", 0 );
    level.timeoutplunderextractionsites = getdvarint( "scr_bmo_timeout_plunderextract", 0 );
    level.moveplunderextractionsitesonuse = getdvarint( "scr_bmo_move_plunderextract_onuse", 0 ) == 1;
    level.extractplunderhelihealth = int( getdvarint( "scr_bmo_extract_heli_health", 999 ) * 100 );
    level.extractplunderheliinvulnerable = getdvarint( "scr_bmo_extract_heli_invulnerable", 1 );
    level.bankplunderextractinstantly = getdvarint( "scr_bmo_extract_plunder_instant", 1 );
    level.plunderextractalertnearby = getdvarint( "scr_bmo_plunder_extract_alert", 1 );
    level.matchstartextractsitedelay = getdvarint( "scr_bmo_matchstart_extractsitedelay", 120 );
    level.hideallunselectedextractpads = getdvarint( "scr_bmo_plunderextract_hide_unused", 1 );
    level.setupevenlydistributedpads = scripts\engine\utility::ter_op( getdvar( "mapname" ) == "mp_br_mechanics", 0, getdvarint( "scr_bmo_plunderextract_distribution", 1 ) );
    level.scorerequiresbanking = getdvarint( "scr_bmo_score_requires_banking", 0 );
    level.disablewinonscore = getdvarint( "scr_bmo_disable_win_on_score", 0 );
    level.doendofmatchotsequence = getdvarint( "scr_bmo_eom_ot_timer", 30 );
    level.setallclientomnvarot = level.doendofmatchotsequence > 0;
    level.overtimebuiltintomatchtimer = getdvarint( "scr_bmo_ot_as_match_time", 1 );
    level.bankingoverlimitwillendot = getdvarint( "scr_bmo_eom_bank_to_end", 0 );
    level.mapedgeextractionlocs = getdvarint( "scr_dmz_mapEdgeExtractionLocs", 0 );
    level.modespawnendofgame = ::playerspawnendofgame;
    level.endmatchcameratransitions = getdvarint( "scr_bmo_endMatchCameraTransitions", 1 );
    level.overtimecashmultiplier = getdvarfloat( "scr_bmo_overtimeCashMultiplier", 2 );
    setomnvar( "ui_br_overtime_cash_multiplier", level.overtimecashmultiplier );
    level.disableonemilannounce = getdvarint( "scr_bmo_disable_one_mil_announce", 0 );
    level.dmzlootleaderupdateonpickup = getdvarint( "scr_dmz_loot_leader_update_on_pickup", 0 ) == 1;
    level.dmzwincost = getdvarint( "scr_dmz_win_cost", 1000 ) * 1000;
    level.dmzextractcost = getdvarint( "scr_br_extract_cost", 300 );
    level.dmzminextractcost = getdvarint( "scr_br_extract_cost_min", 40 );
    level.dmzextractcostdecrease = getdvarint( "scr_br_extract_cost_decrease", 20 );
    level.minplunderextractions = getdvarint( "br_min_plunder_extractions", 7 );
    level.maxplunderextractions = getdvarint( "br_max_plunder_extractions", 7 );
    level.brdisablefinalkillcam = 1;
    thread scripts\mp\gametypes\br_lootchopper::init();
    thread scripts\mp\gametypes\br_c130airdrop::init();
    thread initpostmain();
    thread initdialog();
}

initpostmain()
{
    waittillframeend;
    level.uavsettings["uav"].timeout = 60;
    scripts\mp\flags::gameflaginit( "collect_done", 0 );
    scripts\mp\flags::gameflaginit( "helipad_wait_done", 0 );
    scripts\mp\flags::gameflaginit( "placement_updates_allowed", 0 );
    scripts\mp\flags::gameflaginit( "activate_cash_lzs", 0 );
    scripts\mp\flags::gameflaginit( "activate_cash_drops", 0 );
    scripts\mp\flags::gameflaginit( "activate_cash_helis", 0 );
    level.modemayconsiderplayerdead = ::brmayconsiderplayerdead;
    level.brmodevariantrewardcullfunc = ::missions_clearinappropriaterewards;
    level.ononeleftevent = ::ononeleftevent;
    level.onplayerkilled = ::onplayerkilled;
    level.onpostkillcamcallback = ::onpostkillcamcallback;
    level.ontimelimit = ::ontimelimit;
    scripts\mp\utility\spawn_event_aggregator::registeronplayerspawncallback( ::playerlootleadervalidity );
    scripts\mp\utility\disconnect_event_aggregator::registerondisconnecteventcallback( ::onplayerdisconnect );
    thread setupmission();
    thread scripts\mp\gametypes\br_lootchopper::lootchopper_managespawns();
    thread scripts\mp\gametypes\br_c130airdrop::c130airdrop_managedrop();
    level.lootleadermarks = [];
    level.expiredlootleadermarks = [];

    for ( var_0 = 0; var_0 < level.lootleadermarkcount; var_0++ )
    {
        if ( level.lootleadermarksontopteams > 0 && var_0 == 0 )
        {
            var_1 = 1;
            var_2 = 5;
        }
        else
        {
            var_1 = 1;
            var_2 = 4;
        }

        var_3 = spawnstruct();
        var_3.modifier = "";
        var_3 scripts\mp\gametypes\br_quest_util::createquestcircle( var_1, var_2 );
        var_3 scripts\mp\gametypes\br_quest_util::setmapcirclesize( level.lootleadermarksize );
        level.lootleadermarks[var_0] = var_3;
        var_3 = spawnstruct();
        var_3.modifier = "";
        var_3 scripts\mp\gametypes\br_quest_util::createquestcircle( var_1, var_2, 2 );
        var_3 scripts\mp\gametypes\br_quest_util::setmapcirclesize( level.lootleadermarksize );
        level.expiredlootleadermarks[var_0] = var_3;
    }

    if ( !istrue( level.usemilestonephases ) )
        level thread placementupdatewait();

    thread waitandstartplunderpolling();
    level thread scripts\mp\gametypes\br_analytics::initcashtracking();
    cleanupents();

    if ( level.teamplunderscoremechanic )
    {
        if ( getdvarfloat( "scr_dmz_loot_leader_update_interval", 15.0 ) > 0.0 )
            level thread updatelootleadersonfixedinterval();
    }

    if ( level.showplunderextracticonsinworld && level.shownonscriptableextracticons )
        level thread createheliextractobjectiveicons();

    if ( level.timeoutplunderextractionsites > 0 )
    {
        scripts\mp\flags::gameflagwait( "prematch_done" );

        if ( level.matchstartextractsitedelay > 0 )
            wait( level.matchstartextractsitedelay );

        level thread startplunderextractiontimers();
    }

    if ( istrue( level.mapedgeextractionlocs ) )
        level thread initextractionlocations();

    level.validautoassignquests = [];
    level.validautoassignquests[0] = "assassination";
    level.validautoassignquests[1] = "domination";
    level.validautoassignquests[2] = "scavenger";
    level thread eomawardplayerxp();

    if ( istrue( level.endmatchcameratransitions ) )
        level thread initendcameralocations();

    var_4 = getdvarint( "scr_bmo_c130OverrideSpeed", -1 );

    if ( var_4 > 0 )
        level.br_level.c130_speedoverride = var_4;

    level thread changecirclestateatlowtime();

    if ( level.doendofmatchotsequence && level.overtimebuiltintomatchtimer )
        level thread changetimertoovertimetimer();
}

changecirclestateatlowtime()
{
    level endon( "game_ended" );

    if ( scripts\mp\gametypes\br_public::isdmztutorial() )
        return;

    level endon( "end_circlestate_timer" );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    var_0 = getdvarint( "scr_br_timelimit" );
    setomnvar( "ui_br_circle_state", 5 );
    setomnvar( "ui_hardpoint_timer", gettime() + int( var_0 * 1000 ) );
    wait( int( max( var_0 - 300, 0 ) ) );
    setomnvar( "ui_br_circle_state", 6 );
}

changetimertoovertimetimer()
{
    level endon( "game_ended" );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    var_0 = getdvarint( "scr_br_timelimit" );
    var_1 = int( max( var_0 - level.doendofmatchotsequence, 0 ) );

    if ( var_1 <= 0 )
        return;

    wait( var_1 );
    level notify( "end_circlestate_timer" );
    level thread triggerovertimetimer( undefined, undefined, 1 );
}

waitandstartparachuteoverheadmonitoring()
{
    level endon( "game_ended" );
    level.activeparachutersfactionvo = [];
    level.activeparachutersfactionvo["uktl"] = "dx_bra_uktl_respawning_enemy_in_area";
    level.activeparachutersfactionvo["rutl"] = "dx_bra_rutl_respawning_enemy_in_area";
    level.parachuteoverheadwarningtimeoutms = getdvarint( "scr_parachute_overhead_warning_timeout_ms", 45000 );
    level.parachuteoverheadwarningprematchtimeoutms = getdvarint( "scr_parachute_overhead_warning_prematch_timeout_ms", 20000 );
    level.parachuteoverheadwarningradius = getdvarint( "scr_parachute_overhead_warning_radius", 2000 );
    level.parachuteoverheadwarningheight = getdvarint( "scr_parachute_overhead_warning_height", 3000 );
    level thread watchparachutersoverhead( level.parachuteoverheadwarningprematchtimeoutms );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    level notify( "cancel_watch_parachuters_overhead" );
    waitframe();
    level thread watchparachutersoverhead( level.parachuteoverheadwarningtimeoutms );
}

waitandstartplunderpolling()
{
    level endon( "game_ended" );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    thread plunderrankupdate();
}

cleanupents()
{
    scripts\cp_mp\utility\game_utility::removematchingents_bykey( "delete_on_load", "targetname" );
    scripts\cp_mp\utility\game_utility::removematchingents_bymodel( "door_prison_cell_metal_mp", 1 );
    scripts\cp_mp\utility\game_utility::removematchingents_bymodel( "door_wooden_panel_mp_01", 1 );
    scripts\cp_mp\utility\game_utility::removematchingents_bymodel( "me_electrical_box_street_01", 1 );
}

onplayerdisconnect( var_0 )
{
    if ( isdefined( var_0 ) && scripts\mp\flags::gameflag( "prematch_done" ) )
    {
        var_0 scripts\mp\gametypes\br_pickups::droponplayerdeath();

        if ( isdefined( level.dmz_numteamswithplayers ) )
        {
            if ( level.teamdata[var_0.team]["players"].size == 0 )
                level.dmz_numteamswithplayers--;
        }
        else
            calculatenumteamswithplayers();
    }
}

ononeleftevent( var_0 )
{

}

playerwelcomesplashes( var_0 )
{
    self endon( "disconnect" );
    self waittill( "spawned_player" );
    wait 1;
    scripts\mp\hud_message::showsplash( "br_prematch_welcome_dmz" );

    if ( !istrue( level.br_infils_disabled ) )
        self waittill( "joining_Infil" );
    else
        scripts\mp\flags::gameflagwait( "prematch_done" );

    wait 1;
    level thread calculatenumteamswithplayers();

    if ( soundexists( "dx_bra_uktl_bm_tut_earn_cash" ) )
        level scripts\mp\gametypes\br_public::brleaderdialogplayer( "bm_tut_earn_cash", self );

    scripts\mp\hud_message::showsplash( "br_gametype_dmz_welcome" );

    if ( istrue( self.isbrsquadleader ) )
        level scripts\mp\gametypes\br_public::brleaderdialogplayer( "deploy_squad_leader", self, 1, 0, 4.5 );

    if ( istrue( level.autoassignfirstquest ) && istrue( level.br_prematchstarted ) )
        autoassignquest( self );
}

spawnhandled( var_0 )
{
    return istrue( var_0.br_infilstarted ) && scripts\mp\flags::gameflag( "prematch_done" );
}

getdropbagdelay()
{

}

trackserverend()
{
    level endon( "game_ended" );
    scripts\mp\flags::gameflagwait( "prematch_done" );
    var_0 = 0;
    var_1 = 0;
    var_2 = 0;

    if ( scripts\mp\gametypes\br_public::isdmztutorial() )
        return;

    for (;;)
    {
        wait 1;
        var_3 = scripts\mp\gamelogic::gettimeremaining() / 1000;

        if ( istrue( level.bmoovertime ) )
            break;

        if ( var_3 <= 600 && !var_0 )
        {
            var_0 = 1;
            setomnvar( "ui_br_circle_state", 1 );

            foreach ( var_5 in level.players )
                var_5 scripts\mp\utility\lower_message::setlowermessageomnvar( 69, undefined, 10 );

            continue;
        }

        if ( var_3 <= 300 && !var_1 )
        {
            var_1 = 1;
            setomnvar( "ui_br_circle_state", 2 );

            foreach ( var_5 in level.players )
                var_5 scripts\mp\utility\lower_message::setlowermessageomnvar( 70, undefined, 10 );

            continue;
        }

        if ( var_3 <= 0 && !var_2 )
        {
            var_2 = 1;

            if ( level.teamplunderscoremechanic )
                return;

            setomnvar( "ui_nuke_countdown_active", 0 );
            level.mercywintriggered = 1;
            var_9 = findanyaliveplayer();

            if ( isplayer( var_9 ) )
            {
                var_9 scripts\mp\killstreaks\nuke::tryusenuke();
                wait 10;
                var_10 = findteamwithnoplayers();
                level thread scripts\mp\gamelogic::endgame( var_10, game["end_reason"]["mercy_win"], game["end_reason"]["mercy_loss"], 0, 1 );
            }
            else
            {
                var_10 = findteamwithnoplayers();
                level thread scripts\mp\gamelogic::endgame( var_10, game["end_reason"]["mercy_win"], game["end_reason"]["mercy_loss"], 0, 1 );
            }
        }
    }
}

calculatenumteamswithplayers()
{
    level.dmz_numteamswithplayers = 0;

    foreach ( var_1 in level.teamdata )
    {
        if ( var_1["players"].size > 0 )
            level.dmz_numteamswithplayers++;
    }
}

findanyaliveplayer()
{
    var_0 = getarraykeys( level.teamdata );

    foreach ( var_2 in var_0 )
    {
        if ( level.teamdata[var_2]["alivePlayers"].size > 0 )
            return level.teamdata[var_2]["alivePlayers"][0];
    }

    return undefined;
}

findteamwithnoplayers()
{
    var_0 = getarraykeys( level.teamdata );

    foreach ( var_2 in var_0 )
    {
        if ( level.teamdata[var_2]["players"].size == 0 )
            return var_2;
    }

    return "tie";
}

onplayerkilled( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 )
{
    scripts\mp\gametypes\br::onplayerkilled( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 );

    if ( scripts\mp\gametypes\br_public::isdmztutorial() )
        return;

    var_10 = getdvarint( "scr_bmo_mode_splash_window", 7500 );

    if ( isdefined( level.splashtime_vip ) && level.splashtime_vip + var_10 > gettime() )
        scripts\mp\hud_message::showsplash( "bm_vips_marked" );

    if ( isdefined( level.splashtime_lzs ) && level.splashtime_lzs + var_10 > gettime() )
        scripts\mp\hud_message::showsplash( "bm_extract_heli_start" );

    if ( isdefined( level.splashtime_drops ) && level.splashtime_drops + var_10 > gettime() )
        scripts\mp\hud_message::showsplash( "br_c130airdrop_incoming" );

    if ( isdefined( level.splashtime_helis ) && level.splashtime_helis + var_10 > gettime() )
        scripts\mp\hud_message::showsplash( "br_lootchopper_incoming" );
}

brmayconsiderplayerdead( var_0 )
{
    return 1;
}

playerrespawn()
{
    level endon( "game_ended" );
    self endon( "disconnect" );

    if ( istrue( level.gameended ) )
        return;

    if ( getdvarint( "scr_bmo_use_spawn_fix", 1 ) == 1 )
        self endon( "brWaitAndSpawnClientComplete" );

    var_0 = scripts\mp\utility\teams::getteamdata( self.team, "teamCount" );

    if ( !istrue( self.squadwiped ) || var_0 == 1 )
    {
        var_1 = 1.0;
        wait( var_1 );
    }

    if ( istrue( self.hasrespawntoken ) && isdefined( level.tokenrespawnwaittime ) )
        var_2 = level.tokenrespawnwaittime;
    else if ( level.incrementalrespawnpunish > 0.0 )
        var_2 = clamp( self.pers["deaths"] * level.incrementalrespawnpunish, 0.0, level.incrementalrespawnpunishmax );
    else if ( isdefined( level.autorespawnwaittime ) )
        var_2 = level.autorespawnwaittime;
    else
        var_2 = getdvarint( "scr_br_extract_spawn_wait", 20 );

    var_3 = getdvarfloat( "scr_bmo_respawn_predict_hint_time", 10.0 );

    if ( var_2 < var_3 )
        var_2 = var_3;

    if ( level.respawntimedisable != 0 )
        var_2 = 0;

    var_4 = getdvarfloat( "scr_bmo_squad_wiped_stream_time", 5.0 );
    scripts\engine\utility::ent_flag_init( "playerRespawn_intermission_spawned" );
    self.intermissionspawnorigin = undefined;
    self.intermissionspawntime = undefined;

    if ( istrue( self.squadwiped ) || var_0 == 1 )
    {
        var_5 = scripts\mp\gametypes\br_gulag::playergulaggetrespawnpoint();
        var_6 = scripts\mp\gametypes\br_gulag::playerprestreamrespawnorigin( var_5 );
        thread fadetogearingup( 0.0, var_0 > 1 );
        wait( var_4 );
    }
    else if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
    {
        scripts\mp\utility\lower_message::setlowermessageomnvar( 9, int( gettime() + var_2 * 1000 ) );
        var_7 = var_2 - var_3;
        var_8 = var_2 - var_7;
        var_9 = var_2 - getdvarfloat( "scr_bmo_respawn_intermission_time", 6.0 );
        thread fadetogearingup( var_9 );
        var_10 = scripts\engine\utility::waittill_notify_or_timeout_return( "squad_wipe_death", var_7 );

        if ( var_10 == "squad_wipe_death" )
        {
            var_5 = scripts\mp\gametypes\br_gulag::playergulaggetrespawnpoint();
            var_6 = scripts\mp\gametypes\br_gulag::playerprestreamrespawnorigin( var_5 );
            thread fadetogearingup( 0.0, 1 );
            wait( var_4 );
        }
        else
        {
            thread updateprestreamrespawn();
            var_10 = scripts\engine\utility::waittill_notify_or_timeout_return( "squad_wipe_death", var_8 );

            if ( var_10 == "squad_wipe_death" )
            {
                var_5 = scripts\mp\gametypes\br_gulag::playergulaggetrespawnpoint();
                var_6 = scripts\mp\gametypes\br_gulag::playerprestreamrespawnorigin( var_5 );
                thread fadetogearingup( 0.0, 1 );
                wait( var_4 );
            }
        }
    }

    self notify( "stop_updatePrestreamRespawn" );
    var_5 = scripts\mp\gametypes\br_gulag::playergulaggetrespawnpoint();
    var_6 = scripts\mp\gametypes\br_gulag::playerprestreamrespawnorigin( var_5 );

    if ( istrue( self.squadwiped ) )
    {
        self.squadwiped = 0;

        if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
            scripts\mp\hud_message::showsplash( "bm_your_squad_wiped" );
    }

    if ( isteamextracted( self.team ) )
        return;

    if ( istrue( self.hasrespawntoken ) )
    {
        thread scripts\mp\gametypes\br_gulag::triggerrespawnoverlay( 4 );
        self.tokenrespawned = 1;
        scripts\mp\gametypes\br_pickups::removerespawntoken();
    }

    if ( isteamextracted( self.team ) )
        return;

    if ( getdvarint( "scr_skip_respawn_gate", 1 ) == 0 )
        scripts\mp\gametypes\br_public::playerwaittillstreamhintcomplete();

    scripts\engine\utility::ent_flag_clear( "playerRespawn_intermission_spawned" );
    self.intermissionspawnorigin = undefined;
    self.intermissionspawntime = undefined;
    scripts\mp\utility\lower_message::setlowermessageomnvar( 0 );
    scripts\mp\playerlogic::spawnplayer( undefined, 0 );
    scripts\cp_mp\execution::_clearexecution();
    scripts\mp\gametypes\br_pickups::initplayer();

    if ( scripts\mp\gametypes\br_public::isdmztutorial() && isdefined( level.player_respawn_struct ) )
        var_5 = scripts\engine\utility::getstruct( level.player_respawn_struct, "targetname" );

    scripts\mp\gametypes\br_gulag::gulagwinnerrespawn( 1, undefined, var_5, 1, var_6, 1 );
    scripts\mp\gametypes\br::unmarkplayeraseliminated( self );
    level thread scripts\mp\battlechatter_mp::trysaylocalsound( self, "player_respawn" );
}

playershouldrespawn( var_0 )
{
    return 1;
}

updateprestreamrespawn()
{
    self endon( "disconnect" );
    self endon( "spawned_player" );
    self endon( "stop_updatePrestreamRespawn" );

    for (;;)
    {
        if ( scripts\engine\utility::ent_flag( "playerRespawn_intermission_spawned" ) )
        {
            var_0 = scripts\mp\gametypes\br_gulag::playergulaggetrespawnpoint();
            var_1 = gettime();

            if ( distance2dsquared( self.intermissionspawnorigin, var_0.origin ) > 25000000 )
            {
                self.intermissionspawnorigin = var_0.origin;
                self.intermissionspawntime = var_1;
                self setorigin( var_0.origin );
                scripts\mp\gametypes\br_public::playerclearstreamhintorigin();
            }

            if ( var_1 - self.intermissionspawntime >= getdvarfloat( "scr_bmo_spawn_fallback_hint_delay", 2.0 ) * 1000 )
            {
                var_0 = scripts\mp\gametypes\br_gulag::playergulaggetrespawnpoint( 1 );
                var_2 = scripts\mp\gametypes\br_gulag::playerprestreamrespawnorigin( var_0 );
            }
        }
        else
        {
            var_0 = scripts\mp\gametypes\br_gulag::playergulaggetrespawnpoint();
            var_2 = scripts\mp\gametypes\br_gulag::playerprestreamrespawnorigin( var_0 );
        }

        wait 1.0;
    }
}

fadetogearingup( var_0, var_1 )
{
    level endon( "game_ended" );
    self endon( "disconnect" );
    self endon( "spawned_player" );
    self notify( "fadeToGearingUp" );
    self endon( "fadeToGearingUp" );

    if ( isdefined( var_0 ) && var_0 > 0 )
        wait( var_0 );

    var_2 = 1.0;
    thread scripts\mp\gametypes\br_gulag::fadeoutin( var_2 );
    wait( var_2 - 0.25 );

    if ( istrue( var_1 ) )
        self setclientomnvar( "ui_br_transition_type", 6 );
    else
        self setclientomnvar( "ui_br_transition_type", 2 );

    wait 0.25;

    if ( getdvarint( "scr_bmo_use_spawn_intermission_fix", 1 ) == 1 )
    {
        scripts\mp\gametypes\br_public::playerclearstreamhintorigin();
        var_3 = scripts\mp\gametypes\br_gulag::playergulaggetrespawnpoint();
        scripts\mp\gametypes\br_spectate::playerclearspectatekillchainsystem();
        scripts\mp\gametypes\br::spawnintermission( var_3.origin, var_3.angles );
        scripts\mp\spectating::setdisabled();
        self.intermissionspawnorigin = var_3.origin;
        self.intermissionspawntime = gettime();
        scripts\engine\utility::ent_flag_set( "playerRespawn_intermission_spawned" );
    }
}

setupmission()
{
    level.brgametype.extractposition = getextractionposition();
    thread makeextractionobjective();
}

getextractionposition()
{
    var_0 = 2500;
    var_1 = level.br_level.br_mapcenter;
    var_2 = ( 0, randomfloatrange( 0, 360 ), 0 );
    var_3 = anglestoforward( var_2 );
    var_4 = level.br_level.br_circleradii[0] * 2;
    var_5 = var_1 + var_3 * var_4;
    var_5 = scripts\mp\gametypes\br_c130::snappointtooutofboundstriggertrace( var_1, var_5 );
    var_6 = getent( "airstrikeheight", "targetname" );
    var_7 = ( var_5[0], var_5[1], var_6.origin[2] );
    var_8 = tracegroundpoint( var_7 );
    var_5 = var_8 + ( 0, 0, var_0 );
    return var_5;
}

makeextractionobjective()
{
    var_0 = scripts\mp\objidpoolmanager::requestobjectiveid( 1 );
    level.brgametype.objectiveiconid = var_0;

    if ( var_0 != -1 )
    {
        scripts\mp\objidpoolmanager::objective_add_objective( var_0, "current", level.brgametype.extractposition, "icon_waypoint_koth" );
        scripts\mp\objidpoolmanager::update_objective_setbackground( var_0, 0 );
        scripts\mp\objidpoolmanager::objective_playermask_hidefromall( var_0 );
    }
}

setupmissionwidget()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );
    var_0 = getcurrentxp();
    var_1 = scripts\mp\gametypes\br_quest_util::getquesttableindex( "gt_extract_1" );

    foreach ( var_3 in level.players )
    {
        var_3 scripts\mp\gametypes\br_quest_util::setquestindexomnvar( var_1 );
        var_3 scripts\mp\gametypes\br_quest_util::uiobjectivesetparameter( var_0 );
    }
}

getcurrentxp()
{
    var_0 = getdvarint( "scr_br_extract_xp", 5000 );
    var_1 = getdvarint( "scr_br_extract_xp_min", 2000 );
    var_2 = getdvarint( "scr_br_extract_xp_decrease", 200 );

    if ( var_1 > 0 )
        var_0 = int( max( var_1, var_0 - level.brgametype.numextractions * var_2 ) );

    return var_0;
}

showextractionobjectivetoteam( var_0 )
{
    var_1 = level.teamdata[var_0]["players"];

    foreach ( var_3 in var_1 )
        scripts\mp\objidpoolmanager::objective_playermask_addshowplayer( level.brgametype.objectiveiconid, var_3 );
}

hideextractionobjectivefromteam( var_0 )
{
    var_1 = level.teamdata[var_0]["players"];

    foreach ( var_3 in var_1 )
        scripts\mp\objidpoolmanager::objective_playermask_hidefrom( level.brgametype.objectiveiconid, var_3 );
}

doteamextractedupdate()
{
    level.brgametype.numextractions++;
    level.brgametype.hudextractnum setvalue( level.brgametype.numextractions );
    level.brgametype.hudextractnum thread huddopulse();
    level.brgametype.hudextractmax thread huddopulse();
    var_0 = getcost();
    level.brgametype.hudcost setvalue( var_0 * 100 );

    foreach ( var_3, var_2 in level.teamdata )
    {
        if ( isdefined( var_2["teamCount"] ) && var_2["teamCount"] > 0 )
            updateteamplunderhud( var_3 );
    }

    var_4 = getcurrentxp();

    foreach ( var_6 in level.players )
    {
        if ( !isteamextracted( var_6.team ) )
        {
            var_6 thread scripts\mp\hud_message::showsplash( "br_gametype_extract_extracted" );
            var_6 scripts\mp\gametypes\br_quest_util::uiobjectivesetparameter( var_4 );
        }
    }
}

setteamasextracted( var_0 )
{
    level.teamdata[var_0]["extracted"] = 1;
}

isteamextracted( var_0 )
{
    return istrue( level.teamdata[var_0]["extracted"] );
}

brendgame()
{
    wait 1.5;
    handleendgamesplash();
    scripts\mp\gamelogic::endgame_regularmp( level.brgametype.firstteam, game["end_reason"]["objective_completed"], game["end_reason"]["br_eliminated"] );
}

handleendgamesplash()
{
    foreach ( var_1 in level.players )
    {
        if ( !isteamextracted( var_1.team ) )
            var_1 setclientomnvar( "post_game_state", 2 );
    }
}

setuptimelimit()
{
    level endon( "game_ended" );
    var_0 = 120;
    scripts\mp\flags::gameflagwait( "prematch_done" );

    if ( !istrue( level.br_infils_disabled ) )
        level waittill( "br_ready_to_jump" );

    waitframe();
    var_1 = createhudelem( &"MP_BR_INGAME/EXTRACT_COLLECT_PLUNDER", undefined, "CENTER", "CENTER", 0, -170 );
    var_1.alpha = 1;
    var_2 = scripts\mp\hud_util::createservertimer( "default", 1.5 );
    var_2 scripts\mp\hud_util::setpoint( "CENTER", "CENTER", 0, -150 );
    var_3 = getdvarint( "scr_br_extract_timecollect", 180 );

    if ( var_3 > 0 )
    {
        setomnvar( "ui_hardpoint_timer", gettime() + int( var_3 * 1000 ) );
        var_2 settimer( var_3 );
        wait( var_3 );
    }

    scripts\mp\flags::gameflagset( "collect_done" );
    var_4 = getdvarint( "scr_br_extract_timewaitactive", 180 );

    if ( var_4 > 0 )
    {
        var_1.label = &"MP_BR_INGAME/EXTRACT_HELIPADS_ACTIVE";
        var_1 thread huddopulse();
        setomnvar( "ui_hardpoint_timer", gettime() + int( var_4 * 1000 ) );
        var_2 settimer( var_4 );
        wait( var_4 );
    }

    scripts\mp\flags::gameflagset( "helipad_wait_done" );
    var_5 = getdvarint( "scr_br_extract_timeextract", 840 );
    var_1.label = &"MP_BR_INGAME/EXTRACT_HELIPAD";
    var_1 thread huddopulse();
    setomnvar( "ui_hardpoint_timer", gettime() + int( var_5 * 1000 ) );
    var_2 settimer( var_5 );
    var_6 = max( var_5 - var_0, 0 );
    wait( var_6 );
    var_7 = max( var_5 - var_6, 0 );
    var_2.color = ( 1, 0, 0 );
    var_2 thread huddopulse();
    thread clocksounds( var_7 );
    wait( var_7 );
    var_2 destroy();
    thread brendgame();
}

clocksounds( var_0 )
{
    level endon( "game_ended" );
    setomnvarforallclients( "ui_final_moments_notify", gettime() + var_0 * 1000 );

    while ( var_0 > 0 )
    {
        var_1 = 0;
        var_2 = scripts\mp\gamelogic::getclocksoundaliasfortimeleft( var_0 );

        if ( var_0 > 60 && var_0 % 10 == 0 || var_0 <= 60 && var_0 > 30 && var_0 % 2 == 0 || var_0 <= 30 )
            var_1 = 1;

        if ( var_1 )
        {
            foreach ( var_4 in level.players )
                var_4 playlocalsound( var_2 );
        }

        var_0 = var_0 - 1;
        wait 1;
    }
}

setupteamplunderhud()
{
    var_0 = 155;
    var_1 = 15;
    var_2 = -3;
    var_3 = 3;

    if ( level.teamplunderscoremechanic )
    {
        var_4 = getwincost();
        var_5 = &"MP_BR_INGAME/WIN_COST_TEXT";
        var_6 = var_4;
    }
    else
    {
        var_4 = getcost();
        var_5 = &"MP_BR_INGAME/EXTRACT_COST_TEXT";
        var_6 = var_4 * 100;
    }

    level.brgametype.hudcoststring = createhudelem( var_5, undefined, "LEFT", "CENTER", var_3, var_0, undefined, undefined, 1 );
    level.brgametype.hudcost = createhudelem( &"MP_BR_INGAME/EXTRACT_COST_MILLION", undefined, "LEFT", "CENTER", 65 + var_3, var_0, undefined, undefined, 1 );
    level.brgametype.hudplunderstring = createhudelem( &"MP_BR_INGAME/YOUR_TEAM_PLUNDER_TEXT", undefined, "RIGHT", "CENTER", 5 + var_2, var_0, undefined, undefined, 1 );
    var_7 = 0;
    level.brgametype.leaderplunderstring = createhudelem( &"MP_BR_INGAME/LEADER_PLUNDER_TEXT", var_7, "CENTER", "CENTER", 0, var_0 + var_1, undefined, undefined, 1 );

    foreach ( var_9 in level.teamnamelist )
    {
        var_10 = createhudelem( &"MP_BR_INGAME/EXTRACT_PLUNDER", 0, "RIGHT", "CENTER", 70 + var_2, var_0, undefined, var_9, 1 );
        var_11 = createhudelem( &"MP_BR_INGAME/ST_PLACE", undefined, "RIGHT", "CENTER", -65 + var_2, var_0, undefined, var_9, 1 );
        var_11 setvalue( 1 );
        var_10.placement = var_11;
        setteamplunderhud( var_9, var_10 );
    }

    scripts\mp\flags::gameflagwait( "prematch_done" );

    if ( !istrue( level.br_infils_disabled ) )
        level waittill( "br_ready_to_jump" );

    level.brgametype.hudcoststring.alpha = 1;
    level.brgametype.hudcost.alpha = 1;
    level.brgametype.hudplunderstring.alpha = 1;
    level.brgametype.leaderplunderstring.alpha = 1;

    foreach ( var_9 in level.teamnamelist )
    {
        var_10 = getteamplunderhud( var_9 );
        var_10.alpha = 1;
        var_10.placement.alpha = 1;
    }
}

setteamplunderhud( var_0, var_1 )
{
    level.teamdata[var_0]["hudPlunder"] = var_1;
    var_1.plundercount = 0;
    var_1.plundercountroll = 0;
}

getteamplunderhud( var_0 )
{
    return level.teamdata[var_0]["hudPlunder"];
}

createhudelem( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9 )
{
    if ( !isdefined( var_8 ) )
        var_8 = 1.5;

    if ( isdefined( var_7 ) )
        var_10 = newteamhudelem( var_7 );
    else if ( isdefined( var_9 ) )
        var_10 = newclienthudelem( var_9 );
    else
        var_10 = newhudelem();

    var_10.elemtype = "font";
    var_10.font = "default";
    var_10.fontscale = var_8;
    var_10.basefontscale = var_8;
    var_10.x = 0;
    var_10.y = 0;
    var_10.width = 0;
    var_10.height = int( level.fontheight * var_8 );
    var_10.xoffset = 0;
    var_10.yoffset = 0;
    var_10.children = [];
    var_10 scripts\mp\hud_util::setparent( level.uiparent );
    var_10.hidden = 0;
    var_10.alpha = 0;
    var_10 scripts\mp\hud_util::setpoint( var_2, var_3, var_4, var_5 );

    if ( isdefined( var_0 ) )
        var_10.label = var_0;

    if ( isdefined( var_1 ) )
        var_10 setvalue( var_1 );

    if ( isdefined( var_6 ) )
        var_10.color = var_6;

    return var_10;
}

getteamplunder( var_0 )
{
    var_1 = 0;

    if ( !level.scorerequiresbanking )
    {
        var_1 = var_1 + level.teamdata[var_0]["plunderTeamTotal"];
        var_1 = var_1 + level.teamdata[var_0]["plunderInDeposit"];
    }

    var_1 = var_1 + level.teamdata[var_0]["plunderBanked"];
    return var_1;
}

getteamcarriedplunder( var_0 )
{
    return level.teamdata[var_0]["plunderTeamTotal"];
}

getbankedplunder( var_0 )
{
    return level.teamdata[var_0]["plunderBanked"];
}

sortplayerplunderscores( var_0, var_1 )
{
    return var_0 > var_1;
}

hackclientomnvarclamp( var_0, var_1 )
{
    var_2 = int( min( var_1, 131072 ) );
    self setclientomnvar( var_0, var_2 );
}

hackomnvarclamp( var_0, var_1 )
{
    var_2 = int( min( var_1, 131072 ) );
    setomnvar( var_0, var_2 );
}

plunderrankupdate()
{
    level endon( "game_ended" );
    var_0 = 0;
    var_1 = [];
    var_2 = 0;
    var_3 = 0;
    var_4 = 0;
    var_5 = 0;
    var_6 = 0;
    var_7 = 0;
    var_8 = level.hideleaderhashuntilpercent != 0;
    level.splashtime_vip = undefined;
    level.splashtime_lzs = undefined;
    level.splashtime_drops = undefined;
    level.splashtime_helis = undefined;

    for (;;)
    {
        waittillframeend;
        var_9 = level.called50percentprogress && level.called75percentprogress && level.called90percentprogress;
        var_10 = getwincost();
        var_0 = var_0 + level.framedurationseconds;

        if ( istrue( level.usemilestonephases ) )
            var_11 = scripts\mp\flags::gameflag( "placement_updates_allowed" );
        else
            var_11 = var_0 > level.secondsbeforeplacementupdates;

        var_12 = level.showplacementsplashesandmusic && var_11;
        var_13 = scripts\mp\gamescore::getteamscoreplacements();
        var_14 = level.teamplunderscoremechanic;
        var_15 = undefined;
        var_16 = undefined;
        var_17 = undefined;
        var_18 = [];
        var_19 = [];
        var_20 = [];
        var_21 = "none";
        var_22 = "none";
        var_23 = -1;
        var_24 = -1;

        foreach ( var_26 in level.teamnamelist )
        {
            var_27 = getteamplunder( var_26 );
            var_28 = getbankedplunder( var_26 );
            var_29 = updateteamplunderscore( var_27, var_26, level.brgametype.firstteam );
            var_30 = getpreviousplacement( var_26 );
            var_31 = var_13[var_26];

            if ( var_27 > var_23 )
            {
                if ( var_23 > var_24 )
                {
                    var_24 = var_23;
                    var_22 = var_21;
                }

                var_23 = var_27;
                var_21 = var_26;
            }
            else if ( var_21 != "none" )
            {
                if ( var_27 > var_24 )
                {
                    var_24 = var_27;
                    var_22 = var_26;
                }
            }

            var_32 = ( var_27 - var_28 ) * 100;
            var_33 = var_27 * 100;
            var_34 = var_33 - var_32;

            if ( var_32 < 0 )
                var_32 = 0;

            if ( var_33 >= var_10 * 0.9 )
                var_15 = var_26;
            else if ( var_33 >= var_10 * 0.75 )
                var_16 = var_26;
            else if ( var_33 >= var_10 * 0.5 )
                var_17 = var_26;

            if ( var_33 >= var_10 * level.plundermusicfourth )
                thread scripts\mp\music_and_dialog::plunder_ninetypercent_music();
            else if ( var_33 >= var_10 * level.plundermusicthird )
                thread scripts\mp\music_and_dialog::plunder_seventyfivepercent_music();
            else if ( var_33 >= var_10 * level.plundermusicsecond )
                thread scripts\mp\music_and_dialog::plunder_fiftypercent_music();
            else if ( var_33 >= var_10 * level.plundermusicfirst )
                thread scripts\mp\music_and_dialog::plunder_thirtypercent_music();

            var_35 = scripts\mp\utility\teams::getfriendlyplayers( var_26, 0 );

            foreach ( var_37 in var_35 )
            {
                var_31 = var_13[var_37.team];

                if ( !var_6 )
                    var_31 = 155;

                var_37 setclientomnvar( "ui_br_team_placement", var_31 );
                var_37 setclientomnvar( "ui_br_player_position", var_31 );
                var_37 hackclientomnvarclamp( "ui_br_team_cash_banked", int( var_34 * 0.01 ) );
                var_37 hackclientomnvarclamp( "ui_br_team_cash_pockets", int( var_32 * 0.01 ) );
                var_19[var_37.guid] = var_37.plundercount;
                var_20[var_37.guid] = var_37;
            }

            if ( var_12 )
            {
                if ( var_31 == 1 )
                {
                    if ( var_31 < var_30 || var_30 == -1 )
                        var_18[var_18.size] = var_26;
                }
                else if ( var_31 <= 5 )
                {
                    if ( var_30 > 5 )
                        showsplashtoteam( var_26, "bm_top_5" );
                }
                else if ( var_31 <= 10 )
                {
                    if ( var_30 > 10 )
                        showsplashtoteam( var_26, "bm_top_10" );
                }
            }

            if ( var_33 >= var_10 )
            {
                autobankteamplunder( var_26 );

                if ( var_14 && level.doendofmatchotsequence > 0 && !level.disablewinonscore )
                {
                    level thread triggerovertimetimer( var_26, level.teamdata[var_26]["plunderBanked"] * 1000 >= var_10 );
                    continue;
                }

                if ( var_14 && level.doendofmatchotsequence > 0 && level.disablewinonscore )
                {
                    level.initialwinningteam = var_26;
                    continue;
                }

                if ( level.teamplunderexfil && !istrue( level.exfilactive ) )
                {
                    level.exfilactive = 1;
                    level thread startbmoexfilprocess( var_26 );
                    continue;
                }

                if ( !level.teamplunderexfil && var_14 && !level.disablewinonscore )
                    level thread givewincondition( var_26 );
            }
        }

        var_40 = [];
        var_41 = [];

        if ( level.lootleadermarksontopteams == 1 )
            var_40 = tablesort( game["teamPlacements"], "up" );
        else if ( level.lootleadermarksontopteams == 2 )
        {
            var_40[0] = var_21;
            var_41 = tablesort( var_19, "down" );
        }
        else
            var_41 = tablesort( var_19, "down" );

        level.brgametype.firstteam = var_21;

        if ( var_21 == "none" )
        {
            var_1 = [];
            waitframe();
            continue;
        }

        if ( var_22 != "none" )
            hackomnvarclamp( "ui_br_cash_second", int( var_24 ) );

        hackomnvarclamp( "ui_br_cash_leader", int( var_23 ) );

        if ( !var_8 && var_23 * 100 >= var_10 * level.hideleaderhashuntilpercent * 0.01 )
        {
            setomnvar( "ui_br_leader_hash_percentage_hit", 1 );
            var_8 = 1;
        }

        if ( !istrue( var_7 ) && istrue( level.usemilestonephases ) )
        {
            if ( !var_2 && var_23 * 100 >= var_10 * level.milestonephasepercent_vips * 0.01 )
            {
                var_2 = 1;
                level.splashtime_vip = gettime();
                scripts\mp\flags::gameflagset( "placement_updates_allowed" );

                if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                    showsplashtoall( "bm_vips_marked" );
            }

            if ( !var_3 && ( level.scorerequiresbanking || var_23 * 100 >= var_10 * level.milestonephasepercent_lzs * 0.01 ) )
            {
                var_3 = 1;
                level.splashtime_lzs = gettime();
                scripts\mp\flags::gameflagset( "activate_cash_lzs" );

                if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                    scripts\mp\gametypes\br_public::brleaderdialog( "extract_enabled", 0, undefined, undefined, undefined, "bm" );
            }

            if ( !var_4 && var_23 * 100 >= var_10 * level.milestonephasepercent_drops * 0.01 )
            {
                var_4 = 1;
                level.splashtime_drops = gettime();
                level.br_level.c130_speedoverride = 3044;
                scripts\mp\flags::gameflagset( "activate_cash_drops" );
            }

            if ( !var_5 && ( istrue( level.bmoovertime ) || var_23 * 100 >= var_10 * level.milestonephasepercent_helis * 0.01 ) )
            {
                var_5 = 1;
                level.splashtime_helis = gettime();
                scripts\mp\flags::gameflagset( "activate_cash_helis" );

                if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                    scripts\mp\gametypes\br_public::brleaderdialog( "event_chopper", 0, undefined, undefined, undefined, "bm" );
            }

            if ( !var_6 && var_23 * 100 >= var_10 * level.hideplacementuntilpercent * 0.01 )
                var_6 = 1;

            if ( var_2 && var_3 && var_4 && var_5 )
                var_7 = 1;
        }

        if ( !var_12 || var_23 == 0 )
        {
            waitframe();
            continue;
        }

        if ( level.lootleadermarksontopteams > 0 )
        {
            if ( level.dmzlootleaderupdateonpickup || istrue( level.plunderupdatelootleadermarks ) )
            {
                updatelootleadermarks( var_19, var_41, var_20, var_40 );
                level.plunderupdatelootleadermarks = 0;
            }
        }
        else if ( level.dmzlootleaderupdateonpickup || istrue( level.plunderupdatelootleadermarks ) )
        {
            updatelootleadermarks( var_19, var_41, var_20 );
            level.plunderupdatelootleadermarks = 0;
        }

        foreach ( var_26 in var_18 )
        {
            if ( istrue( level.topteamsever[var_26] ) )
            {
                if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                    showsplashtoteam( var_26, "bm_top_team_regained" );

                continue;
            }

            if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                showsplashtoteam( var_26, "bm_top_team" );

            level.topteamsever[var_26] = 1;
        }

        if ( var_18.size > 0 )
        {
            foreach ( var_26 in var_1 )
            {
                if ( !scripts\engine\utility::array_contains( var_18, var_26 ) )
                    showsplashtoteam( level.topteam, "bm_top_team_lost" );
            }
        }

        var_1 = var_18;
        level.topteam = var_21;
        level.previousplacements = var_13;

        if ( !var_9 )
        {
            if ( !level.called90percentprogress && isdefined( var_15 ) )
            {
                level.called90percentprogress = 1;

                if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                {
                    if ( !level.disablepercentageannouncements )
                    {
                        showsplashtoallexceptteam( var_15, "bm_first_to_90_them" );
                        showsplashtoteam( var_15, "bm_first_to_90_us" );
                        level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "gamestate_90_perc_first", var_15, 1, undefined, "bm" );

                        foreach ( var_47 in level.teamnamelist )
                        {
                            if ( var_47 != var_15 )
                                level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "gamestate_90_perc_enemy", var_47, 1, undefined, "bm" );
                        }
                    }
                }
            }
            else if ( !level.called75percentprogress && isdefined( var_16 ) )
            {
                level.called75percentprogress = 1;

                if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                {
                    if ( !level.disablepercentageannouncements )
                    {
                        showsplashtoallexceptteam( var_16, "bm_first_to_75_them" );
                        showsplashtoteam( var_16, "bm_first_to_75_us" );
                        level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "gamestate_75_perc_first", var_16, 1, undefined, "bm" );

                        foreach ( var_47 in level.teamnamelist )
                        {
                            if ( var_47 != var_16 )
                                level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "gamestate_75_perc_enemy", var_47, 1, undefined, "bm" );
                        }
                    }
                }
            }
            else if ( !level.called50percentprogress && isdefined( var_17 ) )
            {
                level.called50percentprogress = 1;

                if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                {
                    if ( !level.disablepercentageannouncements )
                    {
                        showsplashtoallexceptteam( var_17, "bm_first_to_50_them" );
                        showsplashtoteam( var_17, "bm_first_to_50_us" );
                        level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "gamestate_50_perc_first", var_17, 1, undefined, "bm" );

                        foreach ( var_47 in level.teamnamelist )
                        {
                            if ( var_47 != var_17 )
                                level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "gamestate_50_perc_enemy", var_47, 1, undefined, "bm" );
                        }
                    }
                }
            }
        }

        waitframe();
    }
}

autobankteamplunder( var_0 )
{
    if ( !isdefined( level.autobankedteams ) )
        level.autobankedteams = [];

    if ( scripts\engine\utility::array_contains( level.autobankedteams, var_0 ) )
        return;

    var_1 = scripts\mp\utility\teams::getteamdata( var_0, "players" );
    var_2 = scripts\mp\gametypes\br_plunder::createplayerplundereventdata();
    var_2.playanimation = 0;

    foreach ( var_4 in var_1 )
    {
        if ( isdefined( var_4.plundercount ) && var_4.plundercount > 0 )
            var_4 scripts\mp\gametypes\br_plunder::playerplunderbank( var_4.plundercount, undefined, var_2 );
    }

    level.autobankedteams[level.autobankedteams.size] = var_0;
}

overtimecashmultiplier()
{
    level thread scripts\mp\gametypes\br_plunder::processcashpileovertimemultiplier( level.overtimecashmultiplier );
}

placementupdatewait()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );

    if ( isdefined( level.secondsbeforeplacementupdates ) )
        wait( level.secondsbeforeplacementupdates );

    scripts\mp\flags::gameflagset( "placement_updates_allowed" );
}

getpreviousplacement( var_0 )
{
    if ( isdefined( level.previousplacements ) )
        return level.previousplacements[var_0];

    return -1;
}

showsplashtoall( var_0 )
{
    foreach ( var_2 in level.players )
        var_2 scripts\mp\hud_message::showsplash( var_0 );
}

showsplashtoteam( var_0, var_1 )
{
    foreach ( var_3 in level.teamdata[var_0]["players"] )
        var_3 scripts\mp\hud_message::showsplash( var_1 );
}

showsplashtoallexceptteam( var_0, var_1 )
{
    foreach ( var_3 in level.players )
    {
        if ( isdefined( var_3 ) && var_3.team != var_0 )
            var_3 scripts\mp\hud_message::showsplash( var_1 );
    }
}

triggermatchendtimer( var_0 )
{
    scripts\mp\gamelogic::resumetimer();
    level.starttime = gettime();
    level.discardtime = 0;
    level.timerpausetime = 0;
    var_1 = getdvarfloat( "scr_bmo_900k_timer", 10 );
    var_2 = "scr_" + scripts\mp\utility\game::getgametype() + "_timelimit";
    level.watchdvars[var_2].value = var_1;
    level.overridewatchdvars[var_2] = var_1;
}

updateteamplunderhud( var_0 )
{
    var_1 = getteamplunderhud( var_0 );
    var_2 = getteamplunder( var_0 );
    var_3 = getcost();

    if ( var_2 >= var_3 && var_1.color != ( 0, 1, 0 ) )
    {
        var_1.color = ( 0, 1, 0 );
        var_4 = level.teamdata[var_0]["players"];

        foreach ( var_6 in var_4 )
            var_6 playlocalsound( "br_plunder_atm_deposit_gtr" );
    }
}

dohudplunderrollsound( var_0, var_1 )
{
    var_2 = level.teamdata[var_1]["players"];

    foreach ( var_4 in var_2 )
    {
        if ( var_0 > 0 )
        {
            if ( isdefined( var_4.hudplunderstart ) )
            {
                var_5 = gettime() - var_4.hudplunderstart;

                if ( var_5 <= 6000 )
                    break;
            }

            var_4.hudplunderstart = gettime();

            if ( isalive( var_4 ) )
                var_4 playlocalsound( "br_plunder_atm_use" );

            continue;
        }

        var_4.hudplunderstart = undefined;
        var_4 stoplocalsound( "br_plunder_atm_use" );
    }
}

givewincondition( var_0 )
{
    if ( istrue( level.triggeredwincondition ) )
        return;

    if ( !istrue( level.triggeredwincondition ) )
        level thread printallteamsplundercount();

    level.triggeredwincondition = 1;
    level thread scripts\mp\gamelogic::endgame( var_0, game["end_reason"]["dmz_plunder_win"], game["end_reason"]["dmz_plunder_loss"], 0, 1 );
}

playerdropplunderondeath( var_0, var_1 )
{
    if ( isdefined( self.c130 ) )
        return 1;

    if ( istrue( level.gameended ) )
        return 1;

    if ( istrue( self.isdisconnecting ) )
    {
        var_2 = self.plundercount;
        var_3 = self.plundercount;
        var_4 = 0;
    }
    else
    {
        if ( isdefined( level.minplunderdropondeath ) && self.plundercount < level.minplunderdropondeath )
        {
            var_4 = 0;
            var_2 = self.plundercount;
            var_3 = self.plundercount;
        }
        else
        {
            var_4 = int( self.plundercount * ( ( 100 - level.percentageplunderdrop ) * 0.01 ) );
            var_2 = self.plundercount - var_4;
            var_3 = var_2;
        }

        if ( istrue( level.bmoovertime ) )
        {
            if ( var_2 <= level.bonusdeathplunderot && ( !isdefined( var_1 ) || var_1 != self ) )
                var_2 = level.bonusdeathplunderot;
        }
        else if ( var_2 <= level.bonusdeathplunder && ( !isdefined( var_1 ) || var_1 != self ) )
            var_2 = level.bonusdeathplunder;

        if ( isdefined( level.maxplunderdropondeath ) )
        {
            if ( var_2 >= level.maxplunderdropondeath )
            {
                var_2 = level.maxplunderdropondeath;
                var_3 = var_2;
                var_4 = self.plundercount - level.maxplunderdropondeath;
            }
        }
    }

    self.plundercountondeath = var_4;

    if ( var_3 > 0 )
        scripts\mp\gametypes\br_plunder::playerplunderdrop( var_3 );

    if ( var_2 >= level.plunderfxondropthreashold )
        playfx( scripts\engine\utility::getfx( "money" ), self.origin + ( 0, 0, 32 ) );

    var_5 = var_2;

    if ( istrue( self.wasexecuted ) )
        var_5 = int( var_2 * level.executioncashmultiplier );

    var_6 = var_5;

    if ( istrue( level.bmoovertime ) && ( !isdefined( var_1 ) || self != var_1 ) )
    {
        var_6 = int( var_5 * level.overtimecashmultiplier );

        if ( isdefined( level.maxplunderdropinovertime ) && var_6 > level.maxplunderdropinovertime )
            var_6 = level.maxplunderdropinovertime;
    }

    if ( scripts\mp\gametypes\br_public::isdmztutorial() && istrue( self.cashtorefund ) )
        var_6 = int( var_6 + self.cashtorefund );

    var_7 = dropplunderbyrarity( var_6, var_0 );

    foreach ( var_9 in var_7 )
        var_9.lootsource = "combat";

    if ( scripts\mp\gametypes\br_public::isdmztutorial() )
        level notify( "victim_death_drop", self, var_1, var_7 );

    if ( isdefined( var_1 ) && self == var_1 || !level.killcam )
        self setclientomnvar( "ui_br_plunder_dropped", var_3 );
    else
        self.postkillcamplunderlost = var_3;

    scripts\mp\gametypes\br_analytics::trackcashevent( self, "combat", var_3 * -1 );

    if ( isdefined( level.lootleaders ) && scripts\engine\utility::array_contains( level.lootleaders, self ) )
        playfx( scripts\engine\utility::getfx( "money" ), self.origin + ( 0, 0, 64 ) );

    return 1;
}

onpostkillcamcallback()
{
    if ( isdefined( self.postkillcamplunderlost ) )
    {
        self setclientomnvar( "ui_br_plunder_dropped", int( self.postkillcamplunderlost ) );
        self.postkillcamplunderlost = undefined;
    }
}

dropplunderbyrarity( var_0, var_1 )
{
    if ( !istrue( level.br_plunder_enabled ) )
        return;

    var_2 = [];
    var_3 = [];
    var_4 = 0;

    for ( var_5 = level.br_plunder.quantity.size - 1; var_5 >= 0; var_5-- )
    {
        var_3[var_5] = int( var_0 / level.br_plunder.quantity[var_5] );
        var_4 = var_4 + var_3[var_5];

        if ( var_0 <= 0 )
            break;
        else
            var_0 = var_0 - var_3[var_5] * level.br_plunder.quantity[var_5];
    }

    for ( var_6 = level.br_plunder.quantity.size - 1; var_6 >= 0; var_6-- )
    {
        if ( !isdefined( var_3[var_6] ) )
            continue;

        for ( var_7 = 0; var_7 < var_3[var_6]; var_7++ )
        {
            [var_9, var_10, var_11] = scripts\mp\gametypes\br_pickups::getitemdroporiginandangles( var_1 + var_2.size, self.origin, self.angles, self );
            var_12 = scripts\mp\gametypes\br_pickups::spawnpickup( level.br_plunder.names[var_6], var_9, var_10, level.br_plunder.quantity[var_6], 1, var_11 );
            scripts\mp\gametypes\br_plunder::modify_plunder_itemsinworld( level.br_plunder.names[var_6], 1 );

            if ( isdefined( var_12 ) )
            {
                var_2[var_2.size] = var_12;

                if ( scripts\mp\gametypes\br_plunder::inplunderlivelobby() )
                    level.br_plunder_ents[level.br_plunder_ents.size] = var_12;
            }
        }
    }

    level.br_plunder.plunder_items_dropped = level.br_plunder.plunder_items_dropped + var_4;
    level.br_plunder.plunder_value_dropped = level.br_plunder.plunder_value_dropped + var_0;
    level thread scripts\mp\gametypes\br_plunder::dropplundersounds( self.origin, var_2.size );
    return var_2;
}

getcost()
{
    if ( level.dmzminextractcost > 0 )
        var_0 = int( max( level.dmzminextractcost, level.dmzextractcost - level.brgametype.numextractions * level.dmzextractcostdecrease ) );

    return level.dmzextractcost;
}

getwincost()
{
    return level.dmzwincost;
}

playertakeextractionplunder( var_0 )
{
    if ( self.plundercount < var_0 )
        var_0 = self.plundercount;

    if ( !isdefined( self.plunderforextract ) )
        self.plunderforextract = 0;

    self.plunderforextract = self.plunderforextract + var_0;
    scripts\mp\gametypes\br_plunder::playersetplundercount( self.plundercount - var_0 );
    return var_0;
}

teamclearextractplunder( var_0 )
{
    var_1 = level.teamdata[var_0]["players"];

    foreach ( var_3 in var_1 )
        var_3.plunderforextract = 0;
}

teamrefundplunder( var_0 )
{
    var_1 = level.teamdata[var_0]["players"];

    foreach ( var_3 in var_1 )
    {
        if ( isdefined( var_3.plunderforextract ) )
        {
            var_3 scripts\mp\gametypes\br_plunder::playerplunderpickup( var_3.plunderforextract );
            var_3.plunderforextract = 0;
            var_3 iprintlnbold( "Extraction refunded, chopper shot down." );
        }
    }
}

huddopulse()
{
    self endon( "death" );

    if ( istrue( self.pulsing ) )
        return;

    var_0 = 0.5;
    var_1 = 4;
    self.pulsing = 1;
    var_2 = self.fontscale;
    self changefontscaleovertime( var_0 );
    self.fontscale = var_1;
    wait( var_0 );
    self changefontscaleovertime( var_0 );
    self.fontscale = var_2;
    self.pulsing = undefined;
}

drophelicrate( var_0 )
{
    var_1 = spawn( "script_model", var_0.origin );
    var_1 setmodel( "military_skyhook_backpack" );
    var_2 = var_0.origin;
    var_3 = ( var_2[0], var_2[1], -12000 );
    var_4 = scripts\engine\trace::create_contents( 0, 1, 1, 1, 1, 1, 0 );
    var_5 = scripts\engine\trace::ray_trace( var_2, var_3, var_0, var_4 );
    var_6 = var_5["position"];
    var_7 = var_2[2] - var_6[2];

    if ( var_7 > 0 )
    {
        var_8 = sqrt( 2 * var_7 / 800 );
        var_1 moveto( var_6, var_8, var_8, 0 );
        wait( var_8 );
    }

    var_1.origin = var_6;
    playfx( scripts\engine\utility::getfx( "airdrop_crate_impact" ), var_6 );
    var_1 cratedropplunder();
    var_1 delete();
}

cratedropplunder()
{
    var_0 = getcost();
    scripts\mp\gametypes\br_plunder::dropplunderbyrarity( var_0, 0 );
}

calculatehelitimetoflysec( var_0, var_1, var_2 )
{
    var_3 = var_0 * 0.0000157828;
    var_4 = 0.5 * var_2;
    var_5 = var_1;
    var_6 = -1 * var_3;
    var_7 = ( -1 * var_5 + sqrt( var_5 * var_5 - 4 * var_4 * var_6 ) ) / ( 2 * var_4 );
    var_7 = var_7 * 3600;
    var_7 = var_7 + 1.5;
    return var_7;
}

calculatehelitimetoarrive( var_0 )
{
    var_1 = calculatehelitimetoflysec( 30000, 100, 125 );
    var_2 = calculatehelitimetoflysec( var_0, 25.0, 31.25 );
    var_3 = var_1 + var_2;
    return var_3;
}

helisetteamextractionhud( var_0, var_1 )
{
    var_2 = gettime() + int( var_1 * 1000 );
    var_3 = level.teamdata[self.team]["alivePlayers"];

    foreach ( var_5 in var_3 )
    {
        var_5 setclientomnvar( "ui_br_plunder_extract_state", var_0 );
        var_5 setclientomnvar( "ui_br_plunder_extract_end_time", var_2 );
    }
}

helibankplunder()
{
    if ( !isdefined( self.plunder ) )
        return;

    level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "plunder_extract_success", self.team, 1 );
    var_0 = 0;
    var_1 = 0;

    foreach ( var_3 in self.plunder )
    {
        var_0 = var_0 + var_3.plundercount;

        if ( var_3.player.team != self.team )
            var_1 = 1;
    }

    scripts\mp\gametypes\br_analytics::branalytics_plunder_extraction_success( self.plunder.size, var_0, "little_bird", var_1, self.endpoint );
    level.br_plunder.extraction_helicoptor_total_plunder = level.br_plunder.extraction_helicoptor_total_plunder + var_0;
    level.br_plunder.extraction_helicoptor_num_completed++;
    scripts\mp\gametypes\br_plunder::entityplunderbankalldeposited();
}

heliusecleanup()
{
    if ( isdefined( self.usable ) )
    {
        level.br_depots = scripts\engine\utility::array_remove( level.br_depots, self.usable );
        self.usable = undefined;
    }
}

helicleanupdepotonleaving( var_0 )
{
    self.usable endon( "death" );
    scripts\engine\utility::waittill_either( "leaving", "death" );
    heliusecleanup();
}

helicreateextractvfx( var_0 )
{
    self.vfxent = spawn( "script_model", var_0 );
    self.vfxent setmodel( "scr_smoke_grenade" );
    self.vfxent.angles = ( 0, 90, 90 );
    self.vfxent playloopsound( "smoke_carepackage_smoke_lp" );
    self.vfxent setscriptablepartstate( "smoke", "on" );
}

helicleanupextract( var_0 )
{
    if ( isdefined( self.vfxent ) )
    {
        self.vfxent stoploopsound();
        self.vfxent delete();
    }

    if ( istrue( var_0 ) && isdefined( self.site ) )
        self.site setscriptablepartstate( self.site.type, self.site.activestate );
}

helidisapateextractvfx()
{
    self endon( "death" );

    if ( !isdefined( self.vfxent ) )
        return;

    wait 5;
    self.vfxent endon( "death" );
    self.vfxent setscriptablepartstate( "smoke", "dissipate" );
    self.vfxent playsound( "smoke_canister_tail_dissipate" );
    wait 1;
    self.vfxent stoploopsound();
    wait 4.5;
    self.vfxent delete();
}

spawnheli( var_0, var_1, var_2, var_3 )
{
    var_4 = vectortoangles( var_2 - var_1 );
    var_5 = 99;
    var_6 = scripts\cp_mp\vehicles\vehicle_tracking::_spawnhelicopter( var_0, var_1, var_4, "veh_apache_plunder_mp", "veh8_mil_air_mindia8_plunder_x" );

    if ( !isdefined( var_6 ) )
        return;

    var_7 = var_2 * ( 1, 1, 0 );
    var_6.damagecallback = ::callback_vehicledamage;
    var_6.speed = 50;
    var_6.accel = 99999;
    var_6.health = 1000;
    var_6.maxhealth = var_6.health;
    var_6.team = var_0.team;
    var_6.owner = var_0;
    var_6.defendloc = var_2;
    var_6.lifeid = 0;
    var_6.flaresreservecount = var_5;
    var_6.pathgoal = var_2;
    var_6.pathexit = var_3;
    var_6.endpoint = var_7;
    var_6.goalyaw = var_4[1];
    var_6.vehiclename = "magma_plunder_chopper";
    var_6 setcandamage( 1 );
    var_6 setmaxpitchroll( 10, 25 );
    var_6 vehicle_setspeed( var_6.speed, var_6.accel );
    var_6 sethoverparams( 50, 100, 50 );
    var_6 setturningability( 0.05 );
    var_6 setyawspeed( 45, 25, 25, 0.5 );
    var_6 setotherent( var_0 );
    spawnrope( var_6 );
    var_6 thread scripts\mp\killstreaks\flares::flares_handleincomingstinger( undefined, undefined );
    var_6 thread healdamage();
    var_6 thread handledestroydamage();
    var_6 thread helidestroyed();
    return var_6;
}

spawnrope( var_0 )
{
    var_1 = spawn( "script_model", ( 0, 0, 0 ) );
    var_1 setmodel( "br_plunder_extraction_delivery_rope" );
    var_1 linkto( var_0, "side_door_l_jnt", ( 11, 20, 42 ), ( 0, 180, 0 ) );
    var_2 = spawn( "script_model", ( 0, 0, 0 ) );
    var_2 setmodel( "br_plunder_extraction_delivery_bag" );
    var_2 linkto( var_1, "dyn_rope_end", ( 0, 0, 0 ), ( 0, 0, 0 ) );
    var_0.rope = var_1;
    var_0.crate = var_2;
}

helidestroyed()
{
    self endon( "heli_gone" );
    self endon( "swapped" );
    var_0 = self.owner;
    var_1 = self.team;
    self waittill( "death", var_2, var_3, var_4, var_5 );
    teamrefundplunder( var_1 );
    helicleanup();

    if ( !isdefined( self ) )
        return;

    if ( !isdefined( self.largeprojectiledamage ) && !istrue( self.isdepot ) )
    {
        self vehicle_setspeed( 25, 5 );
        thread helicrash( 75 );
        scripts\mp\hostmigration::waitlongdurationwithhostmigrationpause( 2.7 );
    }

    heliexplode( var_2 );
}

helicleanup()
{
    if ( isdefined( self.rope ) )
        self.rope delete();

    if ( isdefined( self.crate ) )
        self.crate delete();
}

heliexplode( var_0 )
{
    var_1 = self gettagorigin( "tag_origin" ) + ( 0, 0, 40 );
    self radiusdamage( var_1, 256, 140, 70, var_0, "MOD_EXPLOSIVE" );
    playfx( scripts\engine\utility::getfx( "little_bird_explode" ), var_1, anglestoforward( self.angles ), anglestoup( self.angles ) );
    playsoundatpos( var_1, "veh_chopper_support_crash" );
    earthquake( 0.4, 800, var_1, 0.7 );
    playrumbleonposition( "grenade_rumble", var_1 );
    physicsexplosionsphere( var_1, 500, 200, 1 );
    self notify( "explode" );
    wait 0.35;
    level thread scripts\mp\gametypes\br::teamsplashbr( "br_gametype_extract_heli_shot_down", self.owner, self.owner.team );
    helicleanupextract( 1 );
    helidelete();
}

helidelete()
{
    scripts\cp_mp\vehicles\vehicle_tracking::_deletevehicle( self );
}

helicrash( var_0 )
{
    self endon( "explode" );
    self notify( "heli_crashing" );
    self setvehgoalpos( self.origin + ( 0, 0, 100 ), 1 );
    scripts\mp\hostmigration::waitlongdurationwithhostmigrationpause( 1.5 );
    self setyawspeed( var_0, var_0, var_0 );
    self settargetyaw( self.angles[1] + var_0 * 2.5 );
}

handledestroydamage()
{
    self endon( "death" );
    self endon( "leaving" );
    self endon( "swapped" );

    for (;;)
    {
        self waittill( "damage", var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10, var_11, var_12, var_13 );
        var_9 = scripts\mp\utility\weapon::mapweapon( var_9, var_13 );

        if ( ( var_9.basename == "aamissile_projectile_mp" || var_9.basename == "nuke_mp" ) && var_4 == "MOD_EXPLOSIVE" && var_0 >= self.health )
        {
            callback_vehicledamage( var_1, var_1, 9001, 0, var_4, var_9, var_3, var_2, var_3, 0, 0, var_7 );
            helicleanupextract( 1 );
        }
    }
}

callback_vehicledamage( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10, var_11, var_12 )
{
    if ( isdefined( var_1 ) )
    {
        if ( isdefined( var_1.owner ) )
            var_1 = var_1.owner;
    }

    if ( ( var_1 == self || isdefined( var_1.pers ) && var_1.pers["team"] == self.team && !level.friendlyfire && level.teambased ) && var_1 != self.owner )
        return;

    if ( self.health <= 0 )
        return;

    var_2 = scripts\mp\utility\killstreak::getmodifiedantikillstreakdamage( var_1, var_5, var_4, var_2, self.maxhealth, 3, 4, 5 );
    scripts\mp\killstreaks\killstreaks::killstreakhit( var_1, var_5, self, var_4, var_2 );
    var_1 scripts\mp\damagefeedback::updatedamagefeedback( "" );

    if ( self.health - var_2 <= 900 && ( !isdefined( self.smoking ) || !self.smoking ) )
        self.smoking = 1;

    self vehicle_finishdamage( var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10, var_11 );
}

helileave()
{
    self endon( "death" );
    self notify( "leaving" );
    self.leaving = 1;
    self setvehgoalpos( self.pathgoal, 1 );
    self settargetyaw( self.goalyaw );
    helisetteamextractionhud( 3, self.flytime );
    self waittill( "goal" );
    self vehicle_setspeed( self.speed, self.accel );
    self setvehgoalpos( self.pathexit, 1 );
    self settargetyaw( self.goalyaw );
    self waittill( "goal" );
    self stoploopsound();
    helibankplunder();
    helisetteamextractionhud( 0, 0 );
    self notify( "heli_gone" );
    helidelete();
}

helidescend( var_0, var_1 )
{
    self endon( "death" );
    var_2 = var_0[0];
    var_3 = var_0[1];
    var_4 = ( var_2, var_3, var_1 );
    self setvehgoalpos( var_4, 1 );
    self settargetyaw( self.goalyaw );
    self vehicle_setspeed( 25.0, 31.25 );
    thread helidestroyvehiclestouchnotify();
    thread helidestroyvehiclestouchtrace();
    self waittill( "goal" );
    self sethoverparams( 1, 1 );
    wait 1;
    self sethoverparams( 25, 20, 10 );
}

entisalivevehicle()
{
    return isalive( self ) && ( scripts\common\vehicle::isvehicle() || isdefined( self.classname ) && self.classname == "script_vehicle" );
}

helidestroyvehiclestouchnotify()
{
    self endon( "leaving" );
    self endon( "death" );

    for (;;)
    {
        self waittill( "touch", var_0 );

        if ( isdefined( var_0 ) && var_0 entisalivevehicle() )
            var_0 dodamage( var_0.health, self.origin, var_0, var_0, "MOD_CRUSH" );
    }
}

helidestroyvehiclestouchtrace()
{
    self endon( "leaving" );
    self endon( "death" );
    var_0 = 70;
    var_1 = -80;
    var_2 = 150;
    var_3 = 25;
    var_4 = -100;

    for (;;)
    {
        var_5 = getentarrayinradius( "script_vehicle", "classname", self.origin, getdvarfloat( "test_radius", 400 ) );

        if ( var_5.size <= 1 )
        {
            wait 0.5;
            continue;
        }

        var_6 = scripts\engine\trace::create_vehicle_contents();
        var_7 = anglestoforward( self.angles );
        var_8 = self.origin + var_7 * getdvarfloat( "test_f", var_2 ) + ( 0, 0, getdvarfloat( "test_d", var_1 ) );
        var_9 = scripts\engine\trace::sphere_trace( var_8, var_8 + ( 0, 0, 1 ), var_0, self, var_6 );
        var_10 = var_9["entity"];

        if ( isdefined( var_10 ) && var_10 entisalivevehicle() )
        {
            var_10 dodamage( var_10.health, self.origin, var_10, var_10, "MOD_CRUSH" );
            waitframe();
            continue;
        }

        var_8 = self.origin + var_7 * getdvarfloat( "test_m", var_3 ) + ( 0, 0, getdvarfloat( "test_d", var_1 ) );
        var_9 = scripts\engine\trace::sphere_trace( var_8, var_8 + ( 0, 0, 1 ), var_0, self, var_6 );
        var_10 = var_9["entity"];

        if ( isdefined( var_10 ) && var_10 entisalivevehicle() )
        {
            var_10 dodamage( var_10.health, self.origin, var_10, var_10, "MOD_CRUSH" );
            waitframe();
            continue;
        }

        var_8 = self.origin + var_7 * getdvarfloat( "test_b", var_4 ) + ( 0, 0, getdvarfloat( "test_d", var_1 ) );
        var_9 = scripts\engine\trace::sphere_trace( var_8, var_8 + ( 0, 0, 1 ), var_0, self, var_6 );
        var_10 = var_9["entity"];

        if ( isdefined( var_10 ) && var_10 entisalivevehicle() )
        {
            var_10 dodamage( var_10.health, self.origin, var_10, var_10, "MOD_CRUSH" );
            waitframe();
            continue;
        }

        waitframe();
    }
}

tracegroundheight( var_0 )
{
    var_1 = 125;
    var_2 = tracegroundpoint( var_0, 100, [ self ] );
    var_3 = var_2[2];
    var_4 = var_3 + var_1;
    return var_4;
}

tracegroundpoint( var_0, var_1, var_2 )
{
    var_3 = -99999;
    var_4 = ( var_0[0], var_0[1], var_3 );
    var_5 = scripts\engine\trace::create_world_contents();
    var_6 = undefined;

    if ( isdefined( var_1 ) )
        var_6 = scripts\engine\trace::sphere_trace( var_0, var_4, var_1, var_2, var_5 );
    else
        var_6 = scripts\engine\trace::ray_trace( var_0, var_4, var_2, var_5 );

    return var_6["position"];
}

heliwatchgameendleave()
{
    self endon( "death" );
    self endon( "leaving" );
    level waittill( "game_ended" );
    thread helileave();
}

initextractionlocations()
{
    var_0 = [];

    switch ( level.mapname )
    {
        case "mp_br_mechanics":
            var_0[var_0.size] = ( 1500, 1500, 0 );
            var_0[var_0.size] = ( 2500, 1500, 0 );
            var_0[var_0.size] = ( 3500, 1500, 0 );
            var_0[var_0.size] = ( 4500, 1500, 0 );
            break;
        case "mp_donetsk":
            var_0[var_0.size] = ( -33750, -36000, 155 );
            var_0[var_0.size] = ( -41550, -7950, 515 );
            var_0[var_0.size] = ( -37500, 15350, 1130 );
            var_0[var_0.size] = ( -31950, 52015, 2265 );
            var_0[var_0.size] = ( -18510, 64815, 1940 );
            var_0[var_0.size] = ( 15565, 60050, 2680 );
            var_0[var_0.size] = ( 44400, 39255, -50 );
            var_0[var_0.size] = ( 59780, 13800, 555 );
            var_0[var_0.size] = ( 61200, -8445, 30 );
            var_0[var_0.size] = ( 59325, -38390, -210 );
            var_0[var_0.size] = ( 8660, -36630, -640 );
            break;
        case "mp_mb_tut":
            break;
    }

    level.extractionlocations = var_0;
    level.extractingplayers = [];
    level thread monitorextractionlocations();
}

createextractionlocation( var_0 )
{

}

monitorextractionlocations()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );

    foreach ( var_1 in level.extractionlocations )
    {
        var_2 = scripts\mp\objidpoolmanager::requestobjectiveid( 1 );

        if ( var_2 != -1 )
        {
            scripts\mp\objidpoolmanager::objective_add_objective( var_2, "active", var_1, "icon_waypoint_flag" );
            scripts\mp\objidpoolmanager::update_objective_setbackground( var_2, 0 );
            scripts\mp\objidpoolmanager::objective_playermask_hidefromall( var_2 );
            scripts\mp\objidpoolmanager::objective_playermask_showtoall( var_2 );
        }

        level thread spawnextractsmoke( var_1 );
    }

    for (;;)
    {
        foreach ( var_1 in level.extractionlocations )
        {
            var_5 = scripts\mp\utility\player::getplayersinradius( var_1, 300 );

            foreach ( var_7 in var_5 )
            {
                if ( !scripts\engine\utility::array_contains( level.extractingplayers, var_7 ) && !istrue( var_7.extractioncomplete ) )
                    var_7 extactionstart( var_1 );
            }
        }

        foreach ( var_7 in level.extractingplayers )
        {
            if ( distancesquared( var_7.origin, var_7.extractionlocation ) > 90000 )
            {
                var_7 extactioncancel();
                continue;
            }

            var_7.extractiontime = var_7.extractiontime - level.framedurationseconds;

            if ( var_7.extractiontime <= 0.0 )
                var_7 extactioncomplete();
        }

        waitframe();
    }
}

spawnextractsmoke( var_0 )
{
    var_1 = scripts\engine\utility::drop_to_ground( var_0, 50, -200, ( 0, 0, 1 ) );
    var_2 = spawn( "script_model", var_1 + ( 0, 0, 3 ) );
    var_2 setmodel( "scr_smoke_grenade" );
    wait 1.0;
    playfxontag( scripts\engine\utility::getfx( "vfx_smk_signal_red" ), var_2, "tag_fx" );
    var_2 playloopsound( "mp_flare_burn_lp" );
}

extactionstart( var_0 )
{
    self iprintlnbold( "Extraction Start!" );
    level.extractingplayers = scripts\engine\utility::array_add( level.extractingplayers, self );
    self.extractionlocation = var_0;
    self.extractiontime = 10;
    thread showextractiontime();
}

showextractiontime()
{
    self endon( "death_or_disconnect" );
    self endon( "extactionCancel" );

    while ( self.extractiontime > 0 )
    {
        self iprintlnbold( "Extracting in " + scripts\engine\math::round_float( self.extractiontime, 1 ) );
        waitframe();
    }
}

extactioncancel()
{
    self notify( "extactionCancel" );
    self iprintlnbold( "Extraction Canceled!" );
    level.extractingplayers = scripts\engine\utility::array_remove( level.extractingplayers, self );
    self.extractionlocation = undefined;
    self.extractiontime = undefined;
}

extactioncomplete()
{
    self iprintlnbold( "Extraction Complete!" );
    level.extractingplayers = scripts\engine\utility::array_remove( level.extractingplayers, self );
    self.extractioncomplete = 1;
    kick( self getentitynumber(), "EXE/PLAYERKICKED_EXTRACTED" );
}

autoassignquest( var_0 )
{
    if ( !isdefined( level.questinfo.teamsonquests ) || scripts\engine\utility::array_contains( level.questinfo.teamsonquests, var_0.team ) )
        return;

    var_1 = [];

    foreach ( var_3 in level.validautoassignquests )
        var_1 = scripts\engine\utility::array_combine( var_1, getlootscriptablearray( "brloot_" + var_3 + "_tablet" ) );

    var_1 = scripts\engine\utility::array_randomize( var_1 );
    var_5 = undefined;
    var_6 = undefined;

    foreach ( var_10, var_8 in var_1 )
    {
        var_9 = scripts\engine\utility::distance_2d_squared( var_0.origin, var_8.origin );

        if ( var_9 <= 16777216 && var_9 >= 16384 )
        {
            var_0 scripts\mp\gametypes\br_quest_util::takequesttablet( var_8 );
            return;
        }

        if ( !isdefined( var_5 ) || var_9 < var_6 )
        {
            var_5 = var_8;
            var_6 = var_9;
        }
    }

    if ( isdefined( var_5 ) )
        var_0 scripts\mp\gametypes\br_quest_util::takequesttablet( var_5 );
}

missions_clearinappropriaterewards( var_0 )
{
    var_1 = [];

    foreach ( var_4, var_3 in var_0 )
    {
        if ( var_4 == "circle_peek" )
            continue;

        var_1[var_4] = var_3;
    }

    return var_1;
}

handleplunderendgamesplash( var_0 )
{
    var_1 = [];

    if ( isdefined( var_0 ) && var_0 != "tie" )
        var_1 = scripts\mp\utility\teams::getteamdata( var_0, "players" );

    var_2 = scripts\mp\gamelogic::getbrendsplashpostgamestate();

    foreach ( var_4 in level.players )
    {
        if ( var_1.size > 0 && scripts\engine\utility::array_contains( var_1, var_4 ) )
        {
            var_4 setclientomnvar( "post_game_state", var_2 );
            var_4 setclientomnvar( "ui_br_end_game_splash_type", 11 );
            continue;
        }

        var_4 setclientomnvar( "post_game_state", var_2 );
        var_4 setclientomnvar( "ui_br_end_game_splash_type", 12 );
    }
}

updateteamplunderscore( var_0, var_1, var_2 )
{
    var_3 = scripts\mp\gamescore::_getteamscore( var_1 );
    var_4 = var_0 - var_3;

    if ( var_4 != 0 )
    {
        var_5 = scripts\engine\utility::ter_op( scripts\mp\gametypes\br_public::isdmztutorial(), 1, undefined );
        level thread scripts\mp\gamescore::giveteamscoreforobjective( var_1, var_4, 0, undefined, var_5, var_2 );
    }

    return var_4;
}

updatelootleadersonfixedinterval()
{
    level notify( "restartLootLeaders" );
    level endon( "restartLootLeaders" );
    level endon( "game_ended" );
    var_0 = getdvarfloat( "scr_dmz_loot_leader_update_interval", 15.0 );
    var_1 = getdvarfloat( "scr_dmz_loot_leader_update_interval_blink", 5.0 );
    var_2 = getdvarint( "scr_bmo_circle_pulse_start", 800 );
    var_3 = getdvarint( "scr_bmo_circle_pulse_end", 200 );
    var_4 = var_0 - var_1;
    scripts\mp\flags::gameflagwait( "placement_updates_allowed" );

    for (;;)
    {
        level.plunderupdatelootleadermarks = 1;
        scripts\engine\utility::waittill_notify_or_timeout( "bmo_overtime_start", var_4 );

        if ( var_1 > 0 )
        {
            foreach ( var_6 in level.lootleadermarks )
                var_6.mapcircle setmapcirclestyleindex( 2 );

            scripts\engine\utility::waittill_notify_or_timeout( "bmo_overtime_start", var_1 );

            foreach ( var_6 in level.lootleadermarks )
                var_6.mapcircle setmapcirclestyleindex( 0 );
        }
    }
}

updatecirclepulse( var_0, var_1, var_2 )
{
    var_3 = gettime();
    var_2 = var_2 * 1000;
    var_4 = int( var_3 + var_2 );
    var_5 = abs( var_0 - var_1 );

    for (;;)
    {
        var_3 = gettime();
        var_6 = clamp( 1.0 - ( var_4 - var_3 ) / var_2, 0.0, 1.0 );
        var_7 = scripts\engine\utility::ter_op( var_0 < var_1, var_5 * var_6 + var_0, var_0 - var_5 * var_6 );
        setdvar( "PPRTMPMQM", var_7 );

        if ( var_6 == 1.0 )
            break;

        waitframe();
    }
}

updatelootleadermarks( var_0, var_1, var_2, var_3 )
{
    level.lootleadersprev = level.lootleaders;
    level.lootleaders = [];

    if ( level.lootleaderoneperteam )
    {
        if ( istrue( level.bmoovertime ) )
        {
            level.lootleadermarkcount = adjustlootleadermarkcount();

            foreach ( var_5 in level.lootleadermarks )
                var_5.mapcircle hide();
        }
    }

    if ( level.lootleadermarksontopteams == 1 )
    {
        for ( var_7 = 0; var_7 < level.lootleadermarkcount; var_7++ )
        {
            var_8 = getlootteamleader( var_7, var_0, var_3 );

            if ( isdefined( var_8 ) )
            {
                level.lootleaders[level.lootleaders.size] = var_2[var_8];

                if ( istrue( level.expiredlootleaderenabled ) )
                {
                    if ( isdefined( var_2[var_8].expiredlootleaderinstance ) )
                        removeplayerasexpiredlootleader( var_2[var_8].expiredlootleaderinstance );
                }
            }
        }
    }
    else if ( level.lootleadermarksontopteams == 2 )
    {
        var_8 = getlootteamleader( 0, var_0, var_3 );

        if ( isdefined( var_8 ) )
        {
            level.lootleaders[0] = var_2[var_8];

            if ( istrue( level.expiredlootleaderenabled ) )
            {
                if ( isdefined( var_2[var_8].expiredlootleaderinstance ) )
                    removeplayerasexpiredlootleader( var_2[var_8].expiredlootleaderinstance );
            }
        }

        foreach ( var_10 in var_1 )
        {
            var_11 = var_2[var_10];
            var_12 = var_0[var_10];

            if ( var_12 == 0 )
                break;

            if ( scripts\engine\utility::array_contains( level.lootleaders, var_11 ) )
                continue;

            level.lootleaders[level.lootleaders.size] = var_11;

            if ( istrue( level.expiredlootleaderenabled ) )
            {
                if ( isdefined( var_11.expiredlootleaderinstance ) )
                    removeplayerasexpiredlootleader( var_11.expiredlootleaderinstance );
            }

            if ( level.lootleaders.size == level.lootleadermarkcount )
                break;
        }
    }
    else
    {
        foreach ( var_11 in var_1 )
        {
            var_12 = var_0[var_11];

            if ( var_12 == 0 )
                break;

            level.lootleaders[level.lootleaders.size] = var_2[var_11];

            if ( istrue( level.expiredlootleaderenabled ) )
            {
                if ( isdefined( var_2[var_11].expiredlootleaderinstance ) )
                    removeplayerasexpiredlootleader( var_2[var_11].expiredlootleaderinstance );
            }

            if ( level.lootleaders.size == level.lootleadermarkcount )
                break;
        }
    }

    foreach ( var_11 in level.lootleadersprev )
    {
        if ( isdefined( var_11 ) && !scripts\engine\utility::array_contains( level.lootleaders, var_11 ) )
            removeplayeraslootleader( var_11 );
    }

    for ( var_7 = 0; var_7 < level.lootleadermarkcount; var_7++ )
    {
        var_18 = level.lootleadermarks[var_7];

        if ( isdefined( level.lootleaders[var_7] ) )
        {
            var_18.targetplayer = level.lootleaders[var_7];
            var_18.targetplayer.lootleaderinstance = var_18;
            var_19 = ( var_18.targetplayer.origin[0], var_18.targetplayer.origin[1], level.lootleadermarksize );

            if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
                var_19 = var_19 + scripts\engine\math::random_vector_2d() * randomfloatrange( 100, 1000 );

            var_18 scripts\mp\gametypes\br_quest_util::movequestcircle( var_19 );

            if ( istrue( level.lootleadermarksizedynamic ) )
                updatelootleadercirclesize( var_18 );

            var_20 = scripts\engine\utility::array_contains( level.lootleaders, var_18.targetplayer ) && !scripts\engine\utility::array_contains( level.lootleadersprev, var_18.targetplayer );
            var_21 = scripts\mp\utility\teams::getenemyplayers( var_18.targetplayer.team, 0 );

            foreach ( var_11 in var_21 )
                var_18 scripts\mp\gametypes\br_quest_util::showquestcircletoplayer( var_11 );

            var_24 = scripts\mp\utility\teams::getfriendlyplayers( var_18.targetplayer.team, 0 );

            foreach ( var_11 in var_24 )
                var_18 scripts\mp\gametypes\br_quest_util::hidequestcirclefromplayer( var_11 );

            if ( var_20 )
                addplayeraslootleader( var_18 );

            continue;
        }

        var_18.targetplayer = undefined;

        foreach ( var_11 in level.players )
            var_18 scripts\mp\gametypes\br_quest_util::hidequestcirclefromplayer( var_11 );
    }
}

getlootteamleader( var_0, var_1, var_2 )
{
    var_3 = var_2[var_0];
    var_4 = 0;
    var_5 = 0;

    if ( level.teamdata[var_3]["players"].size == 0 )
        return undefined;

    var_6 = level.teamdata[var_3]["players"][0].guid;

    foreach ( var_8 in level.teamdata[var_3]["players"] )
    {
        var_4 = var_1[var_8.guid];

        if ( var_4 > var_5 )
        {
            var_5 = var_4;
            var_6 = var_8.guid;
        }
    }

    return var_6;
}

addplayeraslootleader( var_0 )
{
    var_1 = var_0.targetplayer;

    if ( scripts\mp\utility\player::isreallyalive( var_1 ) )
    {
        if ( !scripts\mp\gametypes\br_public::isdmztutorial() )
            var_1 scripts\mp\hud_message::showsplash( "bm_player_marked" );

        var_1 cashleaderbag_attach();

        if ( istrue( level.expiredlootleaderenabled ) )
            var_1 thread watchforplayerdeath( var_0 );
    }
}

removeplayeraslootleader( var_0 )
{
    if ( isdefined( var_0.attached_bag ) )
        var_0 cashleaderbag_detach();

    if ( isdefined( var_0.lootleaderinstance ) )
    {
        foreach ( var_2 in level.players )
            var_0.lootleaderinstance scripts\mp\gametypes\br_quest_util::hidequestcirclefromplayer( var_2 );

        var_4 = var_0.lootleaderinstance.circleorigin;

        if ( istrue( level.expiredlootleaderenabled ) )
            addplayerasexpiredlootleader( var_0, var_4 );

        var_0.lootleaderinstance.targetplayer = undefined;
        var_0.lootleaderinstance = undefined;
    }

    level.lootleaders = scripts\engine\utility::array_remove( level.lootleaders, var_0 );
}

watchforplayerdeath( var_0 )
{
    self endon( "disconnect" );
    self waittill( "death" );
    var_1 = self.lootleaderinstance.circleorigin;
    removeplayeraslootleader( self );
    addplayerasexpiredlootleader( self, var_1 );
}

addplayerasexpiredlootleader( var_0, var_1 )
{
    if ( scripts\engine\utility::array_contains( level.expiredlootleaders, var_0 ) )
        return;

    level.expiredlootleaders = scripts\engine\utility::array_add( level.expiredlootleaders, var_0 );
    var_2 = undefined;

    foreach ( var_4 in level.expiredlootleadermarks )
    {
        if ( !isdefined( var_4.targetplayer ) )
        {
            var_2 = var_4;
            break;
        }
    }

    if ( !isdefined( var_2 ) )
    {
        var_6 = undefined;

        foreach ( var_4 in level.expiredlootleadermarks )
        {
            if ( !isdefined( var_6 ) || var_4.lastusedtime < var_6 )
            {
                var_6 = var_4.lastusedtime;
                var_2 = var_4;
            }
        }

        var_2.targetplayer notify( "stop_update" );
    }

    var_2.targetplayer = var_0;
    var_2.lastusedtime = gettime();
    var_0.expiredlootleaderinstance = var_2;

    if ( isdefined( var_1 ) )
        var_2 scripts\mp\gametypes\br_quest_util::movequestcircle( var_1 );
    else
    {
        var_9 = ( var_2.targetplayer.origin[0], var_2.targetplayer.origin[1], level.lootleadermarksize );
        var_9 = var_9 + scripts\engine\math::random_vector_2d() * randomfloatrange( 100, 1000 );
        var_2 scripts\mp\gametypes\br_quest_util::movequestcircle( var_9 );
    }

    if ( istrue( level.lootleadermarksizedynamic ) )
        updatelootleadercirclesize( var_2 );

    var_10 = scripts\mp\utility\teams::getenemyplayers( var_2.targetplayer.team, 0 );

    foreach ( var_4 in var_10 )
        var_2 scripts\mp\gametypes\br_quest_util::showquestcircletoplayer( var_4 );

    var_13 = scripts\mp\utility\teams::getfriendlyplayers( var_2.targetplayer.team, 0 );

    foreach ( var_4 in var_13 )
        var_2 scripts\mp\gametypes\br_quest_util::hidequestcirclefromplayer( var_4 );

    thread updateexpiredlootleader( var_2 );
}

updateexpiredlootleader( var_0 )
{
    var_0.targetplayer endon( "stop_update" );
    wait 5.0;
    removeplayerasexpiredlootleader( var_0 );
}

removeplayerasexpiredlootleader( var_0 )
{
    var_1 = var_0.targetplayer;
    level.expiredlootleaders = scripts\engine\utility::array_remove( level.expiredlootleaders, var_0.targetplayer );
    var_0.targetplayer.expiredlootleaderinstance = undefined;
    var_0.targetplayer = undefined;

    foreach ( var_3 in level.players )
        var_0 scripts\mp\gametypes\br_quest_util::hidequestcirclefromplayer( var_3 );

    var_1 notify( "stop_update" );
}

cashleaderbag_attach()
{
    self attach( "accessory_money_bag_large_closed_player", "tag_stowed_back3", 1 );
    self.attached_bag = "accessory_money_bag_large_closed_player";

    if ( self tagexists( "j_bag_left" ) )
        playfxontag( level._effect["vfx_br_cashLeaderBag"], self, "j_bag_left" );

    thread cashleader_trackdeath();
}

cashleaderbag_detach()
{
    if ( isdefined( self ) && isdefined( self.attached_bag ) )
    {
        if ( self tagexists( "j_bag_left" ) )
            stopfxontag( level._effect["vfx_br_cashLeaderBag"], self, "j_bag_left" );

        self detach( self.attached_bag, "tag_stowed_back3" );
        self.attached_bag = undefined;
    }

    self notify( "killthread_bagModelSwap" );
}

cashleader_trackdeath()
{
    self notify( "cashleader_trackDeath" );
    self endon( "cashleader_trackDeath" );
    self endon( "killthread_bagModelSwap" );
    self waittill( "death" );
    cashleaderbag_detach();
}

packextrascore0( var_0 )
{
    var_1 = 0;
    var_2 = 0;

    if ( isdefined( self.plundercount ) )
        var_2 = var_2 + self.plundercount;

    if ( isdefined( self.plunderbanked ) )
        var_2 = var_2 + self.plunderbanked;

    var_2 = int( var_2 / 10 );

    if ( var_2 > 4095 )
        var_2 = 4095;

    var_1 = var_2;
    var_3 = 0;

    if ( isdefined( self.missionparticipation ) )
        var_3 = var_3 + self.missionparticipation;

    if ( var_3 > 15 )
        var_3 = 15;

    var_1 = var_1 + ( var_3 << 12 );
    scripts\mp\utility\stats::setextrascore0( var_1 );
}

packclientmatchdata()
{
    var_0 = 0;
    var_1 = 0;

    if ( isdefined( self.plundercount ) )
        var_1 = var_1 + self.plundercount;

    if ( isdefined( self.plunderbanked ) )
        var_1 = var_1 + self.plunderbanked;

    var_1 = int( var_1 / 10 );

    if ( var_1 > 4095 )
        var_1 = 4095;

    var_0 = var_1;
    var_2 = 0;

    if ( isdefined( self.missionparticipation ) )
        var_2 = var_2 + self.missionparticipation;

    if ( var_2 > 15 )
        var_2 = 15;

    var_0 = var_0 + ( var_2 << 12 );
    return var_0;
}

playerlootleadervalidity()
{
    self endon( "disconnect" );

    if ( isdefined( level.lootleaders ) && scripts\engine\utility::array_contains( level.lootleaders, self ) )
    {
        scripts\mp\hud_message::showsplash( "bm_player_marked" );
        cashleaderbag_attach();
    }
}

updatelootleadercirclesize( var_0 )
{
    if ( isdefined( var_0.targetplayer.plundercount ) )
        var_1 = var_0.targetplayer.plundercount;
    else
        var_1 = 0;

    var_1 = clamp( var_1, level.lootleadermarkweakvalue, level.lootleadermarkstrongvalue );
    var_2 = level.lootleadermarkstrongvalue - level.lootleadermarkweakvalue;
    var_3 = ( var_1 - level.lootleadermarkweakvalue ) / var_2;
    var_4 = level.lootleadermarkweaksize - level.lootleadermarkstrongsize;
    var_5 = level.lootleadermarkweaksize - var_3 * var_4;
    var_0 scripts\mp\gametypes\br_quest_util::setmapcirclesize( var_5 );
}

adjustlootleadermarkcount( var_0 )
{
    var_1 = 0;
    var_0 = scripts\mp\gamescore::getteamscoreplacements();
    var_2 = getwincost();

    for ( var_3 = 1; var_3 < level.maxlootleadermarkcount + 1; var_3++ )
    {
        foreach ( var_5 in level.teamnamelist )
        {
            if ( var_0[var_5] != var_3 )
                continue;

            var_6 = getteamplunder( var_5 ) * 100;

            if ( var_6 >= var_2 || var_0[var_5] == 1 )
                var_1++;
        }
    }

    if ( var_1 > level.maxlootleadermarkcount )
        var_1 = level.maxlootleadermarkcount;

    return var_1;
}

displayplunderloss( var_0, var_1 )
{
    var_2 = 0;
    var_3 = 0;
    var_4 = 20;
    var_1 = var_1 * 100;
    var_5 = var_0.plundercount * 100;
    var_6 = var_5 + var_1;
    var_7 = var_6;
    var_8 = var_5;
    var_9 = var_8 - var_6;
    var_10 = scripts\engine\utility::sign( var_9 );
    var_11 = var_9 / 2.0;
    var_12 = int( var_11 * 2 * level.framedurationseconds );

    if ( var_12 == 0 )
        return;

    var_13 = createhudelem( &"MP_BR_INGAME/PLUNDER_DEATH_LOSS", var_1, "RIGHT", "CENTER", var_2 + 46, var_3, undefined, undefined, 1.25, var_0 );
    var_13.alpha = 1;
    var_14 = createhudelem( &"MP_BR_INGAME/YOUR_PLUNDER_TEXT", undefined, "RIGHT", "CENTER", var_2, var_3 + var_4, undefined, undefined, 1.25, var_0 );
    var_14.alpha = 1;
    var_15 = createhudelem( &"MP_BR_INGAME/EXTRACT_PLUNDER", var_6, "LEFT", "CENTER", var_2 + 45, var_3 + var_4, undefined, undefined, 1.25, var_0 );
    var_15.alpha = 1;
    wait 1.0;

    while ( var_7 != var_8 )
    {
        var_7 = var_7 + var_12;

        if ( var_10 > 0 && var_7 > var_8 || var_10 < 0 && var_7 < var_8 )
            var_7 = var_8;

        var_15 setvalue( var_7 );
        wait( level.framedurationseconds );
    }

    wait 3.0;
    var_13 destroy();
    var_14 destroy();
    var_15 destroy();
}

_getplunderextractlocations( var_0, var_1, var_2 )
{
    if ( var_0.size == 0 || var_1 == 0 )
        return;

    var_3 = [];
    var_4 = [];

    if ( var_0.size > 0 )
    {
        var_0 = scripts\engine\utility::array_randomize( var_0 );
        var_4 = int( min( var_1, var_0.size ) );
    }

    if ( level.setupevenlydistributedpads )
    {
        var_5 = 0;

        if ( level.adjusteventdistributionpadding > 0 && istrue( var_2 ) )
        {
            var_6 = 0;

            for ( var_7 = 0; var_7 < var_4; var_7++ )
            {
                if ( var_6 > level.paddedquadgridcenterpoints.size - 1 )
                    var_6 = 0;

                var_8 = level.paddedquadgridcenterpoints[var_6];

                foreach ( var_10 in var_0 )
                {
                    if ( pointinsquare( var_10.origin, var_8, level.pointinsquarewidth ) )
                    {
                        var_3[var_3.size] = var_10;
                        var_0 = scripts\engine\utility::array_remove( var_0, var_10 );
                        break;
                    }
                }

                var_6++;
            }
        }
        else
        {
            foreach ( var_10 in var_0 )
            {
                var_5 = 0;

                foreach ( var_14 in level.quadgridcenterpoints )
                {
                    var_15 = scripts\engine\utility::distance_2d_squared( var_14, var_10.origin );

                    if ( var_5 == 0 || var_15 < var_5 )
                    {
                        var_5 = var_15;
                        var_10.closestquad = var_14;
                    }
                }
            }

            var_6 = 0;

            for ( var_7 = 0; var_7 < var_4; var_7++ )
            {
                if ( var_6 > level.quadgridcenterpoints.size - 1 )
                    var_6 = 0;

                var_8 = level.quadgridcenterpoints[var_6];

                foreach ( var_10 in var_0 )
                {
                    if ( isdefined( var_10.closestquad ) && var_10.closestquad == var_8 )
                    {
                        var_3[var_3.size] = var_10;
                        var_0 = scripts\engine\utility::array_remove( var_0, var_10 );
                        break;
                    }
                }

                var_6++;
            }
        }
    }
    else if ( var_0.size > 0 )
    {
        var_0 = scripts\engine\utility::array_randomize( var_0 );
        var_4 = int( min( var_1, var_0.size ) );

        for ( var_7 = 0; var_7 < var_4; var_7++ )
            var_3[var_3.size] = var_0[var_7];
    }

    if ( istrue( level.hideallunselectedextractpads ) )
    {
        foreach ( var_21 in var_0 )
        {
            if ( !scripts\engine\utility::array_contains( var_3, var_21 ) )
                var_21 setscriptablepartstate( var_21.type, "hidden" );
        }
    }

    return var_3;
}

pointinsquare( var_0, var_1, var_2 )
{
    var_3 = var_1[0] - var_2;
    var_4 = var_1[0] + var_2;
    var_5 = var_1[1] - var_2;
    var_6 = var_1[1] + var_2;
    return var_0[0] >= var_3 && var_0[0] <= var_4 && var_0[1] >= var_5 && var_0[1] <= var_6;
}

_getsingleplunderextractlocations( var_0, var_1 )
{
    if ( var_0.size == 0 || var_1 == 0 )
        return;

    var_2 = undefined;

    if ( var_0.size > 0 )
    {
        var_0 = scripts\engine\utility::array_randomize( var_0 );

        for ( var_3 = 0; var_3 < var_0.size; var_3++ )
        {
            if ( !scripts\engine\utility::array_contains( level.br_plunder_sites, var_0[var_3] ) )
            {
                var_2 = var_0[var_3];
                break;
            }
        }
    }

    return var_2;
}

findnewplunderextractsite( var_0 )
{
    var_1 = _getsingleplunderextractlocations( scripts\mp\gametypes\br_plunder::getallextractspawninstances(), level.maxplunderextractions );
    var_1.disabled = undefined;
    var_1.helidisabled = undefined;
    var_2 = scripts\engine\utility::ter_op( level.showplunderextracticonsinworld && !level.shownonscriptableextracticons, var_1.activecurrstate, var_1.activestate );
    var_1 setscriptablepartstate( var_1.type, var_2 );

    if ( level.showplunderextracticonsinworld && level.shownonscriptableextracticons )
    {
        scripts\mp\objidpoolmanager::returnobjectiveid( var_0.locale.objectiveiconid );
        var_0.locale.objectiveiconid = -1;
        var_3 = spawnstruct();
        var_3 createquestobjicon( "ui_mp_br_mapmenu_icon_atm", "current", var_1.origin + ( 0, 0, 200 ) );
        var_1.locale = var_3;
    }

    level.br_plunder_sites = scripts\engine\utility::array_remove( level.br_plunder_sites, var_0 );
    level.br_plunder_sites = scripts\engine\utility::array_add( level.br_plunder_sites, var_1 );
}

startplunderextractiontimers()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );
    waitframe();

    foreach ( var_1 in level.br_plunder_sites )
    {
        var_1.disabled = undefined;
        var_2 = scripts\engine\utility::ter_op( level.showplunderextracticonsinworld && !level.shownonscriptableextracticons, var_1.activecurrstate, var_1.activestate );
        var_1 setscriptablepartstate( var_1.type, var_2 );
        var_1 thread runplunderextractsitetimer();

        if ( level.showplunderextracticonsinworld && level.shownonscriptableextracticons )
            var_1.locale thread timeoutradialunfill();
    }

    level thread runextractreroll();
}

runplunderextractsitetimer()
{
    wait( level.timeoutplunderextractionsites );
    self.disabled = 1;
    self.helidisabled = 1;
    self setscriptablepartstate( self.type, self.disabledstate );
}

runextractreroll()
{
    wait( level.timeoutplunderextractionsites + 1 );
    level.br_plunder_sites = _getplunderextractlocations( scripts\mp\gametypes\br_plunder::getallextractspawninstances(), level.maxplunderextractions );

    if ( level.showplunderextracticonsinworld && level.shownonscriptableextracticons )
        level thread createheliextractobjectiveicons();

    level thread startplunderextractiontimers();
}

createheliextractobjectiveicons()
{
    scripts\mp\flags::gameflagwait( "prematch_done" );

    foreach ( var_1 in level.br_plunder_sites )
    {
        var_2 = spawnstruct();
        var_2 createquestobjicon( "ui_mp_br_mapmenu_icon_atm", "current", var_1.origin + ( 0, 0, 200 ) );
        var_1.locale = var_2;
    }
}

timeoutradialunfill()
{
    var_0 = level.timeoutplunderextractionsites;
    var_1 = level.framedurationseconds;
    var_2 = var_1 * 1000;
    var_3 = var_0 * 1000;
    var_4 = var_3 - var_2;
    var_5 = gettime() + var_3;

    while ( gettime() < var_5 )
    {
        var_6 = var_4 / var_3;
        scripts\mp\objidpoolmanager::objective_show_progress( self.objectiveiconid, 1 );
        scripts\mp\objidpoolmanager::objective_set_progress( self.objectiveiconid, var_6 );
        var_4 = max( var_4 - var_2, 1 );
        waitframe();
    }

    scripts\mp\objidpoolmanager::returnobjectiveid( self.objectiveiconid );
    self.objectiveiconid = -1;
}

createquestobjicon( var_0, var_1, var_2 )
{
    self.objectiveiconid = scripts\mp\objidpoolmanager::requestobjectiveid( 1 );

    if ( self.objectiveiconid != -1 )
    {
        scripts\mp\objidpoolmanager::objective_add_objective( self.objectiveiconid, var_1, ( 0, 0, 0 ), var_0 );
        scripts\mp\objidpoolmanager::update_objective_setbackground( self.objectiveiconid, 1 );
        objective_showtoplayersinmask( self.objectiveiconid );
        scripts\mp\objidpoolmanager::objective_set_play_intro( self.objectiveiconid, 1 );

        if ( isdefined( var_2 ) )
            movequestobjicon( var_2 );
    }
    else
    {

    }
}

movequestobjicon( var_0 )
{
    scripts\mp\objidpoolmanager::update_objective_position( self.objectiveiconid, var_0 );
}

showquestobjicontoplayer( var_0 )
{
    objective_addclienttomask( self.objectiveiconid, var_0 );
}

showquestobjicontoall( var_0 )
{
    objective_addalltomask( var_0 );
}

hidequestobjiconfromplayer( var_0 )
{
    objective_removeclientfrommask( self.objectiveiconid, var_0 );
}

deletequestobjicon()
{
    scripts\mp\objidpoolmanager::objective_playermask_hidefromall( self.objectiveiconid );
    scripts\mp\objidpoolmanager::returnobjectiveid( self.objectiveiconid );
}

startbmoexfilprocess( var_0 )
{
    level setexfiltimer();
    level thread showexfilstartsplash( var_0 );

    if ( level.teamplunderexfilvipuav )
        level thread startvipteamuav( var_0 );

    if ( level.teamplunderexfilshowviponly )
        level thread removenonvipteamlocations();

    level thread startexfilchoppers();
    level thread nuke_startexfilcountdown( var_0 );
}

setexfiltimer()
{
    scripts\mp\gamelogic::resumetimer();
    level.starttime = gettime();
    level.discardtime = 0;
    level.timerpausetime = 0;
    var_0 = getdvarfloat( "scr_bmo_exfil_timer", 180 );
    var_1 = "scr_" + scripts\mp\utility\game::getgametype() + "_timelimit";
    level.watchdvars[var_1].value = var_0;
    level.overridewatchdvars[var_1] = var_0;
}

showexfilstartsplash( var_0 )
{
    foreach ( var_2 in level.teamnamelist )
        scripts\mp\gametypes\br_public::brleaderdialogteam( "plunder_extract_requested", var_2 );

    foreach ( var_5 in level.players )
    {
        var_5 thread scripts\mp\hud_message::showsplash( "callout_bmo_exfil_winners" );
        var_5 scripts\mp\utility\lower_message::setlowermessageomnvar( 71, undefined, 20 );
    }
}

startvipteamuav( var_0 )
{
    level.radarmode[var_0] = "normal_radar";
    level.activeuavs[var_0] = 1;
    level.activeadvanceduavs[var_0] = 0;
    scripts\cp_mp\killstreaks\uav::_setteamradarstrength( var_0, 4 );
}

removenonvipteamlocations()
{
    level.lootleadermarkcount = 1;

    if ( getdvarint( "scr_dmz_loot_leader_update_on_pickup", 0 ) == 1 )
    {
        if ( getdvarfloat( "scr_dmz_loot_leader_update_interval", 15.0 ) > 0.0 )
            level thread updatelootleadersonfixedinterval();
    }
}

startexfilchoppers()
{
    foreach ( var_1 in level.br_plunder_sites )
    {
        var_1 setscriptablepartstate( var_1.type, "inuse" );
        var_2 = getgroundposition( var_1.origin, 1 ) + ( 0, 0, 2 );
        var_3 = level.players[0];

        for ( var_4 = 0; var_4 < 200; var_4++ )
        {
            var_3 = findanyaliveplayer();

            if ( isplayer( var_3 ) )
                break;
        }

        var_2 = getgroundposition( var_1.origin, 1 ) + ( 0, 0, 2 );
        var_5 = var_3 playerspawnexfilchopper( var_2, var_1 );

        if ( isdefined( var_5 ) )
        {
            var_5.site = var_1;
            var_1.heli = var_5;
            var_5 helicreateextractvfx( var_2 );
            var_5 thread extractplayers();
        }
    }
}

playerspawnexfilchopper( var_0, var_1 )
{
    var_2 = var_0;
    var_3 = var_2 + ( 0, 0, 2500 );
    var_4 = findclearflightyaw( var_3, var_1 );
    var_5 = ( 0, var_4, 0 );

    if ( getdvarint( "scr_br_plunder_heli_adjust_bag", 1 ) == 1 )
    {
        var_6 = -100;
        var_7 = 60;
        var_8 = anglestoforward( var_5 );
        var_9 = anglestoright( var_5 );
        var_2 = var_2 + var_8 * var_6 + var_9 * var_7;
    }

    var_10 = var_3 + -1 * anglestoforward( var_5 ) * 30000;
    var_11 = var_3 + anglestoforward( var_5 ) * 30000;
    var_12 = spawnheli( self, var_10, var_3, var_11 );
    return var_12;
}

extractplayers()
{
    self endon( "death" );
    self endon( "leaving" );
    self setvehgoalpos( self.pathgoal, 1 );
    self settargetyaw( self.goalyaw );
    var_0 = tracegroundheightexfil( self.pathgoal );
    var_1 = self.pathgoal[2] - var_0;
    self.flytime = calculatehelitimetoarrive( var_1 );
    helisetteamextractionhud( 1, self.flytime );
    self waittill( "goal" );

    foreach ( var_3 in level.players )
        var_3 scripts\mp\utility\lower_message::setlowermessageomnvar( 72, undefined, 20 );

    thread heliwatchgameendleave();
    thread helidisapateextractvfx();
    helidescend( self.endpoint, var_0 );

    foreach ( var_6 in level.teamnamelist )
        level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "plunder_extract_chopper_arrive", var_6, 1 );

    helimakeexfilwait();
    helicleanupextract();

    foreach ( var_6 in level.teamnamelist )
        level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "plunder_extract_chopper_leave", var_6, 1 );

    thread helileave();
}

helimakeexfilwait()
{
    self.isdepot = 1;
    self.usable = self.crate;
    var_0 = self.usable;
    var_0 makeusable();
    var_0 setcursorhint( "HINT_NOICON" );
    var_0 setuseholdduration( "duration_medium" );
    var_0 sethintrequiresholding( 1 );
    var_0 setuserange( 230 );
    var_0 sethintstring( &"MP/BR_USE_EXFIL_CHOPPER" );
    var_1 = level.br_depots.size;
    level.br_depots[var_1] = var_0;

    foreach ( var_3 in level.players )
    {
        if ( !isdefined( var_3 ) )
            continue;
    }

    thread helicleanupdepotonleaving();
    thread heliexfilthink( var_0 );
    helisetteamextractionhud( 2, 300 );
    wait 300;
    self.isdepot = 0;
    heliusecleanup();
}

heliexfilthink( var_0 )
{
    self endon( "death" );
    var_0 endon( "death" );

    for (;;)
    {
        var_0 waittill( "trigger", var_1 );
        var_1 scripts\mp\hud_message::showsplash( "callout_exfil_success" );
        var_1 playerhide();
        var_1 sethidenameplate( 0 );
        var_1 allowmovement( 0 );
        var_1 allowfire( 0 );
        var_1 disableoffhandprimaryweapons( 0 );
        var_1 disableoffhandsecondaryweapons( 0 );
        var_1 disableweapons( 0 );
        var_1 disableweaponswitch( 0 );
        var_1 setcamerathirdperson( 1 );
        var_1 allowcrouch( 0 );
        var_1 allowmelee( 0 );
        var_1 allowjump( 0 );
        var_1 allowprone( 0 );
        var_1 scripts\common\utility::allow_killstreaks( 0 );
        var_1 scripts\common\utility::allow_supers( 0 );
        var_1.safefromnuke = 1;
        var_0 disableplayeruse( var_1 );
    }
}

healdamage()
{
    self endon( "death" );
    self endon( "leaving" );
    self endon( "swapped" );

    for (;;)
    {
        self waittill( "damage", var_0, var_1, var_2, var_3, var_4, var_5, var_6, var_7, var_8, var_9, var_10, var_11, var_12, var_13 );
        self.health = 99999;
    }
}

tracegroundheightexfil( var_0 )
{
    var_1 = 256;
    var_2 = tracegroundpoint( var_0, 100, [ self ] );
    var_3 = var_2[2];
    var_4 = var_3 + var_1;
    return var_4;
}

findclearflightyaw( var_0, var_1 )
{
    if ( isdefined( var_1 ) && isdefined( var_1.flightyaw ) )
        return var_1.flightyaw;

    var_2 = 10;
    var_3 = scripts\engine\trace::create_world_contents();

    for ( var_4 = 0; var_4 < 360; var_4 = var_4 + var_2 )
    {
        var_5 = ( 0, var_4, 0 );
        var_6 = var_0 + -1 * anglestoforward( var_5 ) * 30000;
        var_7 = var_0 + anglestoforward( var_5 ) * 30000;
        var_8 = scripts\engine\trace::sphere_trace( var_0, var_7, 100, undefined, var_3, 1 );

        if ( var_8["fraction"] == 1 )
        {
            if ( isdefined( var_1 ) )
                var_1.flightyaw = var_4;

            return var_4;
        }

        if ( var_4 % 3 == 0 )
            waitframe();
    }

    var_4 = randomfloat( 360 );

    if ( isdefined( var_1 ) )
        var_1.flightyaw = var_4;

    return var_4;
}

nuke_startexfilcountdown( var_0 )
{
    level notify( "mercy_ending_timer_started" );
    level endon( "mercy_ending_triggered" );
    setomnvar( "ui_nuke_timer_type", 1 );
    setomnvar( "ui_nuke_timer_time", level.teamplunderexfiltimer );
    setomnvar( "ui_nuke_countdown_active", 1 );
    var_1 = gettime();
    var_2 = level.teamplunderexfiltimer * 1000 + var_1;
    setomnvar( "ui_nuke_end_milliseconds", level.teamplunderexfiltimer * 1000 + var_1 );

    for (;;)
    {
        waitframe();

        if ( gettime() > var_2 )
            break;
    }

    level thread scripts\mp\gamelogic::endgame( var_0, game["end_reason"]["dmz_plunder_win"], game["end_reason"]["dmz_plunder_loss"], 0, 1 );
    setomnvar( "ui_nuke_countdown_active", 0 );
}

triggerovertimetimer( var_0, var_1, var_2 )
{
    if ( istrue( level.bmoovertime ) )
    {
        if ( istrue( level.bankingoverlimitwillendot ) && istrue( var_1 ) )
            level thread bmoendgameot();

        return;
    }

    level.bmoovertime = 1;
    level notify( "cancel_announcer_dialog" );
    thread scripts\mp\music_and_dialog::plunder_overtime_music();
    level thread overtimecashmultiplier();
    var_3 = scripts\engine\utility::ter_op( level.overtimecashmultiplier >= 1, "bm_overtime_double_cash_num", "bm_overtime_double_cash_perc" );

    if ( isdefined( var_0 ) && ( level.disablewinonscore || !level.disableonemilannounce ) )
    {
        level.initialwinningteam = var_0;
        showsplashtoallexceptteam( var_0, "bm_overtime_start_them" );
        showsplashtoteam( var_0, "bm_overtime_start_us" );
        level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "bm_gamestate_overtime", var_0 );

        foreach ( var_5 in level.teamnamelist )
        {
            if ( var_5 != var_0 )
                level thread scripts\mp\gametypes\br_public::brleaderdialogteam( "bm_gamestate_overtime_enemy", var_5 );
        }
    }
    else
        level.initialwinningteam = getinitialwinningteam();

    if ( !isdefined( var_2 ) )
    {
        scripts\mp\gamelogic::resumetimer();
        level.starttime = gettime();
        level.discardtime = 0;
        level.timerpausetime = 0;
        var_7 = scripts\engine\utility::ter_op( level.disableonemilannounce || istrue( var_2 ), 7, 12 );
        var_8 = "scr_" + scripts\mp\utility\game::getgametype() + "_timelimit";
        level.watchdvars[var_8].value = var_7;
        level.overridewatchdvars[var_8] = var_7;

        if ( !level.disableonemilannounce )
            wait 5;

        showsplashtoall( var_3 );
        wait 5;
        level.ontimelimitgraceperiod = level.doendofmatchotsequence;
        level.currenttimelimitdelay = 0.0;
        level.canprocessot = 1;
        level notify( "bmo_overtime_start" );
    }
    else
    {
        level.ontimelimitgraceperiod = level.doendofmatchotsequence;
        level.currenttimelimitdelay = 0.0;
        level.canprocessot = 1;
        level notify( "bmo_overtime_start" );
        level.forcehideottimer = 1;
        level.forceotlogictorun = 1;
        showsplashtoall( var_3 );
        wait 5;
    }

    level.forcehideottimer = undefined;
    scripts\mp\flags::gameflagwait( "overtime_started" );
    setomnvar( "ui_br_circle_state", 7 );

    if ( !isdefined( var_2 ) )
    {
        var_9 = gettime() + int( level.doendofmatchotsequence * 1000 );
        setomnvar( "ui_hardpoint_timer", var_9 );
    }

    wait( int( max( level.doendofmatchotsequence - 60, 0 ) ) );
    setomnvar( "ui_br_circle_state", 8 );
}

getinitialwinningteam()
{
    var_0 = "";

    foreach ( var_2 in level.teamnamelist )
    {
        var_3 = game["teamPlacements"][var_2];

        if ( var_3 == 1 )
        {
            var_0 = var_2;
            break;
        }
    }

    return var_0;
}

ontimelimit()
{
    if ( level.teamplunderscoremechanic && level.doendofmatchotsequence > 0 && !istrue( level.bmoovertime ) )
    {
        if ( !isdefined( level.initialwinningteam ) )
            level.initialwinningteam = getinitialwinningteam();

        level thread triggerovertimetimer( level.initialwinningteam, level.teamdata[level.initialwinningteam]["plunderBanked"] * 1000 >= getwincost() );
        level waittill( "bmo_overtime_start" );

        while ( level.currenttimelimitdelay < level.ontimelimitgraceperiod )
            wait( level.framedurationseconds );
    }

    level thread bmoendgameot();
}

bmoendgameot()
{
    if ( istrue( level.gameended ) )
        return;

    level thread handlebmoendgamesplash();
    level thread scripts\mp\gamelogic::endgame( level.brgametype.firstteam, game["end_reason"]["dmz_plunder_win"], game["end_reason"]["dmz_plunder_loss"], 0, 1 );
}

handlebmoendgamesplash()
{
    var_0 = scripts\mp\gamelogic::getbrendsplashpostgamestate();

    foreach ( var_2 in level.players )
        var_2 setclientomnvar( "post_game_state", var_0 );
}

playerspawnendofgame()
{
    if ( !istrue( self.controlsfrozen ) )
        scripts\mp\utility\player::_freezecontrols( 1 );

    var_0 = spawnstruct();
    var_1 = self getspectatingplayer();

    if ( !isdefined( var_1 ) )
        var_1 = self;

    var_0.origin = var_1.origin;
    var_0.angles = var_1.angles;

    if ( !var_1 isonground() )
    {
        var_2 = scripts\engine\trace::create_default_contents( 1 );
        var_0.origin = scripts\engine\utility::drop_to_ground( var_0.origin, 0, -20000, undefined, var_2 );
    }

    var_0.origin = var_0.origin + ( 0, 0, 100 );

    if ( !isdefined( level.endmatchcamerastriggered ) )
    {
        level.endmatchcamerastriggered = 1;
        level thread triggerbloodmoneyendcameras();
    }

    return 1;
}

eomawardplayerxp()
{
    level waittill( "give_match_bonus" );
    waitframe();
    var_0 = getdvarfloat( "scr_bmo_eom_held_cash_scalar", 1.0 );
    var_1 = getdvarfloat( "scr_bmo_eom_banked_cash_scalar", 1.0 );
    var_2 = getdvarint( "scr_bmo_eom_initial_winner_bonus", 10000 );
    var_3 = getdvarint( "scr_bmo_eom_over_wincost_bonus", 7500 );
    var_4 = getdvarint( "scr_bmo_eom_top10_bonus", 5000 );

    foreach ( var_6 in level.teamnamelist )
    {
        var_7 = getteamplunder( var_6 ) * 100;
        var_8 = var_7 >= getwincost();
        var_9 = game["teamPlacements"][var_6];
        var_10 = 0;

        if ( var_8 )
            var_10 = var_3;
        else if ( var_9 <= 10 )
            var_10 = var_4;

        var_11 = scripts\mp\utility\teams::getteamdata( var_6, "players" );

        foreach ( var_13 in var_11 )
        {
            var_13 scripts\mp\gametypes\br::stopchallengetimers();
            var_13 scripts\mp\gametypes\br::givematchplacementchallenge( var_9 );

            if ( !var_13 scripts\mp\utility\game::rankingenabled() || !var_13 hasplayerdata() )
                continue;

            var_13 scripts\mp\utility\stats::incpersstat( "cash", int( var_7 / 10000 ) );
            var_14 = var_13.pers["combatXP"];

            if ( !isdefined( var_14 ) )
                var_14 = 0;

            var_13 setplayerdata( "mp", "aarValue", 0, var_14 );
            var_15 = var_13.pers["missionXP"];

            if ( !isdefined( var_15 ) )
                var_15 = 0;

            var_13 setplayerdata( "mp", "aarValue", 1, var_15 );
            var_16 = var_13.pers["lootingXP"];

            if ( !isdefined( var_16 ) )
                var_16 = 0;

            var_13 setplayerdata( "mp", "aarValue", 2, var_16 );
            var_17 = 0;

            if ( isdefined( var_13.plundercount ) )
                var_17 = int( var_13.plundercount * var_0 );

            var_18 = 0;

            if ( isdefined( var_13.plunderbanked ) )
                var_18 = int( var_13.plunderbanked * var_1 );

            var_19 = var_17 + var_18;

            if ( var_19 > 0 )
                var_13 scripts\mp\rank::giverankxp( "cash_conversion_bonus", var_19, undefined, 1, 1 );

            var_13 setplayerdata( "mp", "aarValue", 3, var_19 );
            var_20 = 0;

            if ( isdefined( var_13.matchbonus ) )
                var_20 = int( var_13.matchbonus );

            var_13 setplayerdata( "mp", "aarValue", 4, var_20 );

            if ( var_10 > 0 )
                var_13 scripts\mp\rank::giverankxp( "placement_bonus", var_10, undefined, 1, 1 );

            var_13 setplayerdata( "mp", "aarValue", 5, var_10 );
            var_21 = var_13 getplayerdata( "mp", "aarValue", 6 );
            var_22 = var_21 + var_13.pers["summary"]["xp"];
            var_13 setplayerdata( "mp", "aarValue", 7, var_22 );
        }
    }
}

initendcameralocations()
{
    var_0 = [];
    level.endcameraorigins = [];
    level.endcameraangles = [];

    if ( level.mapname == "mp_br_mechanics" )
    {
        level.endcameraorigins[0] = ( 8682, -1036, 427 );
        level.endcameraangles[0] = ( 14, 163, 0 );
        level.endcameraorigins[1] = ( -1139, -3425, 1116 );
        level.endcameraangles[1] = ( 33, 75, 0 );
        level.endcameraorigins[2] = ( -5567, -4786, 1116 );
        level.endcameraangles[2] = ( 37, 192, 0 );
    }
    else
    {
        level.endcameraorigins[0] = ( -36548, -31983, 2400 );
        level.endcameraangles[0] = ( 12, 72, 0 );
        level.endcameraorigins[1] = ( -17592, -36440, 1379 );
        level.endcameraangles[1] = ( 17, 90, 0 );
        level.endcameraorigins[2] = ( -3520, -34298, 1217 );
        level.endcameraangles[2] = ( 11, 110, 0 );
        level.endcameraorigins[3] = ( -9577, -25957, 360 );
        level.endcameraangles[3] = ( 357, 82, 0 );
        level.endcameraorigins[4] = ( 23022, -26926, 1359 );
        level.endcameraangles[4] = ( 16, 101, 0 );
        level.endcameraorigins[5] = ( 31261, -29753, 1359 );
        level.endcameraangles[5] = ( 27, 52, 0 );
        level.endcameraorigins[6] = ( 44843, -41261, 3220 );
        level.endcameraangles[6] = ( 16, 52, 0 );
        level.endcameraorigins[7] = ( 44229, -15403, 1331 );
        level.endcameraangles[7] = ( 13, 72, 0 );
        level.endcameraorigins[8] = ( 44491, 3484, 1638 );
        level.endcameraangles[8] = ( 23, 11, 0 );
        level.endcameraorigins[9] = ( 16047, -3206, 2613 );
        level.endcameraangles[9] = ( 27, 309, 0 );
        level.endcameraorigins[10] = ( 5668, -5905, 1614 );
        level.endcameraangles[10] = ( 23, 304, 0 );
        level.endcameraorigins[11] = ( -13412, -20443, 1033 );
        level.endcameraangles[11] = ( 11, 109, 0 );
        level.endcameraorigins[12] = ( -30369, -7811, 1680 );
        level.endcameraangles[12] = ( 31, 339, 0 );
        level.endcameraorigins[13] = ( -26278, 4081, 142 );
        level.endcameraangles[13] = ( 6, 110, 0 );
        level.endcameraorigins[14] = ( -16429, 6021, 847 );
        level.endcameraangles[14] = ( 21, 57, 0 );
        level.endcameraorigins[15] = ( -7525, 11672, 1082 );
        level.endcameraangles[15] = ( 14, 46, 0 );
        level.endcameraorigins[16] = ( 8356, 15296, 2021 );
        level.endcameraangles[16] = ( 12, 38, 0 );
        level.endcameraorigins[17] = ( 26010, 29975, 2716 );
        level.endcameraangles[17] = ( 13, 68, 0 );
        level.endcameraorigins[18] = ( 12043, 30910, 3081 );
        level.endcameraangles[18] = ( 21, 88, 0 );
        level.endcameraorigins[19] = ( 7127, 52592, 2100 );
        level.endcameraangles[19] = ( 28, 241, 0 );
        level.endcameraorigins[20] = ( -6693, 56481, 4026 );
        level.endcameraangles[20] = ( 16, 246, 0 );
        level.endcameraorigins[21] = ( -21394, 37175, 757 );
        level.endcameraangles[21] = ( 2, 124, 0 );
        level.endcameraorigins[22] = ( -26151, 25577, 271 );
        level.endcameraangles[22] = ( 357, 10, 0 );
    }

    if ( 0 )
        endcameradebug_think();
}

triggerbloodmoneyendcameras()
{
    var_0 = 0;

    foreach ( var_2 in level.players )
    {
        var_2 thread playmatchendcamera( var_2, getbestcameraindex( var_2 ) );
        var_0++;

        if ( var_0 == 5 )
        {
            waitframe();
            var_0 = 0;
        }
    }
}

endcameradebug_think()
{
    for (;;)
    {
        var_0 = getdvarint( "scr_bmo_testEndCamera", -1 );

        if ( var_0 > -1 )
        {
            playmatchendcamera( level.players[0], getbestcameraindex( level.players[0] ) );
            setdvar( "scr_bmo_testEndCamera", -1 );
        }

        waitframe();
    }
}

getbestcameraindex( var_0 )
{
    var_1 = undefined;
    var_2 = undefined;

    foreach ( var_6, var_4 in level.endcameraorigins )
    {
        var_5 = distance2dsquared( var_0.origin, var_4 );

        if ( var_5 <= 9000000 )
            return var_6;

        if ( !isdefined( var_2 ) || var_2 > var_5 )
        {
            var_2 = var_5;
            var_1 = var_6;
        }
    }

    return var_1;
}

playmatchendcamera( var_0, var_1 )
{
    var_2 = level.endcameraorigins[var_1];
    var_3 = level.endcameraangles[var_1];
    var_4 = var_2 + anglestoright( var_3 ) * 1000;
    var_5 = var_3;
    var_6 = spawn( "script_model", var_2 );
    var_6 setmodel( "tag_origin" );
    var_6.angles = var_3;
    var_0 cameralinkto( var_6, "tag_origin" );
    var_6 moveto( var_4, 60 );
    var_6 rotateto( var_5, 60 );

    if ( 0 )
    {
        wait 5;
        var_0 cameraunlink();
    }
}

printallteamsplundercount()
{
    wait 1;
    var_0 = getwincost();
    var_1 = tablesort( game["teamPlacements"], "up" );

    for ( var_2 = 0; var_2 < var_1.size - 1; var_2++ )
    {
        if ( isdefined( level.teamdata[var_1[var_2]]["plunderTeamTotal"] ) )
            var_3 = getteamplunder( var_1[var_2] ) * 100;
    }
}

initdialog()
{
    wait 1;
    game["dialog"]["gametype"] = "gametype_bmo_plunder";
    game["dialog"]["match_start"] = "gametype_bmo_plunder";
    game["dialog"]["boost_short"] = "boost_bmo_short";
    game["dialog"]["offense_obj"] = "boost_bmo";
    game["dialog"]["defense_obj"] = "boost_bmo";
    game["dialog"]["contract_hold_area"] = "bm_contract_hold_area";
    game["dialog"]["contract_loot_chests"] = "bm_contract_loot_chests";
    game["dialog"]["contract_kill_target"] = "bm_contract_kill_target";
    game["dialog"]["event_chopper"] = "bm_event_chopper";
    game["dialog"]["event_airdrop"] = "bm_event_airdrop";
    game["dialog"]["extract_enabled"] = "bm_extract_enabled";
    game["dialog"]["gamestate_25_perc"] = "bm_gamestate_25_perc";
    game["dialog"]["gamestate_50_perc"] = "bm_gamestate_50_perc";
    game["dialog"]["gamestate_75_perc"] = "bm_gamestate_75_perc";
    game["dialog"]["gamestate_90_perc"] = "bm_gamestate_90_perc";
    game["dialog"]["gamestate_25_perc_enemy"] = "bm_gamestate_25_perc_enemy";
    game["dialog"]["gamestate_50_perc_enemy"] = "bm_gamestate_50_perc_enemy";
    game["dialog"]["gamestate_75_perc_enemy"] = "bm_gamestate_75_perc_enemy";
    game["dialog"]["gamestate_90_perc_enemy"] = "bm_gamestate_90_perc_enemy";
    game["dialog"]["gamestate_25_perc_first"] = "bm_gamestate_25_perc_first";
    game["dialog"]["gamestate_50_perc_first"] = "bm_gamestate_50_perc_first";
    game["dialog"]["gamestate_75_perc_first"] = "bm_gamestate_75_perc_first";
    game["dialog"]["gamestate_90_perc_first"] = "bm_gamestate_90_perc_first";
    game["dialog"]["lead_lost"] = "bm_gamestate_lead_lost";
    game["dialog"]["lead_taken"] = "bm_gamestate_lead_taken";
    game["dialog"]["mission_failure"] = "bm_gamestate_lost";
    game["dialog"]["mission_success"] = "bm_gamestate_win";
    game["dialog"]["gamestate_top_3"] = "bm_gamestate_top_3";
    game["dialog"]["gamestate_top_5"] = "bm_gamestate_top_5";
    game["dialog"]["gamestate_top_10"] = "bm_gamestate_top_10";
    game["dialog"]["bm_gamestate_overtime"] = "bm_gamestate_overtime_million_cash_deposited";
    game["dialog"]["bm_gamestate_overtime_enemy"] = "bm_tut_get_cash";
    game["dialog"]["bm_tut_get_cash"] = "bm_tut_get_cash";
    game["dialog"]["bm_tut_earn_cash"] = "bm_tut_earn_cash";
    game["dialog"]["bm_tut_loot_cash"] = "bm_tut_loot_cash";
    game["dialog"]["event_bank"] = "event_bank";
    game["dialog"]["exfil_arrived"] = "exfil_arrived";
    game["dialog"]["exfil_failed"] = "exfil_failed";
    game["dialog"]["exfil_inbound"] = "exfil_inbound";
    game["dialog"]["exfil_leaving"] = "exfil_leaving";
    game["dialog"]["exfil_start_generic"] = "exfil_start_generic";
    game["dialog"]["exfil_start_win"] = "exfil_start_win";
    game["dialog"]["exfil_start_win_lz"] = "exfil_start_win_lz";
    game["dialog"]["exfil_success_full"] = "exfil_success_full";
    game["dialog"]["exfil_success_partial"] = "exfil_success_partial";
}

watchparachutersoverhead( var_0 )
{
    level endon( "game_ended" );
    level endon( "cancel_watch_parachuters_overhead" );

    for (;;)
    {
        foreach ( var_15, var_2 in level.activeparachuters )
        {
            if ( !isdefined( var_2 ) || !scripts\mp\utility\player::isreallyalive( var_2 ) || !( var_2 isparachuting() || var_2 isinfreefall() ) )
            {
                level.activeparachuters[var_15] = undefined;
                continue;
            }

            var_3 = scripts\common\utility::playersincylinder( var_2.origin, level.parachuteoverheadwarningradius, undefined, level.parachuteoverheadwarningheight );
            var_4 = var_2.team;

            foreach ( var_6 in var_3 )
            {
                if ( isdefined( var_6.c130 ) )
                    continue;

                var_7 = var_4 == var_6.team;

                if ( var_7 )
                    continue;

                var_8 = !scripts\mp\utility\player::isreallyalive( var_6 ) || istrue( var_6.inlaststand );

                if ( var_8 )
                    continue;

                var_9 = var_6 isparachuting() || var_6 isinfreefall();

                if ( var_9 )
                    continue;

                var_10 = gettime();
                var_11 = isdefined( var_6.heardparachuteoverheadtime ) && var_10 - var_6.heardparachuteoverheadtime < var_0;

                if ( var_11 )
                    continue;

                var_6.heardparachuteoverheadtime = var_10;
                var_12 = scripts\mp\gametypes\br_public::brgetoperatorteam( var_6 );
                var_13 = game["voice"][var_12];
                var_6 queuedialogforplayer( level.activeparachutersfactionvo[var_13], "respawning_enemy_in_area", 2 );
            }

            waitframe();
        }

        waitframe();
    }
}
